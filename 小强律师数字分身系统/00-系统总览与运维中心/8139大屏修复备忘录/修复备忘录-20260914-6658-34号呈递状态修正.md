# 修复备忘录 · 8139 大屏修复工作流

**编号**：修复备忘录-20260914-6658-34号呈递状态修正
**日期**：2026-09-14
**工作流**：8139 大屏修复工作流（v1.0.1）
**子屏**：C4 案件生命周期大屏（clm）

---

## 一、受理（用户输入）

> 34 号庭后补充意见（证据⑨强化意见一版）已在一个周前已呈递法院，请修正，按修复工作流处理。

- **归属案件**：(2026)黔0330民初6658号 丁戊祥诉刘九零、刘维吊顶买卖合同纠纷案（被告方，代理）
- **事实**：34 号庭后补充意见（证据⑨强化意见一版）已于 **2026-09-07**（用户所称"一周前"，与 d1.due 一致）呈递法院
- **系统失真**：case-state 单一事实源仍记为"待呈递"，大屏 C4 展示链脱节

## 二、诊断（根因）

**数据错（源数据过时 + 大屏展示链脱节，双层问题）**

1. **源数据 case-state.json 过时**（7 处）：
   - `deadlines.d1.state` = `"pending"`
   - `gates.G4.status` = `"active"`（note："34 号交当事人核验中，待核验通过即呈递"）
   - `stage.note` = "第一次开庭已毕，择期宣判；庭后补充意见呈递中"
   - `nextActions[0]` = "34 号交刘九零/刘维核验，核验通过即呈递（星期一）"
   - `publicView.next` = "呈递庭后补充意见（预计 2026-09-07），其后等待法院宣判"
   - `timeline` 缺 2026-09-07 呈递事件
   - `updatedAt` 停于 `2026-09-05`

2. **大屏展示链脱节（比雅菲案更深一层的断点）**：
   - scan_cases.py 权威输出在 `skills/案件生命周期大屏流水线/dashboard/案件生命周期大屏.html`
   - **8139 C4 实际展示页 `subapps/clm/index.html` 是 2026-09-10 生成的硬编码快照**（DATA 写死在 JS、caseCount=2、d1.state 仍 `"pending"`），不实时读取 scan_cases 输出
   - cockpit-hub-ops README 标注 C4 数据源 `Claw/case-lifecycle/dashboard/` 是又一份旧副本（简报仍写"预计呈递"）
   - `refresh_8139.py` 的 `clm` 项配置（输出文件名 `index.html` / 前端渲染格式）与 scan_cases 实际产物（`案件生命周期大屏.html` 服务端渲染）**不匹配 → 设计缺口**，原地跑刷新无效

## 三、修复动作（带护栏）

1. **先备份**（铁律，三处独立时间戳）：
   - `case-state.json` → `/tmp/6658-34fix-20260914-193641/`
   - `Claw/case-lifecycle` 旧副本 → `/tmp/clm-claw-sync-20260914-194003/`
   - `subapps/clm/index.html` 旧快照 → `/tmp/clm-subapps-sync-20260914-194059/`

2. **修正源数据**（7 处，全部实地读真卷提取，未编造）：
   - `deadlines.d1.state`: `pending` → `done`，note 补"已于 2026-09-07 呈递法院（与 31/33 号一并归整呈递）"
   - `gates.G4.status`: `active` → `pass`，note 改"34 号已交当事人核验通过，并于 2026-09-07 呈递法院"
   - `stage.note`: 改"…34 号庭后补充意见（证据⑨强化意见一版）已于 2026-09-07 呈递法院"
   - `nextActions[0]`: 改"34 号…已于 2026-09-07 呈递法院，持续跟踪择期宣判节点"
   - `publicView.next`: 改"34 号…已于 2026-09-07 呈递法院，其后等待法院宣判"
   - `timeline`: 追加 `{date:2026-09-07, event:"34 号庭后补充意见（证据⑨强化意见一版）呈递法院（与 31/33 号一并归整呈递）", key:true}`
   - `updatedAt`: `2026-09-05` → `2026-09-14`

3. **刷新 scan_cases.py**（先 `--dry-run` → 全量 + 脱敏版）：JSON 合法，脱敏闸门 **PASS 零泄漏**

4. **同步 8139 C4 展示链（关键补漏）**：把 scan_cases 最新产物 cp 到
   - `subapps/clm/index.html`（8139 C4 实际展示页，覆盖 2026-09-10 硬编码快照）
   - `Claw/case-lifecycle/dashboard/案件生命周期大屏.html` + `_脱敏版.html`
   - `Claw/case-lifecycle/out/对外简报-6658.md`

## 四、验证（修复成果用户可见）

- ✅ `case-state.json` JSON 合法，7 处字段全改落位
- ✅ 8139 服务 `HTTP 200`，C4 子屏 `HTTP 200`
- ✅ `subapps/clm/index.html` 含 6658 案（4 处命中）+ d1 "34 号…呈递" + `2026-09-07`
- ✅ 脱敏版零泄漏

## 五、遗留 / 待办

- [ ] **`refresh_8139.py` 的 `clm` 配置缺口**：输出文件名（`index.html`）与格式（前端渲染）均不匹配 scan_cases 实际产物（`案件生命周期大屏.html` 服务端渲染），致 `refresh_8139.py --screen clm` 无法正确刷新 C4。建议下次单独修：将 clm 项改为直接 cp scan_cases 最新产物到 `subapps/clm/index.html`（即本次手动动作固化为脚本），或让 scan_cases 输出文件名对齐 `index.html`。
- [ ] 8139 C4 子屏现展示 scan_cases **全量大屏（13 案）** 而非原 2 案精简快照，视觉风格与 cockpit 深色框架不完全一致（功能正确、显示最新全量数据）。若需还原精简 2 案视图须另做模板。
- [ ] 其余 12 案 `case-state` 仍缺 `caseStateDetail`（DTC-CASE-001 壳案），不在本次范围。

## 六、结论

(2026)黔0330民初6658号 34 号庭后补充意见（证据⑨强化意见一版）呈递状态已修正为「**已呈递（2026-09-07）**」，并同步至 8139 大屏 C4 实际展示链（subapps/clm + Claw 副本）。老强**刷新 127.0.0.1:8139 的 C4 子屏**即可见修复成果。
