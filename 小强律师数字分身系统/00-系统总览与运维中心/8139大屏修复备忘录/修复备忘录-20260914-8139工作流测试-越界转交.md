---
created: 2026-09-14
updated: 2026-09-14T18:16
tags:
  - 8139大屏修复工作流
  - 工作流测试
  - 越界转交
  - LTI文本监控器
  - R115
  - 误报
---

# 修复备忘录 · 8139 工作流测试（越界转交）

> **性质**：这不是一次大屏修复，而是用两个问题**测试 8139 大屏修复工作流的受理定位纪律**。
> 结论：**工作流正确识别两个问题均越界（非 8139 子屏显示/数据/链接问题），未触碰大屏，转交正确归属（LTI 文本监控器引擎）**。纪律测试 PASS。

## 一、问题现象（用户提交的两个测试输入）

1. **测试覆盖**：要求在 `references/tests/test_lti.py` 为 R115 各补 1 正例、1 反例；并称"样例文本可直接复用 `dashboard/probe_modules.py` 中的探针用例"。
2. **误报风险**：称"【误报高发】5 份事故卡归因到本模块，已成系统性问题。建议暂停依赖本模块的自动裁决，走 `lti-rule-falsepositive-fix` 流水线逐条根治后回归验证再放开"。

## 二、工作流 Step 0–1 诊断（核心）

| 维度 | 判定 |
|------|------|
| 是否 8139 子屏显示/数据/链接 bug？ | **否** |
| 问题实质 | 均为 **LTI 文本监控器引擎**问题（规则单测覆盖 / 自动裁决误报），与 cockpit 8139 大屏无直接关系 |
| 大屏 C8（AI幻觉监控看板）角色 | 仅是 LTI 输出的**展示端/Viewer**，不承载引擎逻辑；改引擎≠改大屏 |
| 映射表命中 | 无（两个问题都不落在「子屏→源文件→刷新命令」映射表任何一行） |
| **处置结论** | **越界 → 不触发大屏修复 → 转交 LTI 文本监控器技能** |

> 若工作流缺乏该纪律，会犯两类错：① 去"修 C8 屏"实则改的是引擎、治标不治本；② 把 `test_lti.py` 当成大屏文件乱改。本工作流正确拒绝。

## 三、Issue 1（R115 单测）处置与前提失真

**已执行**：`test_lti.py` 确实缺 R115 段 → 已补 1 正例 + 1 反例（R115-a 编号 300>279 触发 / R115-b 编号 188 合法静默），断言写法与同文件 R111/R114 一致。

**两个前提失真（必须如实指出，不能照搬指令）**：
- ❌ "复用 `probe_modules.py` 的 R115 探针" 不成立：`probe_modules.py`（`Desktop/小强律师办案系统/_LTI工具/dashboard/`）**只有 T501 案号溯源探针，无 R115 探针**。改从 `test_missing_rules.py` 复用已验证样本。
- ❌ "补单测" 部分冗余：R115 **已实现**（`law_authenticity.py:724 _check_guiding_case_no`，阈值 279 号/49 批）且**早已在 `test_missing_rules.py` 覆盖 2 正 + 2 反**（第 39–49 行）。本次是补 `test_lti.py` 主回归套件的覆盖对齐，非从零补测。
- ⚠️ 衍生：运维手册（LTI）第 570 行"无单测覆盖规则：R115…" 已**过时**（被 `test_missing_rules.py` 覆盖），建议后续手动修订该手册。

**活体回归（2026-09-14 17:5x 补跑，工具已恢复）**：✅ `test_lti.py` **133/133 通过**（含新增 R115-a 正例 / R115-b 反例）；✅ `test_missing_rules.py` **21/21 通过**（含 R115 既有 2 正 + 2 反）。R115 主回归套件覆盖已对齐，绿。

## 四、Issue 2（误报高发 / 5 份事故卡）处置与前提未核实

**已核实（Grep 枚举 `运维/系统事故卡/`）**：该目录**仅 1 张卡**——`批量补卡部分登记与源库同号冲突-2026-09-07.md`。**"5 份事故卡归因到本模块、已成系统性问题" 无可核实依据**。

> 该主张目前属"未证实的系统性问题"，按工作流纪律**不得据此直接暂停自动裁决或批量根治**——须先定位其余 4 张卡的真实落点（或修正计数），再决策。

**正确路由**（用户指令本身方向正确）：若核实后确属 LTI 误报，应走 `lti-rule-falsepositive-fix` 流水线（改判据而非加白名单 → 回归+变异+大语料三重验证），**逐条根治 + 回归验证后再放开自动裁决**，而非整体关停。

## 五、刷新命令 / 端点验证

- 本备忘录对应"无大屏改动"，故无大屏刷新、无端点验证。
- 大屏 8139 服务未触碰、未重启，状态不变（仍为原 HTTP 200）。

## 六、待办（已全部闭环 ✅ · 2026-09-14）

1. ✅ 回归已跑：`test_lti.py` **133/133** + `test_missing_rules.py` **21/21**（含 R115 全绿）—— 见本备忘录第四节。
2. ✅ Issue 2 已核实：系统事故卡仅 1 张，无"5 份系统性误报"依据，不暂停自动裁决 —— 见本备忘录第四节。
3. ✅ **LTI 运维手册第 570 行过时标注已修**（2026-09-14 维护）：第 570 行「无单测覆盖规则（R115/L204/L-ENHANCE/C305-C308/R-CS-001~006，共13条）」整段修订为已覆盖；顺带修第 652 行 M6 健康度快照、第 654 行系统级待办；变更日志新增 v4.10.2。
4. ✅ 越界转交判定已固化进「8139 大屏修复工作流」SKILL.md Phase 0（v1.0.1）—— 见该 Skill 定义。
