# 修复备忘录 · 2026-09-17 · D5 继承链可视化

## 报障/需求
老强（2026-09-17 00:33）：D5 权限管理中心「用户组」模块已建好，要求再加**「组 → 角色 → 权限项继承链可视化」**。

## 根因 / 设计
用户组模块（v1.3.5）已有权限基线编辑（继承角色下拉 + 额外权限勾选），但**看不到"基线最终展开成哪些权限项"**，主任难以直观核对批量镜像后会落到组员身上的具体权限。
- 数据链路已具备：`ROLES[k].p`（角色→权限项 map）+ `PERM`（5 类权限项分组）+ `g.role`+`g.extra`（组继承来源）。
- 设计：竖向链路卡片——① 组节点（维度徽章+名称）→ ② 继承角色节点（按 PERM 5 类分组列出角色 `p=1` 的权限项 chip）→ ③ 组叠加节点（仅组额外且角色未含的权限，标「＋组」）。

## 改动（3 处串行编辑）
1. CSS：`</style>` 前补继承链样式（`.inh` / `.inh-node.inh-group|inh-role|inh-extra-node` / `.inh-arrow` / `.inh-cat` / `.inh-chip(.inh-extra)`），dark 主题与现有一致。
2. 模板：`renderGroupDetail` 权限基线 block 后插入「🔗 继承链可视化」gblock，调用 `inheritChainHTML(g)`。
3. JS：新增 `inheritChainHTML(g)`——`rp=ROLES[g.role]?.p||customRoles.p||{}`；按 `PERM` 分类筛出 `rp[k]||extraSet.has(k)` 的 chip（纯组叠加标「＋组」）；`pureExtra=g.extra.filter(k=>!rp[k])` 单独成节点；空角色显示兜底文案。

## 护栏 / 验证
- 备份 `/tmp/9360fix-20260917-003500-permcenter-inherit.html`。
- 9360 服务 200；D5 页面 200；curl 结构命中（inh-node inh-group / 标题 / inheritChainHTML 各 1+）。
- 浏览器级体检（agent-browser）：切用户组→选「民商事诉讼组」→ **JS 错误 0**；组节点✅、角色节点✅(执业律师)、3 类权限项 catRows、25 个 chip、组叠加节点✅(腾讯公益平台 ＋组)、箭头✅。
- 边界：组别叠加标签「＋组」区分"角色已有"与"仅组叠加"，继承自角色的不重复计、纯组叠加单列。

## 状态
✅ 已闭环

## 红线
纯前端演示数据·非接生产；真实账号数据不落前端不外发。
