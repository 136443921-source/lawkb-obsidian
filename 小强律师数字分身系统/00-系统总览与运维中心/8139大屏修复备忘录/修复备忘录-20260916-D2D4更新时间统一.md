---
标题: 修复备忘录-20260916-D2D4更新时间统一
日期: 2026-09-16
来源AI: WorkBuddy 小强
屏幕: D2 知识冲突中台 / D4 心智模型中台（D3 云监控除外·外链）
类别: 渲染错（头部信息统一化，续 v1.2.9/v1.3.1）
状态: 已闭环（D3 待老强确认）
---

# 修复备忘录 · 20260916 · D2/D4「数据更新时间」徽章补齐（参照 D1）

## 问题现象（老强报障）
首页评分卡 D1 有「📊 数据更新时间」徽章，D2/D3/D4 没有，要求统一化、参照 D1。

## 根因分类
**渲染错 + 历史改造漏项**。D1(credits) 在 v1.2.9 已注入 `data-update-time` 徽章 span + `.updTag` 样式。但：
- D2(conflict-arbitration)、D4(mind-models) 当时分别由「另一会话」在 v1.2.9 之后新建/接入，未被 `fix_update_time_anchor.py` 覆盖；
- v1.3.1 的 `sync-time-injected` 脚本**只写了查找 `data-update-time`/`#sub` 的逻辑，却漏了在 h1 内真正插入徽章 span + `.updTag` 样式**——于是脚本运行但找不到元素 → 标题旁无时间。

## 改动文件（唯一落点 cockpit-hub-ops，符合 v1.2.3 铁律）
1. `subapps/conflict-arbitration/index.html`（D2）：
   - `</style>` 前注入 `.updTag` 样式块（与 D1 同款深底绿字胶囊）
   - h1 内补 `<span id="data-update-time" class="updTag">…</span>`（紧跟「D2 知识冲突中台」）
2. `subapps/mind-models/index.html`（D4）：
   - 同上两处
- **备份**：`/tmp/9360fix-20260916-233206-conflict-index.html`、`/tmp/9360fix-20260916-233206-mindmodel-index.html`

## 不做（D3 云监控）
D3 在 `rbac.js` 中 `url:"https://xiaoqiang-cloud-service.app.workbuddy.host/"` —— **外部链接屏，9360 本地无对应 index.html，无法在本地注入徽章**。老强如需统一，须在云端服务自身页面处理，或把 D3 改成本地子屏镜像。已向老强说明。

## 刷新与验证
- 静态屏文件即生效；9360 服务 200
- 线上验证：D2/D4 顶栏 = `D2/D4 … ＋ 📊数据更新时间：…（北京时间）` 徽章 + `h1 .updTag` 样式命中 ✅
- 时间由 `sync-time-injected` 按 `document.lastModified` 真实覆盖（D2/D4 既有 #sub 文本替换兜底同步保留）

## 待办 / 提醒
- 老强浏览器切到 D2/D4 标签页即可见（cache-busting 已带）；若旧缓存显旧，Cmd+Shift+R
- 沉淀：v1.3.1 的 `sync-time-injected` 脚本与「徽章 span 注入」是**两段独立动作**，后续新增屏接入时须二者齐做，否则会重现"脚本在跑但标题无时间"
