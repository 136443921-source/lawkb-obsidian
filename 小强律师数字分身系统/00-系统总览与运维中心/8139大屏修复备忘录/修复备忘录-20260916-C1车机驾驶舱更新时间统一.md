---
标题: 修复备忘录-20260916-C1车机驾驶舱更新时间统一
日期: 2026-09-16
来源AI: WorkBuddy 小强
屏幕: C1 车机驾驶舱
类别: 渲染错（头部信息统一化）
状态: 已闭环
---

# 修复备忘录 · 20260916 · C1 车机驾驶舱「更新时间统一 + 删运维手册行」

## 问题现象（老强报障）
1. C2 决策思维舱标题旁有「📊 数据更新时间」徽章，C1 车机驾驶舱顶部通栏标题旁没有——两屏不统一。
2. C1 顶部通栏「按《数字分身系统运维手册（EV 版）》组织 数据 2026-09-12 10:22 · 4 天前」一行，老强要求删掉。

## 根因分类
**渲染错**。EV 屏的 `data-update-time` 徽章此前（v1.2.9 改造时）插在页面下方 HERO 区 h1 内，顶部 sticky 通栏（首屏最显眼位置）只有 `EV v2.0.0` 徽章 + 标题 + `tb-s` 块（手册行 + 数据时间行），首屏视觉上"没有更新时间"。

## 改动文件
`cockpit-hub-ops/subapps/ev/index.html`（唯一改动落点，符合 v1.2.3 铁律），4 处串行编辑：
1. CSS 选择器 `h1 .updTag` → `.updTag`（徽章样式适配顶栏非 h1 容器）
2. 顶栏重构：删 `tb-s` 整块（手册行+数据行），徽章挪入顶栏紧跟标题
3. HERO h1 移除徽章（避免同 id 重复）
4. JS 清理：删 `$('tb-time').textContent=...`（防 TypeError 中断渲染脚本）；`['age','tb-age']` → `['age']`

**备份**：`/tmp/9360fix-20260916-231932-ev-index.html`

## 刷新与验证
- 静态屏，文件即生效；服务存活 `curl 9360` → 200
- 线上验证：`tb-time/tb-age/运维手册组织` 残留 **0**；顶栏结构 = `EV v2.0.0 + 标题 + 📊数据更新时间徽章` ✅
- 徽章时间由页尾 `sync-time-injected` 脚本按 id `data-update-time` 用 `document.lastModified` 实时覆盖，挪位置后联动不受影响

## 待办 / 提醒
- 老强浏览器需**硬刷新（Cmd+Shift+R）**可见（cache-busting 已在 auth.js 切屏时自动带上，一般直接切页即可）
- 顶栏「数据年龄（·N天前）」随手册行一并删除，HERO 区 meta 仍保留「数据/人工核验/手册」完整信息行，信息未丢失
