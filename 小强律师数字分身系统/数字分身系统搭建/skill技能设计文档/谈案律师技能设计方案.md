---
created: 2026-06-02T23:22
updated: 2026-08-28T19:36
title: 谈案律师技能设计方案
tags: []
maturity: 🌳核心
source: ""
related: []
last_review: ""
review_interval: 30
difficulty: 3
importance: 3
status: draft
---

[[小强律师数字分身系统/数字分身系统搭建/skill技能设计文档/谈案律师技能设计方案_20260528|谈案律师技能设计方案]]_20260528|谈案律师技能设计方案_20260528|谈案律师技能设计方案_20260528|谈案律师技能设计方案_20260528|谈案律师技能设计方案_20260528|谈案律师技能设计方案_20260528|谈案律师技能设计方案_20260528|谈案律师技能设计方案_20260528.md

1. **案情采集阶段**：
    
    - 所有对话记录 → 只保存在本地临时文件（如：`/tmp/tanan_lawyer/conversation_history.txt`）
    - 禁止上传到：腾讯文档、iCloud、Google Drive、任何云端API
2. **材料解析阶段**：
    
    - 所有解析结果 → 只保存在本地临时目录（如：`/tmp/tanan_lawyer/parsed_materials/`）
    - 原始材料文件 → 只保存在本地（你不主动删除，就不会被删除）
    - 禁止上传到：任何云端存储服务
3. **法律检索阶段**：
    
    - 所有检索结果 → 只保存在本地缓存目录（如：`/Users/chenyouqiang/Documents/LawKB/法律检索缓存/`）
    - 禁止上传到：任何云端数据库或API
4. **报告生成阶段**：
    
    - 最终报告 → 只保存在本地Obsidian目录（如：`/Users/chenyouqiang/Documents/LawKB/法律文书/案件分析报告/`）
    - 禁止自动同步到：iCloud、Google Drive、腾讯文档等云存储服务
5. **LTI审核阶段**：
    
    - 所有审核记录和结果 → 只保存在本地临时文件
    - 禁止将任何案件信息传输到云端进行AI审核

---

### 3.2 原则2：门禁原则（访问控制）

**核心目标**：  
只有授权用户（老强）才能查看和使用这些案件信息。其他用户或进程无法访问。

**具体实现**：

1. **文件系统权限控制**（macOS）：
    
    - 所有案件相关目录和文件 → 设置权限为`chmod 600`（只有所有者可以读写）
    - 示例代码：
        
        ```
        import os
        import stat
        
        def set_secure_permissions(file_path):
            """设置文件权限为600（只有所有者可以读写）"""
            os.chmod(file_path, stat.S_IRUSR | stat.S_IWUSR)
        ```
        
2. **进程隔离**：
    
    - "谈案律师技能"运行在独立的进程空间中
    - 其他技能或应用程序无法访问其内存空间
3. **用户认证**（可选，未来添加）：
    
    - 每次启动"谈案律师技能"时，需要输入密码或生物识别（如：Touch ID）
    - 防止他人未经授权使用

---

### 3.3 原则3：总闸原则（一键切断数据外流）

**核心目标**：  
设置一个总开关，可以一键切断所有数据外流（网络传输、云同步、外部API调用）。

**具体实现**：

1. **总闸开关**：
    
    - 在技能配置文件中设置：`CLOUD_ENABLED = False`（默认关闭）
    - 示例代码（`tanan_lawyer_config.py`）：
        
        ```
        # 谈案律师技能配置文件
        CLOUD_ENABLED = False  # 总闸：禁止所有云端传输
        
        # 如果CLOUD_ENABLED = False，以下功能将被禁用：
        # 1. 禁止调用任何云端API（如：腾讯文档API、iCloud API、外部法律数据库API）
        # 2. 禁止上传任何文件到云端
        # 3. 禁止使用任何需要网络传输的功能（如：在线OCR、在线语音识别）
        # 4. 只能使用本地功能（如：本地PDF解析、本地Word解析、本地OCR、本地语音识别）
        ```
        
2. **网络传输拦截**：
    
    - 在技能代码中，所有网络请求都必须先检查`CLOUD_ENABLED`开关
    - 如果`CLOUD_ENABLED = False`，则禁止发送任何网络请求
    - 示例代码：
        
        ```
        import requests
        
        def safe_http_request(url, data):
            """安全的HTTP请求（遵守总闸原则）"""
            if not CLOUD_ENABLED:
                raise Exception("总闸已关闭：禁止所有云端传输")
            
            # 只有在总闸开启时，才能发送网络请求
            response = requests.post(url, json=data)
            return response
        ```
        
3. **云同步拦截**：
    
    - 在技能代码中，所有文件保存操作都必须检查`CLOUD_ENABLED`开关
    - 如果`CLOUD_ENABLED = False`，则禁止保存文件到任何云存储目录（如：iCloud目录、Google Drive目录）
    - 示例代码：
        
        ```
        def safe_save_file(file_path, content):
            """安全的文件保存（遵守总闸原则）"""
            if not CLOUD_ENABLED:
                # 总闸关闭：只能保存到本地非云存储目录
                if is_cloud_directory(file_path):
                    raise Exception("总闸已关闭：禁止保存到云存储目录")
            
            # 保存文件
            with open(file_path, 'w') as f:
                f.write(content)
        ```
        
4. **一键切断功能**：
    
    - 在技能界面中添加一个"一键切断"按钮
    - 点击后，立即设置`CLOUD_ENABLED = False`，并杀掉所有网络请求进程

---

### 3.4 原则4：检索环节个人信息脱敏机制（⚠️ 最重要）

**核心目标**：  
在调用"法律检索助手"（可能连接外部API）之前，**自动脱敏所有个人信息**，防止身份证号、电话、住址、照片等外泄。

**具体实现**：

#### 1. 脱敏模块集成到"谈案律师技能"中

- 在技能代码中，增加一个**"个人信息脱敏模块"**（`desensitize_personal_info.py`）
- 在调用"法律检索助手"之前，必须先调用脱敏模块

#### 2. 脱敏流程

1. **收集案情信息和证据材料**
2. **调用脱敏模块**，对以下信息进行脱敏：
    - 姓名 → `[当事人A]`、`[当事人B]`等
    - 身份证号 → `[身份证号]`
    - 联系电话 → `[联系电话]`
    - 家庭住址 → `[住址]`
    - 照片 → `[照片]`（或禁止上传）
    - 银行卡号 → `[银行卡号]`
    - 电子邮箱 → `[电子邮箱]`
    - 等等
3. **将脱敏后的信息**发送给"法律检索助手"进行检索
4. **检索完成后** → 可选：将脱敏信息还原为原始信息（由你选择）

#### 3. 脱敏模块代码示例（Python）

```
import re

def desensitize_personal_info(text):
    """脱敏个人信息"""
    # 1. 脱敏姓名（需要使用NER模型或规则匹配）
    text = re.sub(r'王德明', '[当事人A]', text)
    text = re.sub(r'王方利', '[当事人B]', text)
    
    # 2. 脱敏身份证号
    text = re.sub(r'\d{17}[\dXx]', '[身份证号]', text)
    
    # 3. 脱敏联系电话
    text = re.sub(r'1[3-9]\d{9}', '[联系电话]', text)
    
    # 4. 脱敏家庭住址
    text = re.sub(r'贵州省贵阳市.*?小区', '[住址]', text)
    
    # 5. 脱敏银行卡号
    text = re.sub(r'\d{16,19}', '[银行卡号]', text)
    
    # 6. 脱敏电子邮箱
    text = re.sub(r'\w+@\w+\.\w+', '[电子邮箱]', text)
    
    return text

def desensitize_image(image_path):
    """脱敏图片（遮挡人脸、身份证号、地址等）"""
    # 使用OpenCV或PIL库
    # 1. 人脸检测 → 遮挡人脸
    # 2. 文字检测 → 遮挡身份证号、地址、电话号码等
    # 3. 保存脱敏后的图片
    pass

def desensitize_audio(audio_path):
    """脱敏录音（变声、去除个人信息）"""
    # 使用音频处理库（如：librosa、pydub）
    # 1. 变声处理
    # 2. 去除个人信息（如：姓名、身份证号、电话号码等）
    # 3. 保存脱敏后的录音
    pass
```

#### 4. 还原模块（可选）

- 在检索完成后，你可以选择**是否将脱敏信息还原为原始信息**
- 如果还原，则报告中使用真实信息
- 如果不还原，则报告中使用脱敏信息（如："`[当事人A]`"）

---

### 3.5 原则5：OCR和录音转录功能的安全机制（⚠️ 新增）

**核心目标**：  
如果必须使用在线OCR API或在线语音识别API，则必须先对图片或录音进行**脱敏处理**。

**具体实现**：

#### 1. OCR功能（图片文字识别）

- **优先使用本地OCR工具**（如：Tesseract）
- **如果必须使用在线OCR API**（如：百度OCR API、腾讯OCR API），则：
    - 先对图片进行**脱敏处理**（遮挡人脸、遮挡身份证号、遮挡地址等）
    - 只发送脱敏后的图片给在线OCR API
    - 接收OCR结果后，再还原为原始图片（可选）

#### 2. 录音转录功能（语音转文字）

- **优先使用本地语音识别工具**（如：whisper.cpp）
- **如果必须使用在线语音识别API**（如：讯飞语音转写API、腾讯语音识别API），则：
    - 先对录音进行**脱敏处理**（变声、去除个人信息等）
    - 只发送脱敏后的录音给在线语音识别API
    - 接收转录结果后，再还原为原始录音（可选）

---

### 3.6 原则6：日志记录和审计（可追溯）

**核心目标**：  
记录所有操作日志，便于审计和追溯。日志只保存在本地，不上传。

**具体实现**：

1. **操作日志**：
    
    - 记录所有关键操作（如：文件访问、功能调用、网络请求）
    - 日志格式：
        
        ```
        [2026-05-28 09:45:12] 访问文件：/Users/chenyouqiang/Documents/LawKB/.../王德明案.md
        [2026-05-28 09:46:33] 调用功能：法律检索助手
        [2026-05-28 09:47:15] 网络请求：已阻止（总闸关闭）
        ```
        
    - 日志文件保存位置：`/Users/chenyouqiang/WorkBuddy/Claw/logs/tanan_lawyer.log`
2. **日志安全**：
    
    - 日志文件权限设置为`chmod 600`（只有所有者可以读写）
    - 禁止上传日志到云端

---

## 四、详细工作流程（迭代式 + 安全机制）

### 4.1 阶段1：案情采集（自由对话 + 本地存储）

**流程步骤**：

1. 你启动技能（说"谈案律师"或上传材料）
2. 你自由描述案情（文字、文件上传均可）
3. 技能通过**追问**逐步澄清关键信息
4. **你可以随时回溯修改**
5. 技能**自动判断信息是否完整** → 完整后进入阶段2

## 相关笔记
- [[谈案律师技能设计方案_20260528]] (共现关键词: API, 技能, 律师)
- [[律师数字分身系统技能配置迭代方案 1.0]] (共现关键词: 分身, API, 技能)
- [[知识飞轮系统/03-连接/概念页/战情报告_QC总闸_雅菲软装窗帘案_20260812]] (共现关键词: 总闸, 证据)
- [[小强律师数字分身系统/数字分身系统搭建/skill技能设计文档/小强律师数字分身系统-使用指导手册-v3.0.0]] (共现关键词: 技能, 律师)
- [[小强律师数字分身系统/数字分身系统搭建/小强律师数字分身系统-用户使用手册-v3.0.0]] (共现关键词: 技能, 律师)
- [[v1法律合同审查智能体迭代技能]] (共现关键词: 技能, 证据)
- [[合同审查思维体系]] (共现关键词: 律师, 证据)
- [[律师用AI梳理证据链的5个重点附提示词]] (共现关键词: 律师, 证据)
- [[模拟法庭管理系统/模拟法庭系统搭建/模拟庭审/模拟法庭战情报告_230号_20260806]] (共现关键词: 总闸, 证据)
- [[知识飞轮系统/03-连接/概念页/战情报告_QC总闸_20260812]] (共现关键词: 总闸, 证据)
- [[小强律师数字分身系统/数字分身系统搭建/skill技能设计文档/律师数字分身系统技能配置迭代方案 1.0]] (共现关键词: 证据, 技能, 律师)
- [[如何让AI帮你打理Obsidian知识库]] (共现关键词: 证据, 技能)
- [[小强律师数字分身系统/数字分身系统搭建/案源开拓的四度模型]] (共现关键词: 律师, 谈案)
- [[小强律师数字分身系统/数字分身系统搭建/谈案报价的理念与技巧]] (共现关键词: 律师, 谈案)
- [[装上这7个Skills你的WorkBuddy直接起飞]] (共现关键词: 技能, 律师, 设计方案)
- [[小强律师数字分身系统/数字分身系统搭建/skill技能设计文档/谈案律师技能设计方案_20260528]] (共现关键词: 技能, 律师, 证据)
- [[小强律师数字分身系统/数字分身系统搭建/数字分身系统智能化升级设计方案]] (共现关键词: 证据, 设计方案)
