---
type: meta
title: 每日日志 2026-09-14
status: active
updated: 2026-09-14T18:26
source_ai: workbuddy
scope: global
confidence: high
tags:
  - 每日日志
  - 基建
created: 2026-09-14
---

# 📅 2026-09-14 · AI 共享记忆中枢落成

## 一句话

老强拍板：**Obsidian LawKB = 所有 AI 与设备的唯一永久记忆中枢**，各平台原生 memory 降级为辅助缓存。中枢今日搭建完成并跑通全链路。

## 今日动作清单

| # | 动作 | 结果 |
|---|---|---|
| 1 | 修复 Obsidian MCP 端口 | ✅ `http://27123` → `https://27124`（原配置导致 MCP 报 Not connected） |
| 2 | 建中枢骨架 | ✅ `_AI-Memory-Hub/` 七个目录 |
| 3 | 写共享记忆协议 v1.0 + 仲裁规则 + Schema | ✅ 3 个元规则文件 |
| 4 | 提炼迁移旧 `.workbuddy/memory` | ✅ 5 偏好卡 / 4 项目卡 / 3 决策卡；旧目录降级只读（留 pointer，未删） |
| 5 | 铺设各 AI 全局规则 | ✅ `~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md`、`~/.gemini/GEMINI.md`、`~/.cursorrules`、`~/AGENTS.md`、`LawKB/AGENTS.md`、`~/.workbuddy/MEMORY.md` |
| 6 | 写三个脚本 | ✅ `read_hub.py` / `write_back.py` / `daily_review.py`（+补救脚本 `fix_updated_format.py`） |
| 7 | 建复盘自动化 | ✅ 每日 01:00 复盘 + 每日 08:00 幂等补跑 |

## 踩坑记录（有价值，别忘）

**坑：Obsidian「update-time-on-edit」插件会改写 `updated` 字段**

- 现象：`updated: 2026-09-14` 被自动改成 `updated: 2026-09-14T13:02`
- 根因：插件默认 `dateFormat = yyyy-MM-dd'T'HH:mm`，且 `ignoreGlobalFolder` 为空
- 处置（双层）：
  1. **治本**：在 `data.json` 的 `ignoreGlobalFolder` 加入 `_AI-Memory-Hub` —— **需重启 Obsidian 才生效**（改配置时 Obsidian 持有内存态，会覆盖写入）
  2. **容错**：脚本内加 `norm_date()`，把 `YYYY-MM-DDTHH:MM` 规范化回纯日期，保证仲裁/淘汰逻辑不失效
  3. **补救**：`99-脚本/fix_updated_format.py --apply`（幂等，重启后跑一次即永久干净）

> ⏳ **待办**：重启 Obsidian 后执行
> `cd /Users/chenyouqiang/Documents/LawKB/_AI-Memory-Hub && python3 99-脚本/fix_updated_format.py --apply`

## 复盘首跑（2026-09-13）

- 扫描命中 11 个文件，提炼候选 139 条，新增 139 / 重复 0 / 冲突 0 / 淘汰 0
- 候选质量可用：抓到了真实规则句（如「D 阶段一律 `top_k=1`」「禁止用单次调用前后余额差推算单价」）
- 报告已落盘 `04-每日日志/2026-09-13.md`，幂等标记已打

## 抽象出来的通用教训

> **配置项改了不等于生效** —— 长跑中的 GUI 应用持有内存态，改 JSON 后必须重启并复检是否被覆写。这与既有 IMA 连接器故障处理经验同源（"改完复检是否被系统覆写"）。

## DTC-CASE-001 案件双轨索引校准（2026-09-14 · 已闭环）
- 现象：SR 召回引擎（扫 case-state.json）仅覆盖 6658/6660 → 漏 11 案；飞轮索引（案件-知识映射）列 13 案但两源失同步。
- 处置：为其余 11 案生成诚实标注的 case-state.json（仅含索引可证字段，待回填字段显式标出），12 案件夹全含 state（合作合同夹承载凤仪+百益双案），SR 召回 ↔ 飞轮索引 13 案全量对齐，召回漏案消除。
- 关键发现：`scan_cases.py`/`sync_case_state.py` 在 SKILL.md/运维手册标注已交付，但 skill 目录仅 SKILL.md、脚本未实装 —— SR 召回实际仅靠飞轮索引被跨案件模式识别读取。
- 决策卡：[[DEC-2026-004-DTC-CASE-001校准]]；校准件：`知识飞轮系统/03-连接/案件-知识映射索引/双向索引校准-DTC-CASE-001-20260914.md`。

## DTC-CASE-001 延伸闭环 · SR 召回引擎脚本补装（2026-09-14 · 已实装）
- 实装 `scan_cases.py`/`sync_case_state.py` 至 skill scripts；`scan_cases.py` 动态 glob 扫描根治硬编码漏案（2→13 案）；`sync_case_state.py` 双写基线已建（6658/6660 一致）。
- 补建 `厚德基金会诉道真百益合同纠纷案` 独立夹 state + 收窄 `合作合同` 为凤仪村案，13 真实案件夹全覆盖。
- 脱敏递归化修复 `黄茂姣` 嵌套泄漏，grep 硬验证 10 项敏感词全清零。
- 验证 PASS：`--dry-run` 13/13；脱敏闸门零泄漏。运维手册 v2.0.2 / SKILL.md v1.2.0 已记。
