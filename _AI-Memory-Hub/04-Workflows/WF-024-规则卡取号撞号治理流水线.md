---
id: WF-024
title: LawKB 规则卡取号 / 撞号治理流水线（next_rule_id + rule_id_guard）
type: workflow
status: active
updated: 2026-09-14
source_ai: workbuddy
scope: global
confidence: high
tags:
  - 规则卡
  - 取号
  - 撞号治理
  - 知识飞轮
  - 工作流
---

# WF-024 · LawKB 规则卡取号 / 撞号治理流水线

> 沉淀自 2026-09-11 规则卡编号漂移治理：取号脚本化 + 同号冲突批量处置。首次实证全库同号冲突 **25 组 → 0 组**，守卫 ERROR **26 → 0**。核心资产 `next_rule_id.py`（取号器）+ `rule_id_guard.py`（守卫）。脚本位于 `LawKB/知识飞轮系统/03-连接/scripts/`。

## 适用场景
- 新建裁判规则卡需要取号（**一切建卡入口**）。
- 建完卡验证编号无冲突。
- 库内已出现同号冲突，需批量治理。
- 全库编号基线体检（只读，最常用）。

## 取号（唯一真源）
```bash
PY=/Users/chenyouqiang/.workbuddy/binaries/python/versions/3.13.12/bin/python3
cd /Users/chenyouqiang/Documents/LawKB/知识飞轮系统/03-连接/scripts
$PY next_rule_id.py -d HT --n 15 --note "批次名"   # 一次取满本批数量
# 建卡落盘（rule_id 用取到的号）
$PY next_rule_id.py --commit-all                  # 提交预留（必做，否则24h自动释放）
```

## 守卫（建卡后必跑）
```bash
$PY rule_id_guard.py            # 全库
$PY rule_id_guard.py --since 60 # 近60分钟改动
$PY rule_id_guard.py --strict   # WARN也判失败（CI）
```
| 码 | 检查 | 级别 |
|---|---|---|
| G1 | 同域同号冲突 | 🔴 ERROR |
| G2 | 文件名 ≠ rule_id（规范允许别名） | 🟠 WARN |
| G3 | 域码不在白名单（当前21域） | 🟠 WARN |
| G4 | rule_id 格式非法/缺失 | 🔴 ERROR |
| G5 | 序号缺号 | ⚪ INFO |
| G6 | 旧式 R0NN- 编号 | 🟠 WARN |
| G7 | 卡型专用域被非指定卡型占用 | 🟠 WARN |

## 取号五条铁律
1. **禁止 `find`/`sort`/凭记忆手工取号**（判流程缺陷）。
2. **一次取满**——批量建卡前一次取足，逐个取号＝放大并发窗口 N 倍。
3. **落盘即 commit**。
4. **口径只看 frontmatter `rule_id`**（文件名只是别名）。
5. **建卡后必跑守卫**。

## 存量同号冲突治理（六-B 铁律）
- **文件名一律不动**——规范禁止批量改名（锈蚀数百条 wiki 链接）；只改 frontmatter：`rule_id`→新号，补 `aliases` 旧号，加 `rule_id_fixed:<日期>` + `rule_id_prev:<旧号>` 追溯标记。
- **保留方判定**：① 文件名与 rule_id 自洽者优先留；② 其次 created 早；③ 其次 mtime 早。
- **新号分配**：优先改回库内空闲的"文件名号"一次解决冲突 + 修复漂移；否则域内顺延 `max+1`。
- **执行顺序**：排查 → dry-run（脚本自校验）→ **cp -a 备份 /tmp 时间戳目录** → 实跑 → 三重回归（守卫 / 幂等 / 结构健康）。脚本检测 `rule_id_fixed` 已处理自动跳过，保证幂等。

## 红线 / 坑
- 🔴 **只改 `rule_id` 不够**：部分卡用 `rule_card:` 存号，两处都要改；`rule_card` 值可能是中文标题而非卡号，须判断后再改。
- 🔴 **`aliases` 形态不一**：内联 / 块格式 + 重复键 YAML 畸形 → 全量收集后统一重建为块格式。
- 🔴 **两位序号** `R-LN-63` 会被正则漏匹配，修复前先查三位补零后号是否被占。
- **心法**：门禁红着等于没门禁——误报淹没有效信号比没门禁更糟，守卫上线前必须先消除误报（`classify_card()` 分 rule / legacy / doc 三类）。

## 产出
- 取号预留记录（批次 note + 号段）
- 守卫报告（G1–G7 级别 + 退出码 0/1/2）
- 冲突治理追溯标记（rule_id_fixed / rule_id_prev / aliases）

## 相关笔记
- [[周报提炼建卡飞轮LOG]] (共现关键词: 规则, 建卡, ---)
- [[R-PI-112]] (共现关键词: rule, ---)
- [[规则卡取号规程-v1.0]] (共现关键词: 规则, rule, 建卡)
- [[WF-026-审判要件卡拆卡流水线]] (共现关键词: 取号, rule, id)
- [[RULE-024---D-阶段口径维持：案例检索一律-`top_k=1`（≈5分次）]] (共现关键词: id, 024, ---)
- [[WF-047-治理席位心智训练流水线]] (共现关键词: ---, 治理, ##)
- [[PROJ-005-律所主任治理席位心智训练备忘录]] (共现关键词: 治理, ---)
- [[经验卡片-制度-医院合规治理框架]] (共现关键词: ##, 治理)
- [[经验卡片-CMS治理-母本位置歧义事故复盘]] (共现关键词: ##, 治理, ---)
- [[WF-027-案件生命周期大屏流水线]] (共现关键词: PY, ##, --)
- [[LEARNINGS]] (共现关键词: --, ---)
