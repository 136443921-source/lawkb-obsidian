# AI 模拟法官 · 心智成熟度备忘录（活文档）

> 本文档是「小强律师 AI 模拟法官」的心智成熟度活档案。**每次训练 AI 模拟法官后，由 agent 自动追加训练记录并更新成熟度仪表盘**；每日 21:00 自动化做兜底归集，防止漏记。
>
> 最后更新：2026-09-16
> 红线：R2（只给候选·待人工确认）贯穿全链路，绝不输出 verdict；本文档不写入任何密钥/令牌；对外演示用占位案号，不暴露真实案号（如 6658）。

---

## 📊 成熟度仪表盘

| 指标 | 当前值 |
|---|---|
| **综合成熟度** | **~98 / 100** |
| 评估日期 | 2026-09-16 |
| 训练轨迹 | 72 → 74 → 76 → 78 → 80 → 84 → 87 → 90 → 93 → 95（09-08 起） |
| R2 红线 | ✅ 全链路标注，无 verdict |
| LTI 门禁 | ✅ 已实现前端五维 QC 校验器（R/L/C/T/P·REJECT=0 才准交付） |
| 实时核条 | ✅ pkulaw live 通 ｜ ⚠️ 元典「鉴权已过·账户余额不足」待充值（重新授权已生效，卡在计费额度） |
| 心证层 | ✅ 已实现（纯离线·候选心证段 + 证据采信说理） |
| §F 数额门控 | ✅ 已实现（违约责任/过错侵权/建工折价等责任成立才解锁数额预估） |
| 类案检索 | ✅ 已接入 LawKB 审判要件卡离线索引（638 张 / 15 域，关键词检索 + 锚定→要件涵摄） |
| 卷宗/证据编号联动 | ✅ 已实现（案号登记 + 证据清单 证N 编号 + 卷宗出处 + 事实框 chip 联动 + QC T2 溯源） |
| 心智卡 ↔ 要件涵摄 | ✅ 已打通（争点锚注入 + 涵摄结论回写 + §F 数额联动 + 类案检索锚定 + QC 五维门禁） |
| 多域竞合深度研判 | ✅ 已实现（路B 重 agent：`multidomain_analyzer.js` 识别候选法律域 + 域间竞合/先决/牵连/吸收/排斥关系 + 候选主路径；抽屉可视化 + 一键→要件涵摄） |
| 争议焦点驱动要件 | ✅ 已实现（disputeFocus 文本→候选法条解析 + 采纳→驱动 curKey；sPanel「🎯 争议焦点→要件」） |
| 客观 eval 套件 | ✅ 已实现（`eval_suite.js` 30 项客观断言 + jsdom 无头 15/15 交互校验，度量成熟度可回归） |
| 推演裁决接审判要件卡矩阵 | ✅ 已实现（judge_mind.js 接 JUDGE_CARDS：卡片正向规则强化采信 / pitfall 踩坑降权 / 拟裁判要旨带卡源援引段；无卡片自动退回纯启发式·向后兼容零破损） |

### 成熟度量表（评分锚 · 满分 100）
| 维度 | 权重 | 得分 | 说明 |
|---|---|---|---|
| 人格层 | 10 | 10/10 | 法官人格卡 / 主持7则 / 说理6则 / 红线4条 已齐 |
| 知识层 | 20 | 19/20 | 195 定性卡 + 3 裁量卡 + 21 法条 + 13 要件模型 + 638 类案审判要件卡索引；元典源仍缺 |
| 推理层 | 25 | 25/25 | 三态涵摄 + 证据缺口归责 + 清单联动 + 心证层 + §F 数额门控（全闭环） |
| 表达层 | 15 | 15/15 | 主持话术 + 文书草拟双面板可用 |
| 核验层 | 15 | 14/15 | 实时核条双源 + LTI 五维 QC 前端校验器（REJECT=0 才准交付）+ 客观 eval 套件（30 项断言可回归）；元典计费额度仍缺 |
| 记忆层 | 10 | 10/10 | 状态持久化 + 跨庭次 |
| 贯通度 | 5 | 5/5 | 争点锚注入 + 涵摄结论回写 + §F 数额联动 + 类案检索锚定 + QC 五维门禁 + 卷宗证据联动 + 多域竞合研判 + disputeFocus 驱动要件 全贯通 |
| **合计** | **100** | **≈ 99** | 折算综合 ~98 |

---

## 🧠 心智模型六层现状

| 层 | 载体 | 成熟度 | 状态 |
|---|---|---|---|
| 人格层 | `judge_persona_v1.md` | 满 | ✅ |
| 知识层 | `judge_mind_import §A-§E`(195) + `§F`(3) / `statute_cache`(21) / 13 要件模型 / `trial_element_cards.js`(638 类案审判要件卡) | 高 | ⚠️ 元典源仍缺 |
| 推理层 | `subsumption_engine.js` | 满 | ✅ 含心证层 + §F 数额门控（责任成立才解锁数额预估） |
| 表达层 | `HOST_CARDS`(R-PR-061~068) / `WS_CARDS`(R-LN-082~084) | 满 | ✅ |
| 核验层 | `yuandian_bridge` + 实时核条 + `lti_qc.js`(R/L/C/T/P 五维) | 高 | ✅ QC 前端校验器已接入；⚠️ 元典计费额度仍缺 |
| 记忆层 | localStorage + BroadcastChannel | 满 | ✅ |

> 心智骨架本质：**争点 → 要件 → 涵摄 →（心证）→（§F 数额）→ 文书**。争点锚注入 + 涵摄结论回写 + §F 数额联动 + 类案检索锚定现已全贯通。

---

## 🛤️ 训练日志

### 2026-09-16 · 自动归集 · 主控七字段→JUDGE_CARDS 矩阵映射表显式固化（judge_persona v3.1.0 接驳章）
- **训练内容**：`judge_persona_v1.md`（审判长 `ai-mock-court-judge` v3.1.0）接驳章新增「主控七字段 → JUDGE_CARDS 矩阵映射表」七行字段级映射：`review_step` / `elements` / `burden_of_proof` / `ruling.support` / `ruling.reject` / `negative_note` / `geo_scope`，含含义、消费点、审计门禁。
- **价值**：此前该映射由 `judge_mind.js v2` 隐式处理（难审计、换引擎即断），现已可审计；法官消费审判要件卡矩阵的主控字段映射透明化，强化 B 方案「推演内核消费矩阵」的可维护性。【R2·候选·待人工确认】

### 2026-09-16 · 自动归集 · 补建模拟法官飞轮详细版备忘录 + 新增 QC审判长↔AI模拟法官 消歧元规则
- **B-1 资产**：新建 `知识飞轮系统/05-调用/模拟法官心智模型与训练备忘录-2026-09-16.md`（244 行 / 20.8KB），数据全量自 `PROJ-模拟法官心智成熟度备忘录.md` 实地迁移；攻🔴/防🔵/裁⚖️/录📝 四方飞轮详细版备忘录至此齐备。
- **B-3 治理**：新增元规则 `00-全局规则/RULE-QCJUDGE-DISAMBIG-QC审判长与AI模拟法官消歧.md`，锚定 QC `side:"qc"`/程序质控 50/100 ↔ 法官 `side:"neutral"`/实体裁判 98/100，双模禁互套评分卡；主表由五方升六方。
- **对备忘录影响**：成熟度维持 ~98/100（今日多源日志一致报 98/100）；消歧规则厘清「QC审判长」与「AI模拟法官」两席位边界，避免后续混评。【R2·候选·待人工确认】

### 2026-09-15 · 自动归集 · 法官人格卡资产核查纠错（judge_persona_v1.md 确证 ACTIVE）
- **核查结论**：9-15 资产实地核验证实 `~/.workbuddy/skills/ai-mock-court-judge/judge_persona_v1.md` 早已存在（status: ACTIVE，五章同构完整：人格定位／行为准则／输出准则／禁区／接驳），此前"法官人格卡缺失待补"属误报，已消除。
- **中台面板佐证**：同日补建蓝队/书记员心智面板时 grep 中台 HTML 确认，原仅「🧠 法官心智」一个面板存在（蓝队/书记员面板 9-15 方补建），反证法官心智面板已落地、无 F1 类空洞。
- **对备忘录影响**：六层「人格层」状态维持 ✅（满），无需补卡。【R2·候选·待人工确认】

### 2026-09-15 · 自动归集 · 纳入模拟法庭管理系统席位体系（四方训练态：法官 ~98/100 ACTIVE）
- **集成动作**：老强要求把已训练的 AI 模拟法官纳入模拟法庭管理系统以开展模拟庭审；法官智能体作为正式席位登记进 `模拟法庭席位绑定配置.json`（v1.4.x），`judge_mind.js v2.0.0` 登记为「审判长推演裁决内核」，席位配置 configVersion 升至 1.3.0/1.4.x；`settings.json → skillOverrides` 补齐 `ai-mock-court-judge` 等 11 项 on。
- **四方训练态评估（9-15）**：⚖️ 法官 **~98/100（ACTIVE）** ｜ 📝 书记员 64/100 M4（ACTIVE）｜ 🔵 蓝队 88/100 M5（ACTIVE）｜ 🔴 红队 89/100 M5（人格卡仍 DRAFT）；法官成熟度与备忘录取仪表盘 ~98 一致、无回退。
- **对备忘录影响**：成熟度维持 ~98/100，评估日期刷新至 2026-09-15；元典计费额度仍缺（唯一 roadmap 遗留项）。【R2·候选·待人工确认】

### 2026-09-14 · 【B 方案】推演裁决内核接入审判要件卡矩阵（judge_mind.js 接 JUDGE_CARDS）
- **训练内容**：将 B 方案落地——让 `judge_mind.js` 推演裁决内核接入 JUDGE_CARDS 审判要件卡矩阵，替代纯启发式加权。
  1. **关键认知修正**：195 张审判要件卡 `favor` 全为「中立」，是「正向裁判规则 + 常见误区 pitfall」基准卡，非偏蓝/偏红立场卡；故 B 方案价值＝用卡片 `support`/`points`（正向规则）与 `pitfall`（误区）校准每条攻防采信度，并让法官心证/拟要旨带卡源可追溯。
  2. `matchCards(text, cards)`：按「名称 token / 法条号 / 域」特异性信号入选关联卡（剔除通用词泛化误选，避免仅凭"保险/合同"等域词命中全部卡）。
  3. `_cardAdj` 方向判定用「非通用 4 字规则短语」(`_ruleHit`，跳过 保险合/投保人/当事人 等黑名单)：arg 复述 pitfall 短语→踩坑降权(-0.7，最高优先级)；复述 support→正向强化(+0.5)；仅相关不复述不计分。
  4. `assessArgument`/`assessDispute`/`renderVerdictDraft`/`runTrialJudge` 全加 `opts.cards` 分支；**无 cards 完全退回 v1 启发式（向后兼容零破损）**；`runTrialJudge` 自动兜底 `window.JUDGE_CARDS`/`window.__JUDGE_CARDS_IMPORT__`。
  5. HTML 接入：行 1784 `assessDispute` + 行 1829 `runTrialJudge` 注入 `cards:JUDGE_CARDS`；行 1789 每轮心证徽标新增「卡片依据」渲染。
- **校验**：`eval_suite.js` **39→51 全绿**（新增 12 条 B 断言：矩阵载入/正向强化/踩坑降权/focusCards 非空/卡片援引段/候选标注/无 cards 退回 v1）；jsdom 无头（浏览器式全局）验 draft 含「审判要件卡援引·R-HT-189」、无 cards 退回 v1。
- **成熟度**：~96 → **~98**（研判层 25/25 满·推演接矩阵）；挂载断点④【推演内核消费矩阵】已闭环，A+B 双断点全通。
- **遗留（唯一 roadmap 项）**：元典账户充值 → 双源核条（鉴权已过，卡在计费额度）。

### 2026-09-14 · 路B 重 agent（多域竞合深度研判）+ disputeFocus 驱动要件 + 客观 eval 套件（三项收口）
- **训练内容 A — 路B 重 agent：多域竞合深度研判（`multidomain_analyzer.js`·UMD·window.MDA）**：
  1. 域分类法 `DOMAIN_TAXONOMY`（7 域：合同效力/违约责任/合同相对性/承揽/建工/过错侵权/证据程序），每域含 statutes keys + 触发关键词。
  2. `detectDomains(text)` 关键词打分排序（同分按教义优先级 deterministic）；`relationBetween()` 依据 `RELATION_MATRIX`（规范化键，避免 a|b/b|a 漏配）判定 竞合/先决/牵连/吸收/排斥/独立 六类关系，含教义/法条依据（如民法典第186条责任竞合、第808条建工吸收于承揽）。
  3. `analyzeConcurrence(text)` 输出 候选域 + 主路径 + 两两关系表 + 候选建议（全 R2 标注，含择一主张/先决/牵连等提示）。
  4. 前端新增「🔀 多域竞合研判」抽屉（顶栏按钮 + 文本框 + 运行）：渲染候选域 chip + 关系表（着色）+ 建议，每候选「→要件涵摄」按钮调 `window.XZ.anchorTo`；主路径同步写入 Path B 文书草拟提示（`#docFocusKey`）。
- **训练内容 B — 争议焦点 disputeFocus 驱动要件选择（引擎 + UI）**：
  1. `subsumption_engine.js` 新增 `FOCUS_KEYWORDS`（短关键词映射）+ `resolveDisputeFocus(focusText)`（候选法条打分排序）+ `buildFromDisputeFocus()`（解析首选法条→直接生成涵摄推理链）。
  2. sPanel 新增「🎯 争议焦点→要件」输入 + 候选 chip + 「采用」按钮：采纳即驱动 `curKey`、`renderElems`、回写 `S.disputeFocus` 并 `run()`；载入时回填已存焦点。
- **训练内容 C — 客观 eval 套件（`eval_suite.js`·Node）**：
  1. 30 项客观断言覆盖 ① 引擎结论类型/§F门控/缺口识别 ② disputeFocus 解析（违约→civ_577/侵权→civ_1165/效力→civ_143）③ 多域竞合（识别≥2域·违约责任↔过错侵权责任竞合·引用第186条·先决关系）④ LTI 五维 QC（良稿 REJECT=0·门控违反 REJECT>0·verdict语言 REJECT>0·案号不一致 REJECT>0）⑤ 纯函数（金额预估/证据编号/案号格式）。
  2. 另保留 jsdom 无头 `test_harness.js`（15/15）做交互回归。
- **校验**：`eval_suite.js` **30/30（100%）**；jsdom 无头 15/15（回归）；concur 抽屉端到端（候选域+竞合+第186条建议渲染·主路径同步 Path B·→要件涵摄触发 anchorTo）通过；disputeFocus 端到端（焦点→候选 chip→采用驱动 sSel+持久化）通过；HTML 内联脚本语法 0 错。
- **成熟度**：~93 → **~95**（核验层 13/15 → 14/15；合计 ≈97 → ≈98）。
- **遗留（下一步）**：① 元典账户充值 → 双源核条齐活（重新授权已生效，卡在计费额度）；② 红蓝对抗推演接入法官心智为参与智能体 —— **已于 09-14 第三场训练收口完成**（见本备忘录顶部最新训练日志）。

### 2026-09-14 · LTI 五维 QC 前端校验器 + 卷宗/证据编号真实联动（两 P1/P2 项收口）
- **训练内容 A — LTI 五维 QC 前端校验器（R/L/C/T/P·REJECT=0 才准交付）**：
  1. 新建 `lti_qc.js`（UMD·window.LTI_QC / Node 可测），实现 `runQC({sub, docText, caseNo, evidence, models})` 五维校验：R 法条真实性（模型命中 / 北大法宝溯源 / 无待回源标记）/ L 法理逻辑（结论类型自洽 / §F 门控被尊重 / 心证倾向不矛盾）/ C 一致性（C301 金额须引第585条30%或候选标注·C302 名称占位·C304 案号格式与一致性）/ T 溯源（建议证据可溯源 / 文书证据编号须落在卷宗登记 / 人工在环标记）/ P 程序（R2 红线无 verdict / 禁绝对词「显然·当然」/ 举证程序完备 / 结论块候选标注）。severity 分 `reject`/`warn`，输出 `dims`+`reject`+`grade`+`report`。
  2. 抽出纯函数 `computeAmountEstimate(liquid,loss,paid)`（民法典第585条 30% 酌减参考上限）供 `amCalc` 复用，消除重复逻辑、提升可测性。
  3. 前端新增「🔬 LTI 五维 QC」抽屉：案号 + 文书草稿 textarea + 运行按钮 → 五维度卡片（✅/⚠️/⛔ 徽章）+ 顶部 **REJECT 计数 + 交付结论**（REJECT=0 才准交付）。打开即自动校验当前要件涵摄结果。
- **训练内容 B — 卷宗/证据编号真实联动**：
  1. 新增「📂 卷宗证据」抽屉：案号登记（格式校验 + 反馈）+ 证据清单表（编号自动 证N / 名称 / 卷宗出处(卷X-Pxx) / 类型 / 证明目的），增删 + localStorage 持久化。
  2. 要件涵摄事实框接入 `datalist="eviList"` 编号联想；推理链渲染时把「证N」渲染为 chip（`renderFactWithChips`），hover 显示卷宗出处（卷宗出处来自卷宗证据登记）→ **事实→证据编号→卷宗出处** 链路真实贯通。
  3. 证据清单 / 举证期限横幅带上案号；并供 QC 的 T2 溯源校验（文书引「证N」须落在登记）。
- **校验**：`lti_qc.js` Node 单测全过（良稿 REJECT=0 / verdict 语言 REJECT=1 / 案号不一致 REJECT=1 / §F 门控未解锁写数额 REJECT=2 / computeAmountEstimate / parseEvidenceRefs / validateCaseNo）；jsdom 无头挂载真实 HTML 全流程校验 **15/15** 通过（全局就绪·类案检索 6 命中·证据 chip 联动卷宗出处·新增证据持久化·civ_577 全满足 amountGate 解锁·QC 良稿 REJECT=0 / 劣质稿 REJECT=1 / 门控违反 REJECT=2）；HTML 内联脚本语法 0 错。
- **成熟度**：~90 → **~93**（核验层 10/15 → 13/15；合计 ≈94 → ≈97；六层与贯通度全满）。
- **遗留（下一步）**：① 元典账户充值 → 双源核条齐活（重新授权已生效，卡在计费额度）；② 路B 重 agent（多域竞合深度研判）；③ 争议焦点 `disputeFocus` 驱动要件选择；④ 客观 eval 套件 —— **②③④ 已于同日晚间 09-14 第二场训练收口完成**（见本备忘录顶部最新训练日志）。

### 2026-09-14 · §F 数额预判门控 + 类案检索接入（两 P0/P1 项收口）
- **训练内容 A — §F 数额预判与要件成立联动（违约责任成立才进数额）**：
  1. 引擎 `subsumption_engine.js` 新增 `AMOUNT_RELEVANT`(5 责任型法条映射) + `computeAmountGate(m, conclusionType)`：非责任型法条 → `relevant:false`（直接说明须先就责任要件涵摄成立）；责任型法条且 `effect-established` → `enabled:true` 并附数额维度(违约金30%/损失赔偿/折价补偿等)；`pending-burden`(真伪不明) / `effect-not-established` → `enabled:false` 锁定。已接入 `buildSubsumption`（推理链写【§F 数额预判门控】标签 + 返回 `amountGate`）+ `amountScaleHints()`。
  2. 前端：`renderChain` 新增 §F 门控横幅（✅解锁 / ⛔锁定）+「进入数额预估」按钮；`renderAmount()` 据 `window.lastSub.amountGate` 自动切 `agb.on/off` + `amCompute` 锁定/解锁 + 数额维度参考；补 `amCalc` 候选测算（民法典第585条 30% 酌减参考上限，纯候选）。
- **训练内容 B — 类案检索接入 LawKB 300+ 审判要件卡**：
  1. `gen_trial_cards.js`（Node）扫描 LawKB `06-沉淀/裁判规则库`(card_type:审判要件卡) + `02-提炼/审判要件卡` 入库索引，解析 id/title/domain/summary/laws/source，去重导出 `trial_element_cards.js`：**638 张 / 15 域**（>300 目标）。
  2. 前端新增「🔎 类案检索」抽屉：关键词检索（AND 优先·宽松 OR 兜底）+ 域过滤 chips + 「用当前涵摄法条联想」(TRIAL_SEED 映射) + 命中卡「⚓ 锚定→要件涵摄」（注入 `#sAnchorCtx` 参考块并打开涵摄面板，由人工选配法条）。
- **校验**：引擎 Node 单测 **8/8** 通过（577成立解锁/真伪不明锁定/不成立锁定/143非责任型/scaleHints/端到端 amountGate）；类案检索算法对真实 638 卡实测「违约 违约金」6 /「挂靠」8 /「精神损害」7 /「欺诈 撤销」40 /「建设工程 折价」2 / 空查 30；seed 联想 6 法条全部命中；HTML 内联脚本 3 块语法 0 错。
- **成熟度**：~87 → **~90**（推理层 24/25 → 25/25 满；知识层 18/20 → 19/20；贯通度 4/5 → 5/5 全贯通；合计 ≈91 → ≈94）。
- **遗留（下一步）**：① 元典账户充值 → 双源核条齐活；② LTI 五维 QC 前端校验器；③ 路B 重 agent（多域竞合深度研判）；④ 卷宗/证据编号真实联动；⑤ 客观 eval 套件。

### 2026-09-14 · P0-2「两张皮打通」落地（judgeReviewA ↔ buildSubsumption）
- **训练内容**：将「法官心智候选面板(`judgeReviewA`)」与「要件涵摄面板(`buildSubsumption`)」由并行两皮打通为闭环流水线，新增 `window.XZ` 全局桥对象：
  1. **争点锚注入（候选→涵摄）**：主脚本新增 `STATUTE_ANCHORS`（6 组关键词→法条映射：欺诈→civ_148 / 意思表示·授权·主体资格→civ_143 / 违约·合同相对性→civ_577 / 过错侵权→civ_1165 / 强制性规定→civ_153 / 合同效力→civ_465）+ `detectAnchor(res, facts, claims)`，扫描法官候选卡名/片段/用户输入事实与诉请，命中即生成「🎯 争点锚定：建议核验【法条】」横幅 + 「⚓ 锚定→要件涵摄」按钮；点击后自动 `sSel` 选法条 + `renderElems` + 打开涵摄面板。
  2. **涵摄结论回写（涵摄→候选）**：sPanel 新增 `buildWritebackHTML(d)` + `window.XZ.setWriteback(d)`，在 `run()/loadPreset()/restoreSubsumption()` 三处生成回写块（法条 / 结论 / 心证 / 证据缺口数，全标【候选·待人工确认】），渲染进 `#judgeReviewOut`；`renderJudgeReviewA` 每次重绘时附带回写块，形成持久闭环。
- **校验**：HTML 内联脚本 3 块语法 0 错；锚点映射单测 **5/5** 通过（意思表示→civ_143 / 欺诈→civ_148 / 违约→civ_577 / 过错侵权→civ_1165 / 无关键词→无锚点）；`conclusionTypeLabel` 回写可用。
- **实时核条现状更正**：元典 MCP 实测已由 `unauthorized` 变为「账户余额不足」——**重新授权已生效（鉴权已过），卡在元典账户计费额度**，须去元典平台充值后双源齐活。
- **成熟度**：~84 → **~87**（贯通度 2/5 → 4/5；核心闭环打通，§F 数额联动仍留 ~13 空间）。
- **遗留（下一步）**：① 元典账户充值 → 双源核条；② §F 数额预判与要件成立联动；③ 类案检索接入。

### 2026-09-14 · P0-1「心证层」落地（纯离线·零风险）
- **训练内容**：在 `subsumption_engine.js` 新增 `buildXinZheng(m, rows, conclusionType)`，为涵摄引擎补上法官心智的"灵魂两层"：
  1. **候选心证段**：依结论三态（成立/不成立/真伪不明）生成"本院倾向于认为…理由为…"——成立倾向点名各要件均满足且无反证；不成立倾向点名不满足要件及主张方；真伪不明点名存疑要件 + 民诉法第67条举证归责。含可选要件不阻断提示。
  2. **证据采信说理**：对已有已证事实的要件逐一认证（采信 / 采纳·证明方向反 / 初步采纳·待认证 / 暂不予认证），写出"形式合法、来源真实、与待证事实关联"等理由，**禁写"显然/当然"**，全程【候选·待人工确认】。
- **前端**：`mock-trial-center-enhanced.html` 新增 violet 心证块（🧠）+ teal 证据采信块（⚖️），接入 `renderChain`（置于证据清单之后）。
- **校验**：引擎 Node 单测 **22/22** 通过（覆盖 143 预设存疑 / 577 全满足 / 143 不满足 / 1165 存疑+可选 / 无 fact / 禁写显然当然 / 无 verdict 字样）；HTML 内联脚本 3 块语法 0 错；推理链确认含【心证】与【证据采信说理】标签。
- **成熟度**：~80 → **~84**（推理层 20/25 → 24/25，补上最大短板心证层；元典未通、两张皮未打通仍留 ~16 空间）。
- **遗留（下一步 P0-2）**：打通 `judgeReviewA` ↔ `buildSubsumption` 两张皮；元典重新授权。

### 2026-09-14 · 心智模型复盘 + 备忘录建立
- **训练内容**：对 AI 模拟法官心智模型做全面复盘，输出六层分层图、已扎实项、P0/P1/P2 缺口清单，并建立本活文档。
- **关键发现（P0）**：① 心证层缺位（无"本院倾向于认为…"、无证据采信/不采信说理）；② 法官心智卡(`judgeReviewA`) ↔ 要件涵摄(`buildSubsumption`) 两张皮未打通；③ 元典双源未通。
- **成熟度**：~78 → ~80（补完双源核条 + 新法条 1165/148 live 入缓存 + 复盘收口）。
- **决议**：下一步优先补"心证层"与"两张皮打通"（纯离线、零风险）。

### 2026-09-09（晚）· 证据清单联动 + 双源核条打通
- **训练内容**：① 证据缺口 → 证据清单 / 举证期限联动（端到端离线闭环）；② 实测 pkulaw MCP live 打通，1165/148 经实填入缓存（19→21）；③ bridge 双源骨架就绪（修正 pkulaw 工具名 `mcp-law-search-service.search_article` + needReauth 上报）；④ widget 加 1165/148 核验 chip。
- **成熟度**：~76 → ~78（证据缺口识别）→ ~80（双源 + 新法条）。
- **遗留**：元典仍 `unauthorized`，须重新授权。

### 2026-09-09（早）· Path B 受限试点 + 证据缺口自主识别
- **训练内容**：北大法宝 live 拆解 1165/148 权威原文（实耗 2 次 live，预算内）；建动态模型双文件 + 引擎自动合并 + `optional` 机制；证据缺口自主识别（归责 + 可选要件降噪）。
- **成熟度**：~74 → ~76。

### 2026-09-09（早）· 即兴主持 + 状态持久化
- **训练内容**：主持面板当庭要素输入 + 🎲 即兴主持 + 🧭 心证档案 + 庭次快照；`normalize()` 扩展存档字段；BroadcastChannel 跨庭次记忆。
- **成熟度**：~72 → ~74。

### 2026-09-08 · 基座搭建（路A + 双面板 + 卡库导入）
- **训练内容**：`judge_persona_v1` 人格卡；`judge_reviewer` 路A（`judgeReviewA` 关键词打分）实现；主持话术面板（`HOST_CARDS` R-PR-061~068）；文书草拟面板（`WS_CARDS` R-LN-082~084）；数额预判面板（§F）；导入 195 张定性卡（§A-§E）+ 3 张裁量卡（§F）。
- **里程碑**：AI 模拟法官从 0 到可用骨架。

---

## 🎯 改进路线图

### 🔴 P0（影响"像不像真法官"）
1. ~~**补「心证层」**~~ ✅ **已完成（2026-09-14）**：三态结论之外，生成"本院倾向于认为……，理由为……"候选心证段 + 证据采信/不采信认证说理（仍标 R2）。纯离线、零风险。
2. ~~**打通「两张皮」**~~ ✅ **已完成（2026-09-14）**：`judgeReviewA` 候选 → 争点锚 → 注入 `buildSubsumption`；涵摄结论回写候选面板（新增 `window.XZ` 桥 + `STATUTE_ANCHORS` 映射，纯离线）。
3. ~~**§F 数额预判与要件成立联动**~~ ✅ **已完成（2026-09-14）**：`computeAmountGate` 责任成立才解锁数额预估；`renderAmount` 据门控自动锁定/解锁 + `amCalc` 候选测算（第585条30%参考）。
4. ~~**类案检索接入 LawKB 审判要件卡**~~ ✅ **已完成（2026-09-14）**：`gen_trial_cards.js` 生成 638 张离线索引（15 域）；widget 新增「🔎 类案检索」抽屉（关键词检索 + 域过滤 + 联想 + 锚定→要件涵摄）。元典账户充值后可与双源核条并联。
5. **元典账户充值** → 双源核条齐活（重新授权已生效，卡在计费额度）。

### 🟠 P1（影响"裁判深度"）
6. ~~**路B 重 agent（多域竞合深度研判）**~~ ✅ **已完成（2026-09-14）**：`multidomain_analyzer.js` 识别候选法律域 + 域间竞合/先决/牵连/吸收/排斥关系 + 候选主路径；widget「🔀 多域竞合研判」抽屉可视化（民法典第186条责任竞合等教义）。
7. ~~**LTI 五维 QC 前端校验器接入生成链路**~~ ✅ **已完成（2026-09-14）**：`lti_qc.js` 五维校验（R/L/C/T/P），REJECT=0 才准交付；widget「🔬 LTI 五维 QC」抽屉自动校验当前涵摄 + 文书草稿。
8. ~~**争议焦点 disputeFocus 驱动要件选择**~~ ✅ **已完成（2026-09-14）**：`resolveDisputeFocus` 文本→候选法条解析 + sPanel「🎯 争议焦点→要件」采纳即驱动 curKey + 回写 S.disputeFocus。

### 🟡 P2（影响"闭环体验"）
9. ~~**卷宗/证据编号真实联动**~~ ✅ **已完成（2026-09-14）**：案号登记 + 证据清单 证N 编号 + 卷宗出处 + 事实框 chip 联动 + 持久化 + QC T2 溯源。
10. ~~**红蓝对抗推演接入法官心智为参与智能体**~~ ✅ **已完成（2026-09-14）**：`judge_mind.js` 内核 + 三轮冲击每轮心证点评 + 战情室拟裁判要旨 + 整场一键裁决 + 心证回写；与法官心智抽屉双向打通。
11. ~~**推演裁决内核接入审判要件卡矩阵（替代纯启发式）**~~ ✅ **已完成（2026-09-14）**：`judge_mind.js` 接 JUDGE_CARDS，卡片正向规则强化采信 / pitfall 踩坑降权 / 拟裁判要旨带卡源援引段；无卡片自动退回纯启发式·向后兼容零破损。
11. ~~**建立客观 eval / 回归测试套件度量成熟度**~~ ✅ **已完成（2026-09-14）**：`eval_suite.js` 30 项客观断言（100% 通过）+ jsdom 无头 `test_harness.js` 15/15 交互回归。

---

## 📌 使用与维护
- **每次训练后**：agent 在训练收口时追加「🛤️ 训练日志」条目，并更新「📊 成熟度仪表盘」（轨迹 / 六层状态 / 各指标）。
- **每日 21:00 自动化兜底**：扫描各工作区每日记忆日志，归集遗漏的训练记录、刷新「最后更新」日期、捕获当日显式成熟度分值。
- **红线**：任何写入保持【候选·待人工确认】标注；绝不写入密钥/令牌；对外演示用占位案号。
- **位置**：`/Users/chenyouqiang/Documents/LawKB/_AI-Memory-Hub/02-当前项目/PROJ-模拟法官心智成熟度备忘录.md`
