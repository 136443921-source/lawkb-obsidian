# 项目长期记忆（LawKB 知识飞轮系统）

> 仅记录跨会话有价值的事实/约定/架构，勿记临时调试。

## 架构前提（重要）

- **`rule_id` 是全局跨链「指针键」，不是唯一性约束。** 概念页(`canonical:`字段明文指向规范卡)、经验卡片、提炼索引、案例摘要、裁判规则库 五类卡故意共享同一 `rule_id` 构成双向链接骨架。任何"同 rule_id 多卡"都**不可**直接判为版本冲突/废卡。
- 因此「版本冲突 VC」类必须按**同目录级**判定（同目录内同 rule_id 且内容不同才算真撞号）；跨目录/跨卡型共享一律判 CR·不冲突（指针链接）。

## 防知识打架机制

- 引擎：`03-连接/scripts/kg_conflict_arbitrator.py`；技能：`~/.workbuddy/skills/lawkb-conflict-arbitration`（v1.0.3）。
- 六类：KE 知识演进 / FC 事实纠正 / VC 版本冲突 / CC 内容矛盾 / CR 上下文相关 / MC 成熟度冲突。每项带置信度+AI建议+三选项（采纳/融合/不冲突）。
- **铁律**：CC 内容矛盾绝不自动执行（误报高）；修复前先 `--dry`；apply 自动备份 /tmp + 幂等 + `_resolved_log.json` 留痕。
- **KE/FC/MC 同源误报（2026-09-16 二轮，与 VC 同源 ~100% 误报，须防）**：
  - `norm_title` 保留数字（不剥 `0-9`），否则 R-HG-035/045、日报日期变体相撞。
  - 流程/流水型文件（每日摄入/日课/采集笔记/周报）非知识主张 → `is_routine()` 跳过。
  - FC/MC 仅 `VERIFIED_LAYERS`（06-沉淀/裁判规则库、03-连接/概念页）内触发；02-提炼(源) vs 06-沉淀(产物) 属正常管道，非冲突。
  - 聚类不用 alias 跨主题边，阈值 0.80。
  - **收敛**：FC 4→0、MC 10→0、KE 84→8、CC 105→2；最终 303 候选中 293 为健康 CR/CC误报（已打 no_conflict），10 项 hold 待人工。
- **🔴 apply 写标记须落 frontmatter**：旧版 `add_fm_flags` bug 把 `conflict_checked` 错写正文/丢失，导致"已处置"不实、下次扫描重报。v1.0.2 修复后须用修复引擎重跑 `mark_no_conflict` 补写 frontmatter（2026-09-16 已对 293 项补写，532 文件，备份 /tmp/kg_conflict_fix_reapply_20260916-151041）。**KE/FC/MC「安全档」不可盲修**：要么方向敏感（哪份现行），要么编号治理（同内容双 rule_id，归 P0-4 编号治理）。**
- 2026-09-16 实测：4361 卡扫出 531 候选（KE104/FC4/CC126/CR287/MC10/**VC0**）。VC=0 因 rule_id 为共享键，属健康信号。

## 工作纪律（老强设定，全局）

- 六-B 安全铁律：批量前先排查确认非误报、dry-run、备份、确认幂等。
- 知识库批量更新约定：10 份/轮。
- 法律文书：LTI 五维 QC（R/L/C/T/P）REJECT=0 才交付；禁止论文化写法；格式依最高法法〔2016〕221号。
