---
title: 类案检索报告卡族 · 案例缓存回填待办（P1-2 配套）
card_family: 类案检索报告
card_type: 待办登记
created: 2026-09-16
updated: 2026-09-18T20:35
source_ai: workbuddy
related_links:
  - LC-011-签收验收不担责
  - LC-012-滥用诉权重复起诉
  - LC-013-撤诉后程序后果
tags:
  - 2026
  - pkulaw
  - 合同法
  - ---
  - 民事诉讼
  - 公司法
  - 账号
  - 回填
---

# 类案检索报告卡族 · 案例缓存回填待办（2026-09-16）

> 关联任务：② P1-2 红队攻击弹药筛「未消/新发」落地；走北大法宝 mcp 回填空缺案例缓存。

## 一、P1-2 弹药筛落地（已完成 · 确定性交付）

- 新建脚本 `红队出庭律师/red_ammo_filter.py`：读取 `反面案例库_状态跟踪.json`，按 `status ∈ {未消,新发}` 筛可用攻击弹药、排除 `已消`，输出 markdown 清单 + 统计。已验证默认/--status/--type/--fmt 四分支。
- 红队 `SKILL.md`「弹药库配置」章节落地：一级弹药位表新增「反面案例库（对抗标本·状态筛选）」行 + 新增「🛡️ 攻击弹药筛选（未消/新发）· P1-2 落地」可执行段（含脚本调用命令 + 状态三态说明 + 蓝队 D3 写回闭环）+ 路径自检补反面案例库路径。消除 persona 第五章声明与 SKILL 实现的脱钩。

## 二、案例缓存回填（本期受阻 · 仅做规范化登记，禁编造案号）

### 阻塞事实（已实测，2026-09-16 13:10）

| 通道 | 状态 | 证据 |
|---|---|---|
| 北大法宝 `mcp__pkulaw` | **不可用** | `DeferExecuteTool` 报 "mcp__pkulaw not found in deferred tools index"；`ToolSearch` 未能返回其任何子方法 schema |
| 元典 `yuandian-mcp` | **余额不足** | `yuandian_case_vector_search` 返回 "您当前账户可用余额不足，请及时充值后重试" |
| 本地法律法规库 | **无民诉法解释源** | `find 法律法规库 -iname "*民诉*解释*"` 为空；`第二百四十七条` 仅误命中民法典/公司法 |

> 与 LC-011/012/013 卡内「元典/法宝当前不可用」「statute_text_pending·待元典/法宝回填核验」标注**完全一致**。
> **红线**：红队 persona 禁区「禁止编造案号」——故本案**不伪造任何案号/法条**，仅登记待补跑队列。

### 空缺清单（待 MCP 通道恢复后一键补跑）

| # | 位置 | 空缺内容 | 检索 query 模板（建议） | 状态 |
|---|---|---|---|---|
| 1 | LC-011 第50行 / LC-012 第34、51行 | 新干(2026)案 | 买卖合同纠纷 + 江西·新干 + 订货单客户栏署名被告且由其签字 → 认定共同买受人/合同相对人 | 待回填 |
| 2 | 同上 | 奉节(2026)渝0236 案 | `(2026)渝0236` + 买卖合同 + 订货单/销售单签字认定买受人 | 待回填 |
| 3 | 同上 | 成都(2024)川01终522 案 | `(2024)川01民终522` + 买卖合同 + 签字认定共同买受人 | 待回填 |
| 4 | 同上 | 定南案 | 江西·定南 + 买卖合同 + 订货单署名 + 共同买受人认定 | 待回填 |
| 5 | LC-012 第46行 / LC-013 第44行 | 民诉法解释 §247（重复起诉构成） | 本地补《民诉法解释》源 或 MCP 法条核验 | 待回填（法条） |
| 6 | LC-013 第44行 | 民诉法解释 §214（撤诉后再次起诉应予受理） | 同上 | 待回填（法条） |

### 解锁动作（任一满足即可重跑）

1. **元典充值**：`yuandian_case_vector_search`（query 自然语言 + `wenshu_filter.ay=["买卖合同纠纷"]`）/ `yuandian_rh_ptal_search`（按案由+全文关键词）检索 #1–#4；`yuandian_rh_qwal_search` 查权威案例。
2. **北大法宝连接恢复**：重启 `mcp__pkulaw` 连接，使其子方法进入 deferred 索引，再用其案例检索方法补跑 #1–#4。
3. **本地补民诉法解释源**：将《最高人民法院关于适用〈民事诉讼法〉的解释》纳入 `法律法规库/程序法/`，即可离线核验 #5–#6，消除 `statute_text_pending`。

### 回填后动作（通道恢复即执行）

- 把真实案号 + 裁判要旨写入对应 LC 卡「风险对照种子 / 法条锚点」段；
- 删除「待回库核验真实案号」「statute_text_pending」占位标记；
- 更新各卡 `updated: 2026-09-xx`，并在本待办文件勾销对应行。

---

## 三、本轮实测复核与诊断订正（2026-09-16 13:41 · 小强 AI 记）

> 接续 13:05「仅登记待办」；本会话新对话已重载宿主 MCP 索引，实测两条检索通道是否真恢复。

### 实测结果（三路全阻，③ 本次仍未真跑）

| 通道 | 本轮实测（2026-09-16 13:41） | 与 13:05 诊断对比 |
|---|---|---|
| 北大法宝 `mcp__pkulaw` | **工具索引已重载**（ToolSearch 可返回 schema，证明 token 已注入→亚型B 已修复）；但实际调用 6 类方法 `anhao_recognition`×1 / `search_case`×3 / `search_article`×2 **全部返回 `code:90001 Unclassified Authentication Failure`（"Error while checking remaining points for user: a4204ed9-3bf5-4340-b392-589ca82dd3a7"）** | 13:33 判「排除亚型D-1、根因锁死连接层」**被实测推翻**；真实根因 = 北法宝**业务层点数/绑定（亚型D-1）**，与 token 无关 |
| 元典 `yuandian-mcp` | `yuandian_get_user_balance` 返回 `pointBalance:0 / countBalance:0` → 仍余额不足，案例向量检索不可用 | 一致（仍 D-1 余额 0） |
| 本地法律法规库 | `法律法规库/司法解释`、`/程序法` 均无《民诉法解释》全文源（仅有《民事诉讼法（2023）》正文）；`find -iname "*民诉*解释*"` 仅命中引用 §91 的规则卡 | 一致（§214/§247 无本地源） |

### 结论

- **三路全阻**：pkulaw 90001 / 元典 0 / 本地无解释源 → P1-2 ③「真实检索回填」**本轮仍无法执行**，空缺清单 #1–#6 维持「待回填」。
- **红线守正**：未伪造任何案号/法条，LC-011/012/013 占位标记（`待回库核验真实案号` / `statute_text_pending`）全部保留。
- **诊断订正（重要）**：13:33 依老强「账号侧正常」口述排除 D-1；但实测 90001 系调用时校验**该 user 剩余点数/绑定**失败，典型为（a）北法宝点数耗尽 或（b）个人版/平台版绑定混淆。token 已注入 ≠ 业务层可用。**亚型B 已愈、亚型D-1 仍在**。

### 解锁清单（精确可执行 · 任一路打通即一键补跑）

1. **北法宝（解 #1–#4 案例 + #5–#6 法条）**：核查账号 `a4204ed9` 点数是否耗尽 / 个人版 vs 平台版绑定（90001 主因）→ 充值或重绑后 pkulaw 即通。已验证可用方法名：
   - `mcp__pkulaw__mcp-case-search-service.search_case`（text + `courthouse_province`/`case_type`/`size` 过滤）
   - `mcp__pkulaw__case_number_recognition.anhao_recognition`（案号标准化，验证奉节/成都片段）
   - `mcp__pkulaw__mcp-law-search-service.search_article` / `get_article`（法条 #5–#6）
2. **元典（解 #1–#4 案例）**：充值后 `yuandian_case_vector_search`（`query` + `wenshu_filter.ay=["买卖合同纠纷"]`）/ `yuandian_rh_qwal_search`（权威案例）。
3. **本地补源（解 #5–#6 法条，零 MCP 依赖）**：将《最高人民法院关于适用〈民事诉讼法〉的解释》全文纳入 `法律法规库/程序法/`（或 `司法解释/`），即可离线核验 §214/§247，消除 `statute_text_pending`。

### 就绪状态

- 空缺清单 query 模板（二节 #1–#6）+ 上述已验证方法名 = **解锁即跑**；回填后按「回填后动作」删占位、改 `updated`、勾销本表行。

---

## 四、连接器修复工作流再查（2026-09-16 13:51 · 小强 AI 记）

> 老强指示"走连接器修复工作流再查"。加载 `workbuddy-connector-enabled-gate`，实地定位活跃 config + 只读诊断 pkulaw 配置态，按四亚型速判框架归类。

### 定位与诊断（只读，零写盘）

- 活跃 config：`~/.workbuddy/connectors/75c7265a-5f11-4420-9e71-be28329d68fd/connector-states.json`（version 4，mtime 最新；`default/` 为旧副本，勿动）。
- pkulaw 配置态：`connectors["pkulaw"] = enabled=True / bound=True` → 连接配置生效（**亚型A 排除**）。
- `headerOverrides` keys = `['kdocs','github','tencent-docs','ima-mcp']`，**无 pkulaw**。

### ⚠️ 关键纠偏（避免误判亚型B）

- pkulaw 是**托管 HTTP connector**（经本地 connector-proxy `http://127.0.0.1:60121/<uuid>/mcp` 转发），其令牌由连接器代理托管、**本就不在 `headerOverrides`**——这是正常架构，非"假连接"证据。
- 真正亚型B 判据 = 调用时缺 Authorization 头 → 报 `401 / Bearer missing / 未连接`。本会话实测调用返回的是 **`90001` 服务端业务错误体**（非 Bearer missing）→ proxy 链路带令牌通、**亚型B 不成立**。
- **架构坐实**：`~/.workbuddy/mcp.json` 的 `mcpServers` 仅含 `fasui-caselibrary` / `obsidian`，**无 pkulaw** → pkulaw 是**平台托管型连接器**（令牌由 connector-proxy 托管、不在本地 `mcp.json`/`headerOverrides`）。故 headerOverrides 无 pkulaw 属正常架构，13:33 据此判"亚型B 假连接"是**误判**。
- 故 13:33「亚型B 假连接」诊断 + 本待办三节「亚型B 已愈」表述均需订正：pkulaw 从来不是 headerOverrides 缺令牌型假连接（托管架构使然），它从 13:05 到本会话始终是**亚型D-1 业务层**。

### 亚型判定（决定性证据）

- 错误体：`{"code":"90001","message":"Unclassified Authentication Failure","description":"Error while checking remaining points for user: a4204ed9-3bf5-4340-b392-589ca82dd3a7"}`。
- `description` 明写 **"checking remaining points for user"** + 请求已抵达北法宝服务端并触发点数校验 → **亚型D-1（业务层点数/绑定）坐实**；连接层 A/B/C 全部排除。
- 翻 `enabled` / UI 重新授权 / 重启 WorkBuddy 对 90001 **均无效**（与 13:33 重授权+新对话实测一致，已坐实"UI 连接正常 ≠ API 能调通"）。

### 🔴 账号绑定错配嫌疑（最需老强核实）

- `user: a4204ed9-3bf5-4340-b392-589ca82dd3a7` 是北法宝云端账号标识；本地 config 不存明文账号，无法本地核对它是否 = 已充值/正常的账号。
- 90001 高频根因 = **连接器绑定的北法宝账号 ≠ 你已充值/正常的账号**（类比 ima 个人版/司内版混淆坑）。若网页端"已充值恢复"仍 90001，首选此因。

### 处置（仅北法宝云端侧有效，WorkBuddy 连接层操作无效）

1. 核对北法宝账号 `a4204ed9` 的点数/套餐是否耗尽或到期；
2. 若已充值仍 90001 → **重连 pkulaw 时登录"已充值的正确账号"**，规避个人版/平台版错绑；或将该 UUID 提供给北法宝/平台支持核对"账号-点数绑定"；
3. **勿再在 UI 反复点连接/重连/重启**（连接层已实测全无效，白耗操作）。

### 三路最终定性（2026-09-16 13:51）

| 通道 | 亚型 | 根因 | 解锁动作（仅云端/本地侧） |
|---|---|---|---|
| pkulaw | **D-1** | 北法宝账号 `a4204ed9` 点数/绑定（疑似账号错绑） | 核账号点数/绑定的正确账号重连 |
| 元典 | **D-2·余额0** | `pointBalance=0` | 充值（先 `get_user_balance` 探活，小 top_k） |
| 本地 | — | 无《民诉法解释》源 | 补源到 `法律法规库/程序法/` |

---

## 五、14:37 重授权实测（老强已于 UI 点击 pkulaw 重新授权）

> 老强在 14:37 前于 UI 点击 pkulaw「重新授权」。本段实测该动作是否解除 90001。

### 实测（重授权后仍 90001 · 账号 UUID 不变）

- 重新发起 2 路真实调用：`mcp-law-search-service.search_article`（民诉法解释 §214）+ `mcp-case-search-service.search_case`（成都案）。
- **结果**：两路仍返回 `code:90001 Unclassified Authentication Failure`，`description` 仍为 "checking remaining points for user: `a4204ed9-3bf5-4340-b392-589ca82dd3a7`"。
- **铁证**：重授权前后的账号 UUID **逐字完全一致** → 重新授权仅刷新了本地 OAuth token（连接层），**未改变/修正北法宝云端账号的绑定与点数状态**。

### 结论（重授权无效，亚型 D-1 再坐实）

- UI 重新授权 = 连接层操作，对 90001（业务层点数/绑定）**无效**，与 13:51「翻 enabled / UI 重新授权 / 重启均无效」预判完全一致。
- 唯一能解除 90001 的动作在**北法宝云端账号侧**（充值 / 将连接器绑定到已充值正确账号），不在 WorkBuddy 连接层。
- **红线守正**：未伪造案号/法条，LC-011/012/013 占位标记不动；P1-2 ③ 仍维持「待回填」。
- **下一步建议老强**：
  1. 登录北法宝核对 `a4204ed9` 账号点数/套餐是否耗尽或到期；
  2. 若已充值仍 90001 → 绑定错配（连接器绑定的北法宝账号 ≠ 已充值账号），需解绑后用**已充值账号**重新授权；
  3. 或把该 UUID 提供给北法宝/平台支持核「账号-点数绑定」。

## 相关笔记
- [[R-HG-058-电子病历系统身份标识未妥善保管的合规责任]] (共现关键词: 2026, 账号, 证据)
- [[轨迹卡-北大法宝连接器90001排查的认知纠偏路径-20260916]] (共现关键词: pkulaw, 证据, 账号)
- [[法宝当前 down 未权威回填，已显式标注待核验且有多案原文确认，未静默断言。
- ⚠️ LC-012「原告明知非被告仍起诉被认定滥用诉权」的干净有利生效案本次未命中，本卡以"证明标准（赣04民终670反向确立）+ 重复起诉驳回（豫01民终9747）"为内核，并附经验卡风险对照；滥用诉权门槛高，6660 以合同相对性+货款已清为主防。
- ⏳ 桌面"小强律师办案系统"夹在本环境缺失，6660 桌面投喂 note 待桌面夹恢复后三处同步。]] (共现关键词: 回填, 民法, 合同)
- [[LTI缓存回填-含零条号键与子条款捕获踩坑]] (共现关键词: 2026, 缓存, 回填)
- [[裁判规则回填-2026-08]] (共现关键词: 2026, 回填, 合同)
- [[裁判规则回填-华宇元典-2026-08]] (共现关键词: 2026, 证据, 起诉)
- [[LTI防幻觉能力再评估-2026-08-31]] (共现关键词: 2026, 缓存, 合同)
- [[LTI防幻觉能力三次评估-2026-08-31]] (共现关键词: 2026, 缓存, 合同)
- [[WF-029-静态看板改版前端三坑排障流水线]] (共现关键词: 缓存, 证据)
- [[RULE-025-R-PI-195-这类缓存值必须先核验公告再引用]] (共现关键词: 缓存, ---)
