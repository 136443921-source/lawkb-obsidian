# 卡组可用性登记 · 机动车保险关系认定（2026-09-09）

| 项 | 内容 |
|---|---|
| 卡组 | R-PI-280~287（8 张，审判要件卡） |
| 适用环节 | 要件事实审查·保险关系认定 / 抗辩审查·商业三者险免责事由 |
| 落盘 | `06-沉淀/裁判规则库/机动车交通事故责任纠纷/` |
| 法条状态 | 4 条北大法宝核填（现行有效）；8 条待回源（据实标注） |
| 交付校验 | ERROR 0 / WARN 0 |
| 死链 | 0 |

## 调用纪律

1. **先定位审理环节** → 读 `review_step`
2. **逐项涵摄** → 用 `elements` 比对本案事实
3. **举证校验** → 用 `burden_of_proof` 判断哪方未举证
4. **预判 + 但书** → 用 `ruling.support`/`ruling.reject` 给**候选结论**，同时触发 `negative_note`

## ⚠️ 使用禁区

- **待回源条文（8 条）不得直接援引正文**，须先经 flk.npc.gov.cn / court.gov.cn 二次核填。
- **贵州省口径**：本组为贵州高院裁判尺度统一指引，跨省援引须核当地口径。
- **🔴 铁律 R2**：机器只给候选（【候选·待人工确认】），不自动裁判。
