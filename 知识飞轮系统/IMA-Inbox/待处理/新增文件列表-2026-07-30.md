---
created: 2026-07-30T10:35
updated: 2026-07-30T12:31
---
# IMA 新增文件列表（2026-07-30）

> 生成：每日知识摄入自动化 B 模块（v1.9）｜监控库：合同文书AI助手 / 律师AI助手 / 人伤法律实务助手 / 合规与政府监管AI助手 / 慈善组织合规AI助手

## 本日采集结果

- **IMA 连接器状态**：`disconnected`（2026-07-30 执行时段）
- **新增识别**：**0 篇**
- **原因**：IMA MCP 连接器未连接，无法读取 5 库 `create_time` 与文件列表。按降级策略记录留痕，待 IMA 重连后由下次运行或回补任务复核窗口新增。

## 处理结论

| 库 | 窗口新增 | 成功写入 | 去重跳过 | 失败 |
|----|----------|----------|----------|------|
| 合同文书AI助手 | — | 0 | 0 | 0（不可读） |
| 律师AI助手 | — | 0 | 0 | 0（不可读） |
| 人伤法律实务助手 | — | 0 | 0 | 0（不可读） |
| 合规与政府监管AI助手 | — | 0 | 0 | 0（不可读） |
| 慈善组织合规AI助手 | — | 0 | 0 | 0（不可读） |

> 注：本日 A1（合同文书每日学习）亦因 IMA 断开跳过（A1=0）。相关窗口新增将在 IMA 恢复后由后续运行回补，避免知识遗漏。

## 回补复核记录（2026-07-30 12:19 主动重试）

- **复核目标窗口**：2026-07-29 10:26 → 2026-07-30 10:26（A1/B 窗口）
- **当前 IMA 连接器状态**：`disconnected`（连接器状态面板二次确认，ima-mcp `disconnected`）
- **OpenAPI 直连复核（备用通道）**：使用本机 IMA OpenAPI 凭证（client_id `4e7144…`）探测 5 库：
  - `search_knowledge_base` 返回 **0 个知识库**；
  - `get_knowledge_base` / `get_knowledge_list` 对 5 个目标 kb_id 均返回 **`code=220004 invalid knowledge_base_id`**。
  - 结论：5 个目标库归属于连接器会话账户，本机 OpenAPI 凭证对应账户无权限/无库，**备用通道不可用**。
- **回补结论**：A1/B 窗口新增**仍无法读取**，回补**暂缓**。需用户在 WorkBuddy 连接器管理页**重新信任（Trust）ima-mcp 连接器**后，由下次 08:30 自动化或用户触发"IMA已重连"即时回补。
- **本日已确认项**：揭东某行诉吴某标案入库编号经公开权威刊载核验为 **2024-10-2-103-002**，已回填 R-SH-004 / 原始案 / 摘要三处。

## 回补复核记录（2026-07-30 后续会话 · 用户报告"IMA已重连"后）

- **用户指令**：报告 IMA 已重连，要求立即手动执行 07-29 10:26 → 07-30 10:26 窗口 5 库新增逐条取文+比对回补。
- **本会话运行时实测（三重验证 ima-mcp 仍不可用）**：
  1. `DeferExecuteTool(mcp__ima-mcp__get_knowledge_base_list)` → **`Error: Tool not found in deferred tools index`**（工具未注册，非网络超时）；
  2. `ToolSearch("ima")` → 仅返回 ardot / obsidian / weixinpay / ImageGen 等，**零 ima-mcp 工具**；
  3. `~/.workbuddy/mcp.json` 仅含 `fasui-caselibrary` / `obsidian` / 禁用的 `enterprise-bg-check`，**无 ima 条目**；connector-proxy 聚合服务器亦不含 ima。
- **根因诊断**：IMA 连接器在桌面端虽被"重连/信任"，但**本对话会话的 MCP 工具索引在会话启动时已生成，不会随桌面端连接器状态变化而热刷新**。因此即便桌面显示已连接，本会话内仍无 ima-mcp 工具可调用。属会话级环境问题，无法由代码在会话内强制刷新。
- **回补结论**：**本会话内仍无法执行** A1/B 窗口回补。需**开启全新对话会话**（工具索引将重新生成并纳入已信任的 IMA 连接器），再发送"IMA已重连，立即回补"即可一次性执行。
- **明日 08:30 自动化提示**：自动化为独立会话，若 IMA 信任已持久化，次日运行应能读取；若届时仍 disconnected，需检查桌面端信任是否真正持久化（而非仅本次会话内点击）。
