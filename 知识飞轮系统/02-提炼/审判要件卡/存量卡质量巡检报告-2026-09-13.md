---
title: 存量卡质量巡检报告-2026-09-13
type: 巡检报告·知识飞轮
mode: 存量裁判规则卡质量巡检（第 2 轮）
date: 2026-09-13
author: 小强（自动化 a5995ba3）
skill_version: 审判要件卡拆卡流水线 v1.27.0 → v1.28.0
gate: _check_deadlinks.py v1.7.0
tags:
  - 质量巡检
  - 待回源
  - 死链
  - YAML
created: 2026-09-13T11:05
updated: 2026-09-18T20:35
---

# 存量卡质量巡检报告 · 2026-09-13（第 2 轮）

> 🔴 **铁律 R2**：本报告输出的是**候选推理**，一律标 `【候选·待人工确认】`。
> 本轮**未做任何法条正文回填**（三通道不可用 + 本地源版本门禁筛除），所有"可核填"结论均为候选，须人工复核后方可入文书。

---

## 〇、一句话结论

**本轮没有核填一条法条，但修掉了三类真缺陷**：① 179 张案由路由卡 YAML 整批崩溃（门禁 FAIL → PASS）；② 74 处真死链（30 个"去标点后完全一致"的安全映射）；③ 29 张卡法条段标题**虚标「元典核填·现行有效」**而内容零核填。
**通道全断（三通道均未注册进本会话工具表）**，法条核填 0 条，属据实登记而非遗漏。

---

## 一、巡检两项（先查库，不凭记忆）

### 1.1 待回源

| 项 | 上轮 09-12 | 本轮 09-13 | 变化 |
|---|---|---|---|
| `statute_text_pending: true` 卡 | 42 | **48** | **+6** |
| 待回源条目 | 70 | **81** | +11 |

新增 6 张来自新流水线产物：`R-JG-034/035`（勘察设计、监理合同）、`R-PR-173/175`（刑事附带民事诉讼）、`R-CF-101~108` 系列（慈善/社会组织）、`R-LN-094~096`、`R-PI-373` 等。

### 1.2 死链（门禁 v1.7.0 分类口径）

| 项 | 上轮 09-12 | 本轮（修前） | 本轮（修后） |
|---|---|---|---|
| 门禁退出码 | 1 | **2（FAIL）** | **1（PASS）** |
| YAML 解析失败 | 0 | **179** | **0** ✅ |
| 🔴 真死链（须修） | 132 | **429** | **355**（−74） |
| 🌱 待建笔记（软引用，非缺陷） | 208 | 280 | 280 |
| 命中文件 | — | 281 / 4742 | 281 / 4742 |

> **为什么修前数字暴涨到 429**：179 张坏 YAML 此前被门禁**跳过**（未检查），修复后它们的真实死链才暴露。这是坑 21「先审计工具再审计数据」的再次验证 —— **数字变难看是门禁恢复视力的结果，不是数据变坏**。

---

## 二、法条核填：**0 条**（通道三断 + 版本门禁）

### 2.1 通道状态（🔴 带时间戳，2026-09-13 10:47~10:50 实测）

| 通道 | 配置层 | 工具表层（ToolSearch 精确查） | 调用层（DeferExecuteTool 真调用） | 判定 |
|---|---|---|---|---|
| ① 北大法宝 pkulaw | `connectors: {}` 空 | **not found** | `not found in the deferred tools index` | ❌ **形态②** |
| ② 华宇元典 yuandian | 同上 | **not found** | 未调用（同批未注册） | ❌ **形态②** |
| ③ 企查查 qcc-legal | 同上 | **not found** | 同上报错 | ❌ **形态②** |

- **对照层**：模糊查询 `法律法规 法条检索` / `北大法宝 pkulaw 法条` 等 → `No matching tools found`；会话工具表仅返回 `agentic_search`（金融限定）与 `agent-mail`。
- **元数据**：`headerOverridesBearerStripped: True`、`staleManagedAuthHeadersPurged: True` → 平台曾执行受管鉴权头清理，属**服务端面**，本地改配置无从修复。
- **形态② 的含义**：区别于 `unauthorized`（形态①）与「探针通但检索余额不足」（形态③/④），本轮是**工具根本没注册进本会话工具表** —— 不是鉴权失败，是**不可达**。
- 按 SKILL §5.0，三通道全不可用 → 降级**本地源**（第 4 顺位）。

### 2.2 本地源兜底：坑 33 版本门禁实测

| 项 | 数 |
|---|---|
| 本地法条源索引 | 101 部 / 9463 条 |
| **过版本门禁后准入** | 🔴 **13 部**（筛除 88 部） |
| 硬排除雷区 | 1（`智能体技能库/红队出庭律师/常用法律法规库/中华人民共和国刑法.md`，中国人大网 2019 页面所挂 **1997 原版**：§17「投毒罪」、§274 两档无罚金、无 §134之一/§175之一） |
| 本地可核填 | 2 条 → **实际 0 条**（仅法规名匹配，命中条文 0，无具体条号可填） |
| 真无源 | **79 条** |

> 🔴 **本轮最重要的方法论发现**：本地法律法规库 **101 部里 88 部（87%）没有 `version` / `sxx` / `source: 元典权威回填` 任一版本元数据**。
> **这意味着"本地库兜底"这条通道的可用面远比想象中窄**——表面上 101 部可用，实际只有 13 部敢用于回填。按坑 33「宁可少填，不可填错」，本轮**一条未填**。

### 2.3 拦截的偏差

| # | 偏差 | 处置 |
|---|---|---|
| 1 | **版本幻觉**：红队刑法库（1997 原版） | 🚫 硬排除，未据以回填（上轮已对 R-LN-049/050/051/052、R-HG-064 加注版本警示，本轮复核仍在） |
| 2 | **标题虚标**：29 张卡标题写「法条依据（**元典核填·现行有效**…）」而卡内**零核填、全待回源** | ✅ 已改为「⏳ 待回源·未核填」，**保留官方核验指引子节** |
| 3 | **状态误标**排查：`statute_text_pending: true` 但含真核填条文的卡 | ✅ **0 张** —— R-PI-373 / R-PR-173 / R-PR-175 虽有 2 条 qcc 真核填，但各仍有 5 处待回源，`pending: true` **状态正确，未误标** |
| 4 | 条号位移 / 条号错误 | 本轮**无**（未核填即无位移风险） |

> **第 2 项的价值**：卡内条文全是「待回源」，标题却宣称「元典核填·现行有效」—— 下游消费方（法官智能体）若只看标题就会当作已权威核填引用。**这是"制造确定性的假象"**（坑 40 同源），比留空白危险得多。

---

## 三、死链修复

### 3.1 分类（真死链去重 82 条 / 350 次）

| 档 | 判据 | 数 | 处置 |
|---|---|---|---|
| **[A] 安全可修** | 同编号唯一候选 & 归一化去标点后**完全一致**（sim=1.0，纯括号全半角/连接符漂移） | **30** | ✅ 已修 74 处 / 41 文件 |
| **[B] 候选待确认** | 0.85 ≤ sim < 1.0（简述被简化，如 `R-AY-103-植物新品种权权属侵权…` vs 真名 `…-权属-侵权纠纷…`） | **13** | ⏸️ **未动**，`【候选·待人工确认】` |
| **[C] 不建议自动修** | 相似 <0.85 / 区间引用 `[[R-AY-096-著作权权属-侵权纠纷单案由路由卡]] · [[R-AY-097-商标权权属-侵权纠纷单案由路由卡]]-…` / 无候选 | 39 | ⏸️ 人工 |

### 3.2 [A] 类典型形态（括号全半角）

```
R-PR-161-偷拍偷录取得视听资料之证据能力（非法证据排除利益衡量）
   → R-PR-161-偷拍偷录取得视听资料之证据能力(非法证据排除利益衡量)
R-PR-157-物证之概念与三特征（外部特征·物质属性·存在状况）
   → R-PR-157-物证之概念与三特征(外部特征·物质属性·存在状况)
```
> 去标点后逐字一致，**肉眼不可辨**，只有与物理文件名集合做运算才能发现（坑 9 的标点变体）。

### 3.3 🔴 坑 34 铁律兑现

- 基名集**复用门禁同款口径**：**4662**（全库 + 法律法规库 + 智能体技能库 + 知识库），非自建子集。
- 判定顺序：**先查 inner 是否已在门禁集中 → 命中即不动**，再谈前缀补全。
- v2/v3 只处理正文 `[[...]]` → **0 条可修**；本轮 v4 扩展到 **frontmatter `related_links` 裸名**后才修到 74 处 —— **真死链的 84%（69/82）藏在 related_links 里，正文链接几乎没问题**。这是本轮对修复器的关键增量。
- 未触发回滚（无需）。

---

## 四、🔴 本轮最高价值发现：179 张卡整批 YAML 崩溃

### 4.1 症状

门禁本轮首跑 **exit=2（FAIL）**，报 179 个文件 `while scanning a simple key`。
**全部集中在新建目录 `06-沉淀/裁判规则库/案由路由/`（R-AY-*，270 张中的 179 张）**，同一错误指纹。

### 4.2 根因（生成器 bug）

```yaml
version_delta: |                                    # ← 块标量开始
2020版（法〔2020〕346号第二次修正）：154. 企业名称（商号）合同纠纷。   # ← 顶格，未缩进！
2025版（法〔2025〕227号第三次修正）：160. ...                        # ← 顶格
漂移机理：第五部分整体顺延...                                        # ← 顶格
⚠️ 校验源：法〔2025〕227号全文...                                    # ← 顶格
来源: 《最高人民法院新民事案件案由规定理解与适用（上册）2021-11...》    # ← 下一个合法 key
```

`version_delta: |` 声明块标量，但**内容行顶格** → YAML 判定块为空 → 后续顶格长行被当作 simple key 扫描 → 崩溃。

### 4.3 修复与验证

| 项 | 结果 |
|---|---|
| 修法 | 对 `version_delta: \|` 后至下一个已知顶格 key 之间的行统一缩进 2 空格 |
| 命中 / 改写后通过 | **179 / 179** |
| 仍失败 | **0** |
| 数据完整性 | YAML 失败 0、嵌套损坏 0、正文内容**零改动**（真 `diff` 复核：差异仅为缩进 + Obsidian 自动更新 `updated`） |
| 备份 | `/tmp/案由路由YAML修复_20260913-104852/`（270 份） |

> ⚠️ **Obsidian 副作用**：写入后 179 个文件的 `updated` 被自动刷新为当日时间戳（非本脚本所为）。缩进未被破坏，YAML 复检仍全通过。

---

## 五、写操作汇总（六-B 全程遵守）

| # | 操作 | 量 | 备份 |
|---|---|---|---|
| 1 | 修案由路由卡 YAML 块标量缩进 | 179 文件 | `/tmp/案由路由YAML修复_20260913-104852/`（270） |
| 2 | 修真死链（[A] 类 30 映射） | 41 文件 / 74 处 | `/tmp/存量卡巡检死链修复_20260913-1105/`（41） |
| 3 | 修法条段标题虚标 | 29 文件 | `/tmp/存量卡巡检标题修正_20260913-1112/`（29） |

- 全部 **dry-run 先行**，`cp -n` 备份，04-LOG append-only。
- 同文件多处修改一律 **Python 原子写入**（坑 19）。
- **回滚白名单陷阱已规避**：备份目录内同一文件不存在 `basename` 版与 `a__b__c` 扁平版并存（本轮统一用扁平版）。

---

## 六、剩余待人工处置项

| # | 项 | 量 | 建议 |
|---|---|---|---|
| 1 | **真死链 [B] 类候选** | 13 条 | 逐条 `ls` 核对真名后决定是否替换（多为案由路由卡简述被简化） |
| 2 | **真死链 [C] 类** | 39 条 | 含 5 条区间引用 `[[R-AY-096-著作权权属-侵权纠纷单案由路由卡]] · [[R-AY-097-商标权权属-侵权纠纷单案由路由卡]]-…` 需**人工拆分为两条链接**；其余无源/低相似，建议并入编号治理 |
| 3 | **待回源 81 条** | 79 真无源 | 待三通道恢复（老强 UI 重新授权 / 元典充值）后二次核填；**优先级 A 档＝核心裁判规则直接依赖的条文**（坑 39） |
| 4 | **frontmatter 嵌套列表残留** | 🔴 **165 文件** | **历史遗留，非本轮引入**（本轮改动的 41 文件改动前命中 0）。根因＝frontmatter 放 `[[A\|B]]` 被 Obsidian 吃掉（坑 18）。已有 `_flatten_nested_lists.py` v1.0 可治，但该脚本历史上曾致 42+26 文件损坏、两次回滚 —— **建议单独立项、关闭 Obsidian 同步后执行** |
| 5 | **本地库版本元数据缺失** | 88 / 101 部 | 建议立项为**元数据补标专项**：给 `法律法规库/` 各法补 `version` + `sxx: 现行有效` + `source`。补标后本地兜底通道可用面从 13 部扩大到约百部，**长期收益远大于逐条远程核填** |

---

## 七、连续 3 轮收敛判定

| 轮 | 日期 | 是否有可修项 |
|---|---|---|
| 第 1 轮 | 2026-09-12 | ✅ 有（核填 2、死链 2、拦截 5） |
| 第 2 轮 | 2026-09-13 | ✅ **有**（YAML 179、死链 74、标题 29） |

→ **未触发「连续 3 轮无可修项」**，本自动化继续运行。
但需注意：**法条核填已连续 2 轮为 0**（通道不可达 + 本地源版本门禁）。若第 3 轮通道仍断，建议老强考虑：
① 转向「本地库版本元数据补标专项」（第 5 项，收益最高）；② 或改为**周巡检**而非日巡检。

---

## 八、附：关键资产

| 资产 | 路径 |
|---|---|
| 本轮报告 | `02-提炼/审判要件卡/存量卡质量巡检报告-2026-09-13.md` |
| 待回源匹配报告（JSON） | `02-提炼/审判要件卡/待回源匹配报告-20260913.json` |
| 死链安全映射 + 候选（JSON） | `/tmp/deadlink_safe_map_0913.json` |
| 死链分类明细（JSON） | `/tmp/deadlink_classify_0913.json` |
| 标题修正明细（JSON） | `/tmp/heading_fix_0913.json` |
| YAML 修复器 | `02-提炼/审判要件卡/_fix_ay_blockscalar_20260913.py` |
| 死链修复器 v4（扩展 related_links） | `02-提炼/审判要件卡/_fix_deadlinks_v4_20260913.py` |
| 匹配器 v2（含坑 33 版本门禁） | `02-提炼/审判要件卡/_match_pending_v2_20260913.py` |
| 标题虚标修正器 | `02-提炼/审判要件卡/_fix_statute_heading_20260913.py` |
| 门禁（v1.7.0，未改动） | `02-提炼/审判要件卡/_check_deadlinks.py` |

---

**报告生成**：2026-09-13 10:45~11:20（自动化 a5995ba3 · 存量卡质量巡检第 2 轮）
**核验**：门禁终检 真死链 355 / 待建笔记 280 / YAML 解析失败 0 / 标题虚标 0

## 相关笔记
- [[存量卡质量巡检报告-2026-09-13-第3轮]] (共现关键词: 刑法, 2026, 09)
- [[存量卡质量巡检报告-2026-09-14]] (共现关键词: 刑法, 2026, 09)
- [[存量卡质量巡检报告-2026-09-15]] (共现关键词: 刑法, 2026, 09)
- [[R-GS-022-指定管理人的一般原则与存量管理]] (共现关键词: 合同, 存量)
- [[2026-09-07]] (共现关键词: 2026, 09, YAML)
