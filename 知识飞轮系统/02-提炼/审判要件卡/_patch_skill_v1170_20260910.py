# -*- coding: utf-8 -*-
"""SKILL.md v1.16.0 -> v1.17.0：新增坑 29（自动化指令的『待拆』状态可能是过期快照）"""
import io, re, sys

P = "/Users/chenyouqiang/.workbuddy/skills/审判要件卡拆卡流水线/SKILL.md"
s = io.open(P, encoding="utf-8").read()
orig = s

K29 = """### 29. 🔴🔴 自动化 prompt 里的「待拆清单」是**静态快照**——开工第一步必须查库判状态，不能直接开拆（2026-09-10 实证）

> 坑 28 讲「**排期表里写的章节标题**可能是错的」；本坑讲更靠前的一步：**「这几个类案还没拆」这个前提本身可能已经不成立**。
> 二者是同一病族（**似真性失效**：不报错、输出看似合理、根子上是错），但本坑的**破坏量级更大**——一旦误判，会**整批复写**。

> **实证**：每日自动化 prompt 固定写「共 6 个待拆」，并列出 6 个类案＋号段＋落盘目录，格式完整、字段齐全，读起来完全可信。
> 2026-09-10 实地查库发现：**6 个类案在 09-04 ~ 09-08 期间已全部拆完**（累计 285 张）。
> 若照 prompt 直接开工，将**重复拆出约 285 张卡**，并占用 `HT / LD / HY / GS` **四个域的大段号段**——
> 号段一旦被占，按规范第三节「已落地卡禁止批量改名」，**几乎不可逆**。

**为什么这类误判高发（三个结构性原因）**：
| # | 原因 | 说明 |
|---|---|---|
| ① | **prompt 是静态的，库是动态的** | 自动化 prompt 写一次跑很多天，而库每天被本流水线／每日摄入流水线并发改写 |
| ② | **格式完整度会伪装成可信度** | 清单带号段、页数、目录，越详细越像"已核实"；但这恰恰是**写 prompt 那一刻**的快照 |
| ③ | **前一轮的收尾汇报会被当成现状** | 上一轮写「剩余 6 个待拆」，下一轮的人（＝下一轮的 AI）直接继承，没人复核 |

**开工前置三步（写进每次拆卡的第一步，不可跳过）**：
1. **按类案特征词反查 `source`，不以号段判断**——号段会被并发流水线占用，且已落地卡与号段并非一一对应；
   ```bash
   find . -name "R-*.md" | xargs grep -l "<类案关键词>" | wc -l
   ```
2. **按 `card_type` 精确过滤**（区间计数会把同号段的他类卡算进来，本轮号段计数 398 vs 真实 391，差 7 张）：
   ```bash
   find . -name "R-*.md" | xargs grep -l "^card_type: 审判要件卡" | wc -l
   ```
3. **交叉校验总数自洽**：`六类案 + 其余类案 + 补拆批 = 全库审判要件卡总数`，对不上就是查漏或重复。

**判定「全部完成」后的处置（用户指令约束）**：
> **只汇报「全部拆卡完成」+ 最终统计，不重复拆卡、不生成任何文件。**
> 可做的**只读**动作：全库门禁复跑、质量指标扫描（四星号／双书名号／缺段／`statute_text_pending`）、六层挂载存在性核验。
> 发现缺陷（如 YAML 解析失败）**可以修**，但须遵守六-B（备份 → 修 → 复检），并**明确区分「本次修复」与「历史遗留」**——本轮修复 1 处门禁 FAIL，历史死链 172 条据实登记、未擅自处置。

**配套建议**：若确认全部完成，应向用户提出**关闭或改造该自动化**（如改为「密度审计补拆」模式），
否则它会每天重复跑一遍"查库 → 发现完成 → 汇报"，空耗运行。

---

"""

# 1) 插入坑 29（在 7.1 节之前）
anchor = "### 7.1 📌 待回源条文批量二次核填 SOP"
assert s.count(anchor) == 1, "7.1 anchor not unique"
s = s.replace(anchor, K29 + anchor)

# 2) 第七节标题计数
old_h = "## 七、⚠️ 二十三个必踩的坑（已验证，2026-09-09 更新）"
new_h = "## 七、⚠️ 二十九个必踩的坑（已验证，2026-09-10 更新）"
assert s.count(old_h) == 1, "h7 not found"
s = s.replace(old_h, new_h)

# 3) frontmatter version
assert s.count("version: 1.16.0") == 1, "version not found"
s = s.replace("version: 1.16.0", "version: 1.17.0")

# 4) 变更日志
old_log = "## 变更日志\n"
new_log = """## 变更日志

### v1.17.0（2026年9月10日）

- **新增** 第七节**坑 29**（🔴🔴 **自动化 prompt 的「待拆清单」是静态快照**：六个类案在 09-04~09-08 已全部拆完而 prompt 仍写「6 个待拆」；照单开拆将**重复产出约 285 张**并占用 HT/LD/HY/GS 四域大段号段，**号段被占按规范第三节几乎不可逆**；三个结构性成因＝prompt 静态 vs 库动态／格式完整度伪装可信度／前轮收尾汇报被当现状；**开工前置三步**＝按类案特征词反查 source（不以号段判）→ 按 `card_type` 精确过滤（区间计数会多算，本轮 398 vs 391 差 7）→ 交叉校验总数自洽；判定完成后**只汇报不生成文件**，只读核验可做，缺陷可修但须六-B 并区分「本次修复」与「历史遗留」）
- **修复** `05-调用/模拟法庭调用记录/20260909-6658吊顶案补充审理推演.md` 的 frontmatter `knowledge_called` 逗号分隔裸标量致 YAML 解析失败 → 改内联列表；全库门禁**解析失败 1 → 0**（呼应坑 21：红着的门禁等于没门禁）
- **实证** 2026-09-10 六类案核验：全库 `R-*` 1331 张 / 审判要件卡 **391** 张，六类案 **285** 张（商品房 28／保险 43／新就业形态 36／婚姻家庭 52／抚养 36／破产 90），质量扫描四星号·双书名号·缺段·`statute_text_pending` **全 0**
"""
assert s.count(old_log) == 1, "changelog anchor not unique"
s = s.replace(old_log, new_log, 1)

io.open(P, "w", encoding="utf-8").write(s)
print("PATCH OK: %d -> %d bytes" % (len(orig), len(s)))
