#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""SKILL v1.19.0 -> v1.20.0 原子写入（坑 19：同文件多处修改必须一次写）"""
import io, re

P = "/Users/chenyouqiang/.workbuddy/skills/审判要件卡拆卡流水线/SKILL.md"
t = io.open(P, encoding="utf-8").read()

# ---- ① frontmatter version ----
t2 = t.replace("version: 1.19.0", "version: 1.20.0", 1)
assert t2 != t, "version 未替换"
t = t2

# ---- ② 标题「三十一个」->「三十二个」 ----
t = t.replace("## 七、⚠️ 三十一个必踩的坑（已验证，2026-09-11 更新）",
              "## 七、⚠️ 三十二个必踩的坑（已验证，2026-09-12 更新）", 1)

# ---- ③ 新增坑 32（插在坑 31 之后、7.1 之前） ----
K32 = """
---

32. 🔴🔴 **页级密度审计会把「source 只标起始页」误判成空白区——判「还有没拆」必须读卡正文（2026-09-12 第十五批实证，密度审计模式的元坑）

> 坑 28（章节标题会错）、坑 29（待拆清单是快照）讲的都是**立项输入**不可信；
> 本坑讲的是**审计工具本身**不可信——而且它比前两个更危险，因为它是「我自己为纠错而造的工具」。
> **一旦误判，代价与坑 29 同级：整批重复建卡 + 白占一大段号段。**

> **实证（2026-09-12，同一轮内连中 4 次）**：
> 用逐页覆盖矩阵审计《第二卷》，报告第五部分婚姻家庭空白 `278 / 291`、第六部分抚养空白 `324 / 334`、
> 第十部分三撤空白 `534-536`，共 5 段「真空白」。实地读卡正文后发现——**一段都不是空白**：
>
> | 报的空白页 | 真实内容 | 实际已被哪张卡覆盖 |
> |---|---|---|
> | p278 | 夫妻共同债务 2.(3)~(6)（超出日常生活／公司债／担保） | **R-HY-027**（E1~E4 四要件全含，source 只写 `PDF页277`） |
> | p291 | 人身安全保护令执行与诉讼效力 | **R-HY-047**（执行主体／协助手续／证据效力／违反后果全含，source 只写 `PDF页290`） |
> | p324 | 人工授精子女、继子女抚养（解释二§18/§19） | **R-HY-069** + **R-HY-068**（source 只写 `PDF页323`） |
> | p334 | 第五章社会治理延伸六项 | **R-HY-086**（source 只写 `PDF页333`） |
> | p534-536 | 三撤与申请再审／执行异议之诉的程序分流 | **R-PR-057**（全文 7440 字，§299 程序吸收、选择权限制全含，source 写 `PDF页529-533`） |
>
> **根因**：卡片 `source` 的页码是**起始页或主引用页**，不是覆盖区间。
> 一条规则跨 2~3 页时，续页在覆盖矩阵里就是空洞——**洞是标注粒度造成的，不是内容缺失**。

> **「换更细的审计器」也不灵（本次实测两种工具都假报）**：
> ① **页级覆盖矩阵** → 假空白（上表）；
> ② **内容级审计**（提取指南骨架标题，去序号后取前 12 字，在卡全文中做子串匹配）
> 　→ 第四部分报 `p230 劳动报酬诉请审查 / p231 解除劳动关系审查 / p232 工时` 未覆盖，
> 　实测 **R-LD-022 / R-LD-025 / R-LD-027** 三张卡完整覆盖——
> 　**因为卡正文不会逐字照抄指南的小标题**（卡写「劳动报酬诉请的审查」，指南骨架写「(二)支付劳动报酬诉请的审查」）。
> → 两种自动审计**互补但都不可信**，都只能用于**排序候选**，不能用于**定案**。

> ✅ **三判定案流程（写进密度审计模式的第一步）**：
> 1. **审计只产出候选，不产结论**——页级矩阵 + 内容级骨架各跑一遍，取并集作候选；
> 2. **逐个候选「读卡正文」**——`grep` 该部分已有卡的 `source` 与「一、裁判规则 / 二、审查要点」两段，
> 　　确认候选内容是否已在卡内（**这一步是唯一判据**）；
> 3. **连续 3 个候选都被判「已覆盖」→ 判定全书饱和**，转「只汇报不建卡」模式（本次即如此收尾）。
>
> **饱和判据要说清**：不是「找不到空白」，而是「找到的候选经读卡核实全部已被覆盖」。

> **配套：审计器自身的三类 bug（本次全中，呼应坑 21「先审计工具再审计数据」）**
> | bug | 表现 | 修法 |
> |---|---|---|
> | 正则回溯出假页号 | `PDF页114-115` 被回溯出 `11`，混入覆盖集合 | 数字后加 `(?![\\d\\-–~至])` |
> | 扉页文本层受损 | 254 页空文本、307 页页首 `O o -- -`、545 页「第十一部分」断字 → 自动定位漏 3 个部分 | 用「**下一页首 200 字含『主持人』**」反查扉页，漏的按实地定位表硬编码并运行时校验 |
> | `source` 无部分名 | 43 张保险卡只写 `PDF页114-115`，按部分名归类得 0 | 按页号落入部分区间兜底 |
>
> **🔴 取号前的最后一道闸**：候选区未「读卡正文」确认前，**不要取号**。
> 本次已取 PR 086-089 / HY 092-101 共 14 个号，核实后全部 `--release` 归还——
> **先取号再核实的顺序是错的**：号段一旦被写进卡，按规范第三节「已落地卡禁止批量改名」几乎不可逆。

> **📌 附带收获**：本轮顺带修掉上一批漏扫的 **16 处页码偏差**（Δ 恒 −1，1-based 遗留），
> 位于 `02-提炼` 的入库索引与踩坑卡——**说明坑 26 的「建完必全库复扫」此前只扫了 `06-沉淀`，扫漏了 `02-提炼`**。
> **复扫范围必须是 `06-沉淀` + `02-提炼` 全覆盖**。

---

"""
anchor = "\n### 7.1 📌 待回源条文批量二次核填 SOP（2026-09-09 破产批实证）"
assert anchor in t
t = t.replace(anchor, K32 + "### 7.1 📌 待回源条文批量二次核填 SOP（2026-09-09 破产批实证）", 1)

# ---- ④ 变更日志新增 v1.20.0 ----
LOG = """### v1.20.0（2026年9月12日）

- **新增** 第七节**坑 32**（🔴🔴 **页级密度审计把「source 只标起始页」误判成空白区**——密度审计模式的元坑：同轮连中 4 次，报的 5 段"真空白"经**读卡正文**核实**全部已被 R-HY-027／047／068／069／086／R-PR-057 覆盖**；根因＝source 页码是起始页而非覆盖区间，**洞是标注粒度造成、不是内容缺失**；**换内容级审计也不灵**——骨架标题子串匹配因卡正文不照抄指南小标题而假报（R-LD-022/025/027 实例）；✅ **三判定案**＝审计只产候选 → 逐个**读卡正文**判覆盖（唯一判据）→ **连续 3 个候选均"已覆盖"即判全书饱和**转"只汇报不建卡"；**取号前必须完成读卡核实**（本次先取 14 号后全部 release，顺序是错的）；附**审计器自身三类 bug**（正则回溯假页号 `114-115`→`11`／三处扉页文本层受损用「下一页含主持人」反查／source 无部分名按页号兜底））
- **修复** 上一批漏扫的 **16 处页码偏差**（Δ 恒 −1，1-based 遗留，位于 `02-提炼` 入库索引与踩坑卡）；**坑 26 复扫范围须含 `02-提炼`**，仅扫 `06-沉淀` 会漏；复扫 877 处残留 **0**
- **实证** 远程双通道第三种失效形态：配置层 `enabled=True/bound=True`，但 ToolSearch 精确查 + DeferExecuteTool 均返 **"not found in the deferred tools index"**（**未注册进本会话工具表**，区别于坑 31 的 `unauthorized`）→ 仍按 §5.0 第三顺位走本地库 + 复用历史批次已核填条文（法宝／元典／最高法官网核填属"已在库权威文本"，可直接引用）
- **新增** 本地法规提取两处样式坑：**民法典条号后是 U+2002（EN SPACE）** 而非普通/全角空格（坑 14 第三代，须用 `[^\\S\\n]*`）；中文数字转条号后**勿重复加"第/条"**（曾写 `^一千零六十四条条` 恒不匹配）
- **实战** 第十五批：密度审计 → 立项纠偏 → **判定全书饱和、未建卡**（零产出、零号段占用）；全库审判要件卡 **447** 张

"""
anchor2 = "### v1.19.0（2026年9月11日）"
assert anchor2 in t
t = t.replace(anchor2, LOG + anchor2, 1)

io.open(P, "w", encoding="utf-8").write(t)
print("✅ SKILL 已升 v1.20.0")
print("   version:", re.search(r"^version: (.+)$", t, re.M).group(1))
print("   坑标题:", re.search(r"## 七、(.+)", t).group(1))
print("   坑32在位:", "32. 🔴🔴" in t)
print("   变更日志 v1.20.0 在位:", "### v1.20.0" in t)
