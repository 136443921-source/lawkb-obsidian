#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""SKILL v1.10.0 → v1.11.0：新增坑 20/21/22（2026-09-08 死链专项实证）"""
import io, os, shutil, time

P = os.path.expanduser("~/.workbuddy/skills/审判要件卡拆卡流水线/SKILL.md")
BK = "/tmp/SKILL备份_20260908"
os.makedirs(BK, exist_ok=True)
shutil.copy2(P, os.path.join(BK, "SKILL.md.v1.10.0"))
print("💾 已备份 →", BK)

s = io.open(P, encoding="utf-8").read()
orig = s

s = s.replace("version: 1.10.0", "version: 1.11.0", 1)
s = s.replace("## 七、⚠️ 十九个必踩的坑（已验证，2026-09-08 更新）",
              "## 七、⚠️ 二十二个必踩的坑（已验证，2026-09-08 更新）", 1)

ANCHOR = "\n## 八、交付校验（ERROR=0 才准交付）"
NEW_PITS = """
20. **🔴 渲染器硬编码书名号 → 《《X》》 双书名号嵌套（2026-09-08 实证，全库 12 处受害）**
    > **实证**：生成器写死 `f"**《{v['law']}》{v['article']}**"`，但法条回填库 `law` 字段本身可能**已带书名号**（如「《中华人民共和国环境保护法》」），渲染成 `《《中华人民共和国环境保护法》》`。全库扫描命中 10 个文件 12 处（5 张 GS 新卡 + 5 张历史 CF 卡），说明**这个病从早期就存在**。
    >
    > **三种形态**（一条正则全覆盖：`《《(.+?)》([^》]*)》` → `《\\1》\\2`）：
    > | 形态 | 样例 | 成因 |
    > |---|---|---|
    > | A 型 | `《《中华人民共和国环境保护法》》` | law 已带书名号，article 为空 |
    > | B 型 | `《《…非法集资…意见》第九条第四款》` | law 已带书名号，article 非空 |
    > | C 型 | `《《民法典》侵权责任编》`（历史 CF 卡） | 同上，article＝编名 |
    >
    > **修法（Fix-the-source，改模板而非改产品）**：
    > ```python
    > def wrap_book(name):
    >     n = str(name).strip()
    >     return n if "《" in n else f"《{n}》"   # 🔴 不能用 startswith/endswith
    > ```
    > **为什么不能判 startswith+endswith**：R-GS-020 的 law 是「《…意见》第九条第四款」——书名号包裹的是**法律名**而非整个字段，`endswith("》")` 为假 → 又被套一层。**「含《即视为已格式化」才是稳的判据。**
    >
    > **铁律**：凡是「字段值可能已经带格式」的渲染（书名号 / 引号 / 单位符号），**必须写成幂等函数**，不能硬编码拼接。

21. **🔴🔴 门禁工具自己会误报——先怀疑门禁，再怀疑数据（2026-09-08 死链专项核心教训）**
    这是坑 14（「MISS 不等于不存在」）的**升级版**：坑 14 说先怀疑**检索范围**，坑 21 说先怀疑**解析器本身**。
    > **实证**：老强交办「全库 1301 条历史死链清理」。实跑发现 **1301 里有七成是门禁自己造的假**：
    > | 病 | 机理 | 虚报量 |
    > |---|---|---|
    > | related_links 写成**字符串** | `for x in <str>` 逐字符迭代，拆出 `R` `-` `8` `,` `医` | −883 次 |
    > | related_links 写成**嵌套列表** `- - 项` | `str(['项'])` → strip("[]") 后残留单引号 | −239 次 |
    > | **解析噪声** `...` `名` `0` | 当成链接计入死链 | −24 次 |
    > | **无 frontmatter** 被判解析失败 | 32 个索引/报告类文档触发 exit(2)，门禁**永远红** | 门禁失效 |
    >
    > **四次升级**（`_check_deadlinks.py` v1.2.0 → v1.6.0）：v1.3.0 兼容字符串 / v1.3.1 递归展平嵌套 / v1.4.0 噪声过滤 / v1.5.0 外部白名单 / v1.6.0 区分「无 frontmatter（合法跳过）」与「YAML 损坏（真 FAIL）」。
    >
    > **三条铁律**：
    > 1. **拿到坏数字先审计工具**，不要急着改数据。问：「这个数字是工具测出来的，还是工具造出来的？」
    > 2. **红着的门禁等于没有门禁**——误报淹没真问题时，人会养成无视门禁的习惯，比没门禁更危险。
    > 3. **正则/解析器要写单元自检**：本批 `_patch_deadlink_v131` 自带 5 个 case（字符串 / 嵌套 / 混合 / 纯列表 / 三层嵌套），**改解析器必须同时改自检**。

22. **🔴 跨库链接 ≠ 死链（2026-09-08 实证：155 个「死链」里 99 个目标真实存在）**
    > **实证**：D 类 48 个 + B 类 12 个中，99 个（212 次）目标**真实存在**，只是位于 LawKB 之外（桌面办案系统 / Documents / IMA 缓存）。Obsidian wiki 链接跨 vault 本就无效——**链接失效 ≠ 链接错误**。
    >
    > **处置**：建**外部链接白名单** `_external_links_whitelist.json`（指纹 → 外部真实路径），门禁命中即放行，**同时保留路径可追溯**。真·无目标的才留在死链里交人工。
    >
    > **副产品**：白名单扫描顺带发现 13 个「文件名＝内容残片」的垃圾页（历史断链消解器 v1.2 把 markdown 残片当链接名建页），隔离 14 项后死链数**先升后降**——因为原指向带引号副本的链接变成唯一候选，反而可以自动改指正规卡。
    >
    > **铁律**：**清理会让数字短暂变难看，别急着回滚**——先把重复项清掉，再收网，数字才会真正下去。

"""
assert ANCHOR in s
s = s.replace(ANCHOR, NEW_PITS + ANCHOR.lstrip("\n"), 1)

OLD_LOG = "## 变更日志\n\n### v1.10.0（2026年9月8日）"
NEW_LOG = """## 变更日志

### v1.11.0（2026年9月8日）

- **新增** 第七节**坑 20**（🔴 渲染器硬编码书名号 → `《《X》》` 双书名号嵌套；三种形态、幂等 `wrap_book()` 修法、**不能用 startswith/endswith 判**的原因）
- **新增** 第七节**坑 21**（🔴🔴 门禁工具自己会误报——先怀疑门禁再怀疑数据；字符串型 / 嵌套列表 / 噪声 / 无 frontmatter 四类虚报，1301 里七成是假；**红着的门禁等于没有门禁**；改解析器必须同时改单元自检）
- **新增** 第七节**坑 22**（🔴 跨库链接 ≠ 死链；外部白名单留路径可追溯；清理会让数字短暂变难看，别急回滚）
- 标题「十九个」→「二十二个」，数据源：2026-09-08 全库死链专项清理（1301 → 153 次，−88.2%）

### v1.10.0（2026年9月8日）"""
assert OLD_LOG in s
s = s.replace(OLD_LOG, NEW_LOG, 1)

assert s != orig
io.open(P, "w", encoding="utf-8").write(s)
print("✏️  已写入 v1.11.0")

s2 = io.open(P, encoding="utf-8").read()
ok = all(["version: 1.11.0" in s2, "二十二个必踩的坑" in s2,
          "坑 20" in s2 or "20. **🔴 渲染器硬编码书名号" in s2,
          "坑 21" in s2 or "21. **🔴🔴 门禁工具自己会误报" in s2,
          "坑 22" in s2 or "22. **🔴 跨库链接" in s2,
          "### v1.11.0" in s2])
print("🔁 复验：", "✅ 全部就位" if ok else "❌ 有缺")
print("   文件大小：", len(s2), "字符")
