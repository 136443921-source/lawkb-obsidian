---
created: 2026-07-28T23:10
updated: 2026-08-28T19:31
tags:
  - 知识管理
  - 日志
---
# 双向链接断链消解报告 · 2026-07-28

> 任务来源：连接层补链（2026-07-28）遗留项——历史遗留 895 处（精确统计为 **919 次出现 / 257 个唯一目标**）双向链接断链。
> 执行：非破坏性（新建 `03-连接/概念页/` 子目录 + 修正明确错写 + 清理畸形源链接），飞轮系统为 git 仓库可整体回滚。

## 一、执行前基线（2026-07-28 连接层补链后）

| 指标 | 值 |
|---|---|
| 全部 `[[链接]]` 总数 | 4975 |
| 断链唯一目标 | 257 |
| 断链总出现次数 | 919 |
| 含断链文件数 | 126 |

断链高度聚类：医疗纠纷/医患法律、公司法/股权、慈善募捐、合同违约、诉讼程序五大主题。

## 二、消解策略

1. **概念页生成（主要手段）**：为 245 个真实断链目标在 `03-连接/概念页/` 建概念枢纽页（文件名精确匹配断链 target，使链接立即解析）。含法条引用型（如 `[[《公司法》第23条|《公司法》第23条]]`，占 76 次出现）与概念型（内置高频法律概念释义词典）。
2. **近似错写源修正**：`[[民事诉讼法 （红队）]]` 等剥离红/蓝队标记指向真实文件（全局剥离 116 处红蓝队标记）。
3. **畸形源链接修复**：`[[35-[[基金会与境外基金会合作案]]` 等（源文件误写的双 `[[`）修正为 `[[基金会与境外基金会合作案]]`（全局修正 40 处）。
4. **文件/路径链接排除**：`[[03-连接/[[知识图谱-2026-07|知识图谱-2026-07]].html]]` 等文件链接不再计入笔记断链（kg_scan 同步修复，剥 `.html` 等扩展名）。
5. **脏名护栏**：消解器绝不创建含 `[[`/`#`/红蓝队 的概念页（仅排除数字前缀如 `01-王德明…` 案件编号，因其为合法笔记名）。

## 三、执行后结果（权威重扫）

| 指标 | 消解前 | 消解后 | 变化 |
|---|---|---|---|
| 双向链接断链 | 919 次 / 257 目标 | **0** | **−100%** |
| 笔记节点总数 | 757 | 992 | +235（概念页） |
| 双向链接总数 | 3831 | 8505 | +4674 |
| 孤儿笔记 | 187 (24.8%) | **29 (2.9%)** | −158 |
| 网络密度 | 0.01346 | 0.0173 | +29% |
| 联通节点 | 568 | 963 | +395 |

> 注：孤儿 29 篇为运维/过程性笔记（含 `连接枢纽-运维过程` 等元笔记），非知识卡片，与断链无关。

## 四、关键脚本（已持久化）

- `03-连接/scripts/resolve_broken_links.py` — 断链消解器：生成概念页 + 近似错写源修正（含脏名护栏、幂等）。
- `03-连接/scripts/fix_source_links.py` — 源链接修复器：修正畸形 `[[数字-[[` 链接、剥离红/蓝队标记、清理空 `[[ ]]` 链接、删除垃圾概念页。
- `03-连接/scripts/kg_scan.py` — 已修复：文件/路径链接（含扩展名）不再误计为笔记断链。

## 五、遗留与建议

- 消解后断链 **0**，知识图谱连通性达历史最佳。
- `连接枢纽-运维过程.md` 的关联段曾误链 `.workbuddy` 记忆文件（已手动清理 2 处）；该枢纽非 link_cards_rules.py 的 GROUPS 分类，不会被周日脚本覆写。
- **防复已落地（2026-07-28 23:13）**：`fix_source_links.py` + `resolve_broken_links.py` 已纳入「周日知识维护批处理」自动化 **阶段 2.5（断链自愈）**，每周日 21:20 自动清理新畸形/红队链接并消解新断链（见自动化 v1.6）。

## 六、回归修复（2026-07-28 23:13 收尾，自愈流水线固化前）

上一轮结尾重扫显示断链回落至 **21 处 / 11 唯一目标**，经核查为三类新引入的断链（非消解不彻底）：

| 类别 | 数量 | 成因 | 修复 |
|---|---|---|---|
| 去重残留 | 11（`合同纠纷-模板_1`） | 知识库压缩去重将 `合同纠纷-模板_1.md` 并入 `合同纠纷-模板.md` 并删除，11 篇源笔记仍链旧名 | `fix_source_links.py` 新增**去重重定向** `[[X_N]]`→`[[X]]`（当 `X` 存在）；本批重定向 12 处 |
| 报告示例误报 | 4（在 `孤立笔记检测报告`/`知识库压缩去重报告` 内，如 `[[民事诉讼法 （红队）]]`、`[[35-[[基金会…]]`） | 报告正文用反引号示例写的 `[[…]]` 被 kg_scan 误判为断链 | kg_scan + 消解器均新增 **META_SKIP**（`孤立笔记检测报告`、`知识库压缩去重报告`），其内示例链接不计入断链、不建概念页 |
| `#` 锚点 + meta 链接误判 | `裁判规则库#规则XXX`(108) + `[[链接检查报告-2026-07-19|链接检查报告]]-*`(90+) + `补链日志-*`/`断链消解报告-*` | 消解器链接正则未剥离 `#` 锚点、且把 meta 报告目录排除出 `existing`，导致指向真实报告文件的链接被误判断链 | 消解器链接正则对齐 kg_scan（剥离 `#`/`\|`）；`existing` 改含**全部** .md（含 meta 报告），仅"扫描断链目标"时跳过 meta 目录 |
| 链接名与文件名不符 | 6（05-调用/案件管理，如 `[[陈长卫劳务致害案-案件笔记|[[陈长卫劳务致害案|陈长卫劳务致害案]]-案件笔记]]` 实际为 `…-知识调用映射.md`；另 4 个目标笔记从未创建） | 笔记间互链名与实际文件名不一致，部分目标笔记规划未建 | 消解器为这 6 个目标建**概念桩页**承接（新建 6 个，链接即解析） |

**修复后权威重扫（2026-07-28 23:13）**：

| 指标 | 值 |
|---|---|
| 双向链接断链 | **0** |
| 笔记节点 | 1002 |
| 双向链接 | 8444 |
| 孤儿笔记 | 31 (3.1%) |
| 网络密度 | 0.01684 |
| 联通节点 | 971 |

— 断链消解 v1.1（2026-07-28，含回归修复 + 自愈流水线固化）

## 相关笔记
- [[知识健康度报告-2026-08-28]] (共现关键词: py, 28, 链接)
- [[2026-08-27]] (共现关键词: 违约, py, 2026)
- [[灵感捕获模板]] (共现关键词: 断链, 消解, 07)
- [[2026-07-13]] (共现关键词: 07, 报告, 合同)
- [[律师申请调查令的几点心得]] (共现关键词: 断链, 消解, 07)
- [[陈长卫案组合分析-医法工伤新规-2026-07-24]] (共现关键词: 断链, 07, 2026)
- [[和解协议]] (共现关键词: 医疗, ---, 07)
- [[链接检查报告-2026-07-26]] (共现关键词: 链接, 医疗, 公司)
- [[补链日志-2026-08-13]] (共现关键词: 链接, 断链, 合同)
- [[企业合规体系建设要点]] (共现关键词: 消解, 2026, 断链)
- [[诉讼流程]] (共现关键词: 断链, 消解, 医疗)
- [[护士执业注册]] (共现关键词: 断链, 消解, 医疗)
- [[小强律师数字分身系统 3.0 使用指导手册]] (共现关键词: 断链, 消解, 07)
- [[补链日志-2026-08-09]] (共现关键词: 合同, 断链, py)
- [[2026-08-09]] (共现关键词: py, 违约, 笔记)
- [[MEMORY]] (共现关键词: 07, 合同, 笔记)
- [[2026-07-28]] (共现关键词: 合同, 断链, py)
