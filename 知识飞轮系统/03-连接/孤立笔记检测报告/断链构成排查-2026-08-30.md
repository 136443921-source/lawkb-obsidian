---
created: 2026-08-30T21:21
updated: 2026-08-30T21:33
---
# 断链构成排查报告 · 2026-08-30（W35 周日批处理后续）

> 排查对象：kg_scan v1.4 标记的 `unresolved_links = 100`（2026-08 图谱口径）
> 排查方法：复刻 kg_scan 链接解析逻辑，抓取全部 100 条 `(源文件, 目标名)`，按「目标去哪了 / 源在哪种目录 / 是否畸形嵌套」三维分类。
> 诊断脚本：`03-连接/scripts/_diag_unresolved.py` + `_diag_unresolved2.py`（落盘 `_diag_unresolved.json` / `_diag_unresolved2.json`）

---

## 🎯 核心结论

**100/100 条均为「畸形嵌套 wikilink」，0 条真知识断链，0 条备份目录噪声。**

| 分类 | 数量 | 说明 |
|---|---:|---|
| 真知识断链（目标全量缺失） | **0** | 干净目标且工作区全量不存在 |
| 备份目录噪声（目标仅存于 `.bak`） | **0** | 非备份副本污染 |
| **畸形嵌套 wikilink（链接本身写坏）** | **100** | 正则把内层 `[[` 抓成目标名 |
| ├─ 内层干净目标真实存在（修括号即可消解） | 55 | 如 `[[[民法典第1218条]]` → 内层 `民法典第1218条` 命中 2 个 .md |
| └─ 内层目标仍嵌套/缺失（需智能归一） | 45 | 如 `[[第五章 [[医院人力资源管理法律实务]]`（引用式嵌套，本就不是独立笔记） |

---

## 🔍 分类依据（证据）

**1. 源目录分布（均来自真知识目录，无运维/系统噪声）：**
- `03-连接/概念页/` 88 条（阶段2.5 新建的 23 个桩页重灾区）
- `IMA-Inbox/待处理/` 9 条（2026-07-23/24 投喂映射清单，源笔记本就含嵌套括号）
- `06-沉淀/裁判规则库/` 3 条（R-PI-088 / R-PI-098 / R-CF-066）

**2. 畸形类型（原文抽样）：**
- 三重括号：`[[[民法典第1218条]]`、`[[[R-PI-053]]`、`[[[民间非营利组织会计制度]]`
- 引用式嵌套：`[[第五章 [[医院人力资源管理法律实务]]`、`[[R-PI-130-[[医疗损害鉴定]]`、`[[合同-[[R-HT-023]]`
- 带书目号前缀：`[[《[[中华人民共和国民事诉讼法]]`

**3. 根因定位（铁证）：**
- 桩页 `03-连接/概念页/R-LN-030.md` 创建时间 = `Aug 30 21:15:18 2026`（= 本论阶段2.5 执行窗口）
- 其正文第 32–34 行即 `- [[[民法典第1218条]]` 三重括号
- 内层目标 `民法典第1218条 / R-PI-053 / 民间非营利组织会计制度 / 医疗纠纷预防和处理条例` 经 `find` 全量校验**均真实存在**（2~3 个 .md 命中）

→ **W34 跟踪点预警的"断链再生 0→100"并非真知识断链再生，而是 `resolve_broken_links.py` 生成概念桩页时把引用清单写成了三重/嵌套括号，被 kg_scan 正则判为断链。** 属"自愈脚本自产断链"的展示型假阳。

---

## ✅ 处置建议（未自动执行，待拍板）

1. **阶段2.5 步骤A 脚本增强**（首选，治本）：在 `fix_source_links.py` 增加「括号归一」规则——
   - 三重/四重 `[[[X]]]` / `[[[[X]]]]` → `[[X]]`（覆盖 55 条可直接消解）
   - 引用式嵌套 `[[A [[B]]` → 解析出最内层 `[[B]]` 或转纯文本（覆盖 45 条）
2. **一次性归一补丁**（次选）：对 `03-连接/概念页/*.md` 与 `IMA-Inbox/待处理/*.md` 跑一次括号清洗（需先六-B 备份）。
3. **kg_scan 正则加固**（辅助）：链接解析前先 `re.sub(r'\[{2,}', '[[', ...)` 折叠多余前缀 `[`，避免"目标名含 `[`"误判。
4. **本周盲区扫描/质量周报**：断链数应计为 **0（真）**，100 为假阳，勿误读为健康劣化。

---

## ✅ 处置结果（已落地 · 2026-08-30 21:25）

**已采纳建议①，将「括号归一」规则写入 `fix_source_links.py`（v1.1）**：

- 新增 `BRACKET_OPEN / BRACKET_CLOSE / REF_NEST` 三个正则 + `_normalize_brackets()` 迭代收敛函数；
- **覆盖范围扩展**：旧 4 规则仍跳过概念页目录，新括号归一**对概念页目录也生效**（W35 排查发现 88/100 假阳就在概念桩页），仅 meta 报告目录（示例链接）仍跳过；
- 幂等、非破坏、纯折叠，不删内容、不改合法 `[[X]]` 链接。

**执行流程（严格走六-B 安全前置）**：
1. 六-B 时间戳备份：`WorkBuddy/Claw/backups/周日批_括号归一_20260830-212456/`（概念页 712 / 待处理 43 / 裁判规则库 634 文件）；
2. dry-run 预览（674 文件将改，抽样对照语义正确、零误伤）；
3. `--apply` 落地：括号归一 **674 文件**；
4. 复扫三个受影响目录：残留三重 `[[[` = **0**、残留引用式嵌套 `[[A [[` = **0**。

**权威复测（kg_scan v1.4，2026-08 图谱）**：
| 指标 | 处置前 | 处置后 |
|---|---:|---:|
| **unresolved_links（断链）** | 100 | **0** ✅ |
| total_notes | 2778 | 2781 |
| total_links | 24307 | 24400 |
| orphan_count | 118 | 117 |
| linked_nodes | 2660 | 2664 |

→ **W34「断链再生 0→100」假阳已彻底消解，连接层断链恢复 0。**

**遗留待办（非阻塞）**：
- ~~`resolve_broken_links.py` 生成概念桩页时仍会写出三重/嵌套括号（本次根因），建议在桩页生成侧同步加括号校验，避免下周自愈再次自产断链；~~ **✅ 已于 21:30 闭环（见下）**
- kg_scan 正则加固（建议③）可作为双保险，降低未来误判概率。

---

## 根因闭环（21:30）：`resolve_broken_links.py` 生成侧括号校验（v1.2）

**问题定位**：W35 那 100 条假阳的**真正源头**是 `resolve_broken_links.py` 的 `build_page()`——它在「同簇概念页互链」段把 `concept_targets` 里**未经 DIRTY 护栏的畸形目标名**直接拼成 `- [[{s}]]`，当 `s` 为 `[民法典第1218条` 类带 `[` 名字时，吐出 `- [[[民法典第1218条]]`，被 kg_scan 误判为断链（自产断链）。

**修复（v1.2，双保险）**：
1. **扫描期 `clean_target()` 归一**：提取目标名即做 `[[[X]]]→X`、`[[A [[B]]→` 内层优先；归一后若命中现有笔记 → 视为「源链接仅语法畸形、非真断链」直接跳过，从源头不收畸形目标、不建垃圾桩页（消 W35 那 55 条三重括号类）。
2. **生成期 `bare_target()` 净化 + 同簇脏名护栏**：`build_page` 输出的标题、法条母法链接、相关笔记、同簇互链**全部过 `bare_target()`**，保证绝不产出 `[[[X]]`；同簇互链对含 `[`/`]` 的畸形目标一律跳过。

**验证（不写盘）**：
- 单元测试 `_test_bracket_guard.py`：**21/21 PASS**（clean_target / bare_target 归一 + build_page 无 `[[[`、无畸形同簇互链）；
- 集成测试：临时写入 `[[[民法典第1218条]]` 源笔记 → 扫描期正确归一并跳过（不进 unresolved、不建桩页）；
- dry-run 整管：`断链唯一目标=0`（源已干净），无崩溃、未写盘。

**安全**：脚本本体已六-B 备份 `WorkBuddy/Claw/backups/resolve_broken_20260830-212958/`（16857 字节）。回归测试 `_test_bracket_guard.py` 保留于 `03-连接/scripts/` 供后续复用。

→ **根因已堵死：下周阶段2.5 自愈即便遇到畸形源链接，也不会再自产 `[[[X]]` 断链。**

---

*生成：2026-08-30 ｜ 数据口径 kg_scan v1.4（2026-08 图谱）｜ 真阳 0 / 假阳 100（畸形嵌套）｜ 处置后权威断链 = 0 ｜ 根因（生成侧）已闭环 v1.2*
