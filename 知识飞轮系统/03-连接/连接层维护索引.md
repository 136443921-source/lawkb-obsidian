---
created: 2026-08-30
type: 维护索引
tags:
  - 连接层
  - 维护
  - 索引
  - 自动化
---

# 连接层维护索引

> **功能**：把「连接层维护」相关的经验卡、脚本、自动化串成一条可检索的维护链路，消灭"修了 bug 但没人知道怎么触发 / 谁在跑 / 跑完怎么验证"的盲区。
> **创建**：2026-08-30（补建韩杰/众志专属卡 → 双向互链 → 23 卡归位 → 修共享 hubfile 覆盖 bug → 抽 SOP 卡，收尾时建立本索引）
> **维护规则**：新增连接层维护类经验卡 / 改 link_cards_rules.py / 调整周日批处理时，同步更新本页"节点清单"。

## 维护链路总览（数据流）

```
[经验卡片 · 连接层维护类]
   ├─ 经验卡片-连接层共享枢纽聚合覆盖bug修复   (纠错 / 元级卡)
   └─ 工作流卡-连接层维护SOP                  (正向 / 流程卡)
        │  被 link_cards_rules.py 写入 相关_links + 枢纽页
        ▼
[03-连接/scripts/link_cards_rules.py]          (v3.1.1，含共享 hubfile 聚合修复)
        │  由 阶段3「连接层自动补链」调用
        ▼
[周日知识维护批处理 automation-1783920443461]  (每周日 21:00，阶段3)
        │  产出 连接层（枢纽页 + 双向链接 + 知识图谱）
        ▼
[下游消费方]
   ├─ 预判式跨案件经验推送 automation-1784039874813  (每周日 19:30，消费经验卡片推到在办案件)
   └─ 协同效果月报 automation-1784367370450          (每月28 23:30，汇总命中率/推送/反馈/采纳率)
```

## 节点清单

### 1. 经验卡片（连接层维护类）
| 卡片 | 类型 | 落位 | 作用 |
|------|------|------|------|
| [[经验卡片-连接层共享枢纽聚合覆盖bug修复]] | 纠错 / 元级 | `02-提炼/经验卡片/学习笔记/` | 记录共享 hubfile（程序知识/法条解读 共用 `连接枢纽-通用程序.md`）last-write-wins 覆盖 bug 的现象 / 根因 / 修复，并固化"双重验证法" |
| [[工作流卡-连接层维护SOP]] | 正向 / 流程 | `02-提炼/经验卡片/学习笔记/` | 5 步标准维护流程 + 风险点清单（共享 hubfile、顶层孤儿、改脚本未备份） |

### 2. 核心脚本
- `03-连接/scripts/link_cards_rules.py` — 连接层自动补链主脚本
  - **版本 v3.1.1（2026-08-30）**：修复共享 hubfile 覆盖 bug —— `程序知识` 与 `法条解读` 共用同一枢纽文件，旧逻辑按分类逐类 `open("w")` 覆盖写，后写覆盖先写；改为按 hubfile 聚合成员一次写出。
  - 产物：`03-连接/连接枢纽-*.md` + `03-连接/scripts/link_lastrun.json`
  - 备份：`link_cards_rules.py.bak-20260830-1530`、`link_cards_rules.py.bak-20260830-1533`

### 3. 自动化（串联触发器）
| 自动化 ID | 名称 | 频率 | 在链路中的角色 |
|-----------|------|------|--------------|
| `automation-1783920443461` | 周日知识维护批处理（v1.11） | 每周日 21:00 | **直接跑 `link_cards_rules.py`**（阶段3 连接层自动补链）——连接层重建的触发源 |
| `automation-1784039874813` | 预判式跨案件经验推送 | 每周日 19:30 | 消费经验卡片（连接层产出），把经验推到在办案件 |
| `automation-1784367370450` | 协同效果月报（飞轮命中率汇总） | 每月28 23:30 | 汇总推送命中 / 反馈 / 采纳率，反向验证连接层价值 |

### 4. 统计与日志（可审计）
- 运行统计：`03-连接/scripts/link_lastrun.json`（机器可读，含 processed / hubs / rules 覆盖率 / 边）
- 补链日志：`03-连接/孤立笔记检测报告/补链日志-YYYY-MM-DD.md`
- 执行留痕：`.workbuddy/automations/automation-1783920443461/memory.md`

## 关联索引（横向）
- [[案件-卡片-规则三维索引]] — 案件 → 卡片 → 规则 三维全局索引
- [[统一知识索引-2026-08]] — 当月统一知识入口
- [[静默任务审计报告-2026-07-20]] — 自动化留痕 / 静默失效审计依据

## 操作提示（照 SOP 执行）
1. 新增 / 移动经验卡片后，必须重跑 `link_cards_rules.py`（或等周日 21:00 批处理）。
2. 重跑后**双重验证**：每卡 `related_links` 含枢纽 + `grep 对应枢纽页` 确认 MOC 可见性（共享 hubfile 分类尤需核查）。
3. 改脚本前 `cp -n` 备份 + 改后 `python3 -c "import py_compile"` 语法校验。
4. 凡单源文件重建 / 覆盖，先 `cp` 时间戳备份 + `ls -l` 核对字节，异常即回滚（六-B，源自 ERR-20260810-001）。
