# IMA 通道阻断 · 路径 C 换源无法执行（2026-09-03 11:23）

> 触发：自动化 `automation-1783920420205` 续跑，意图完成 2026-09-02「路径 C 换源」（绕开 16 个 220030 死链、从 5 库各补 3 篇新鲜可 fetch 文章，使 `ingested_total` 334→349）。

## 一句话结论
**本会话 IMA 通道掉线，无法对候选做 `fetch_media_content` 探活，路径 C 换源被基础设施阻断；按摄入中断铁律保持诚实状态（`ingested_total=334` / `failed_220030=16` / 窗口 open），未回写任何伪造进度。**

## 诊断证据（实测）
- `ToolSearch` 全量检索：`mcp__ima-mcp__*` **全部 absent**（仅 `agent-mail`、`yuandian-mcp` 在线）。
- `connector-states.json`（v4 schema）实读：`ima-mcp` → `"bound": true, "enabled": false`；`"staleManagedAuthHeadersPurged": true`、`"headerOverridesBearerStripped": true`。
- 分型判定：与 2026-09-02 实证「IMA 重启后 ima-mcp 特殊分型」完全一致——**鉴权头被 purge + enabled 被系统权威状态覆写回 false**。

## 为什么不能强行推进
1. **运行态掉线铁律**：配置健康但工具不在索引 → 会话级，不执行 `--apply`，等待恢复。手工翻转 `enabled` 会被系统权威状态覆写（已实证无效）。
2. **摄入中断铁律**：取全文失败（含通道不可用）**绝不回写 `ingested`**。220030 探活是路径 C 的必要前置，无 IMA 即无法验证。
3. **修复路径唯一**：须在 WorkBuddy「连接器管理」对 `ima-mcp` 点 **重新信任 / 重新授权（re-OAuth）**，由系统自身写回 enabled 并拉起 MCP 工具。无人值守自动化无法执行该 UI 动作。

## 当前诚实状态（未变更）
| 项 | 值 |
|---|---|
| `ingested_total` | 334（零泄漏） |
| `failed_220030` | 16（持久内容侧失败，非通道） |
| 窗口 `w_2026-09-02_A1B_channel_down` | open / done 0 |
| 窗口 `w_2026-09-03_A1B_channel_down` | open / done 0（09-03 08:42 登记，N=2 升级阶梯） |
| 元典余额 | 2549（未消耗，本日零 IMA/元典调用） |

## 修复路径（需老强手动）
1. **P0**：WorkBuddy 连接器管理 → `ima-mcp` → **重新信任 / 重新授权（re-OAuth）**。
2. **P0**（内容侧）：IMA 内对 16 个 WeChat 文章文件「查看 / 重新处理」，消解 220030（含义：IMA 内容侧不可用，msg 明示「请至 ima 内查看处理」）。
3. 完成 1+2 后说「继续」→ 首步探活 220030 消失即执行批量回补。

## ⚠️ 重试关键纠正（务必带入下轮）
- **「预留卡号」已过期，不得再用**：09-02 memory 称 R-PI-205/206/207 等 FREE，但 09-03 实地 `find` 查库结果为 **R-HT=128 / R-CF=099 / R-PI=212 / R-LD=006 / R-LN=045 / R-HG=061**——预留号区间已被后续运行消耗。**回补取号一律实地查库 +1，memory 预留号仅供参考。**
- 本地法规库存在条号错位污染（道交解释（二）公众号转载版与法释〔2026〕9 号不一致），`source` 含 `mp.weixin.qq.com` 的文件禁止作条号级引用源。

## 待人工 / 下轮
- IMA 重连 + 16 文件内容侧修复后，按 30 篇（09-02 + 09-03 两个漏窗）目标回补，取号实地查库。
- 回源补案号（前轮已登记）：R-HT-129、R-PI-213、R-LD-007、R-CF-100 等。
