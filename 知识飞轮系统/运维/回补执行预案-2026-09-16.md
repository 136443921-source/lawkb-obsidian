---
created: 2026-09-16T21:29
updated: 2026-09-16T21:38
---
# IMA 漏窗回补 + 三通道补核 · 合并点火预案 v2（2026-09-16）

> **状态：⏸ 待点火** —— 当前 `ima-mcp` 仍 `userDisabled=true`（无 `mcp__ima-mcp__*` 工具），回补段无法执行；三通道（pkulaw / qcc-legal / yuandian-mcp）已在线，补核段依赖已满足。
> **点火条件**：用户在连接器管理页对 **ima-mcp** 完成「重新信任/授权」+ 重启，本会话出现 `mcp__ima-mcp__*` 工具后，回复「点火」即按本预案一把梭。
> **执行顺序**：先跑【阶段一 三通道补核】（不依赖 ima，立即可行）→ 再跑【阶段二 IMA 回补】（依赖 ima 重连），两阶段同批次出合并报告。

---

## 阶段一 · 三通道补核（5 张 09-16 D 卡，source_verified:false → true）

| 卡号 | 域 | 主题 | 当前核验状态 | 需补核字段 |
|---|---|---|---|---|
| R-HT-280 | HT | 承揽质量瑕疵减价/绿色原则 | WebSearch·未核 | 法条有效性、类案案号 |
| R-CF-176 | CF | 慈善信息公开/关联交易处罚 | WebSearch·未核 | 法条有效性（慈善法99/110）、处罚案例 |
| R-PI-399 | PI | 医美广告虚假宣传罚21万 | WebSearch·未核 | 法条有效性、贵阳美贝尔处罚决定书 |
| R-LD-055 | LD | 工亡补助金工伤发生时基数 | WebSearch·未核 | 人社部13号令第11条、案例 |
| R-PI-400 | PI | 交强险限额内全额赔付 | WebSearch·未核 | 道交法76条、宁国法院案号 |

**三通道分工（D0.7）**：
- `pkulaw`：法条有效性 + 现行有效确认 + 指导案例/公报案例检索（不传 type，避免排除普通案例库假阴性）。
- `qcc-legal`：法条逐字原文交叉核验 + 司法案例检索。
- `yuandian-mcp`：案例向量检索 + 司法解释 `sxx` 年份/有效性核验（**计费主杠杆 `top_k=1`**，D 阶段一律 `top_k=1` 控成本）。

**补核铁律**：
- 找不到权威案号/条文 → 标 `source_verified:partial`（部分核验），**绝不编造**案号或法条。
- 仅在三通道至少两源交叉印证后才置 `source_verified:true`，frontmatter 补 `verified_by:三通道(yuandian+pkulaw+qcc-legal)` + `verified_at:2026-09-16`。
- 同步更新 5 篇案例摘要的案号/法条字段。

---

## 阶段二 · IMA 漏窗回补（45 篇，3 漏窗 → backfilled）

| 漏窗 ID | 目标 | 已补 | 状态 |
|---|---|---|---|
| `w_2026-09-14_A1B_channel_down` | 15 | 0 | open |
| `w_2026-09-15_A1B_channel_down` | 15 | 0 | open |
| `w_2026-09-16_A1B_channel_down` | 15 | 0 | open |
| **合计** | **45** | **0** | **待回补** |

- `ingested_total` 现状 **411** → 回补后须 = **456**（增量恰好 45，零泄漏）。
- **5 库映射**（来自 `ima_intake_state.json`）：

| 库 ID | 名称 | 已摄入 |
|---|---|---|
| `7312048136419112` | 合同文书AI助手 | 86 |
| `7312042960642489` | 律师AI助手 | 83 |
| `7312035322822509` | 人伤法律实务助手 | 84 |
| `7333014572917409` | 合规与政府监管AI助手 | 77 |
| `7311644304633438` | 慈善组织合规AI助手 | 81 |

**流程（主控—子代理三段式）**：
1. 挖候选池（主控）：5 库各跑 SELECT_3（深翻页+文件夹递归+媒体类型过滤），`since` 回溯至 `2026-09-14`，三重去重（已摄入 media_id / 已回补 / 跨库重复），每库 3 篇/日 × 3 日 = 9 篇，5 库合计 45。
2. 取号（双维度）：每卡经 `next_rule_id.py -d <域> --n 1` 单一真源取号，域分布按内容判定（医疗+交通→PI，工伤→LD，合同→HT，慈善→CF），分配即回写治理表与排期表。
3. 蒸馏（子代理）：每库一子代理并行——`ima-mcp.fetch_media_content` 取全文 → 采集笔记 → 生成规则卡+案例摘要。**未取到全文一律不写 ingested**；220030 内容侧损坏文件登记 `failed_220030`，窗口维持 open，等用户 IMA 内「查看/重新处理」后回补。

---

## 合并执行纪律（与 v1.27 一致）
- **六-B 备份**：改写 `ima_intake_state.json` / `标签索引.md` / 任何单源文件前，先 `cp` 至 `/tmp` 时间戳目录并 `ls -l` 比对字节数。
- **取号纪律**：绝不手编 rule_id；建卡后 `rule_id_guard.py --since 60` 须 **ERROR 0 / 同号冲突 0** 方可 `--commit-all`。
- **零泄漏**：ingested_total 全程只增不回退；漏窗回填后置 `backfilled`，不逆写已摄入条目。
- **成本门禁**：三通道 `top_k=1`；元典余额当前=0，补核前先查 `yuandian_get_user_balance`，若仍 0 则阶段一降级为「pkulaw+qcc 双通道」并标 `source_verified:partial`。
- **不代点 UI**：本预案不含任何 UI 点击；重授权由用户完成。

## 点火后验证清单（合并）
- [ ] `mcp__ima-mcp__*` 工具已出现；5 库 `get_knowledge_list` 探活成功（对照组可取回=通道健康）
- [ ] **阶段一**：5 卡 `source_verified` 由 false → true/partial；frontmatter 补 `verified_by`+`verified_at`；案号/法条字段回填
- [ ] **阶段二**：45 篇候选池三重去重通过；规则卡取号 `rule_id_guard` 零冲突
- [ ] `ingested_total`: 411 → **456**（增量恰好 45，零泄漏）
- [ ] 3 个漏窗状态置 `backfilled`，`pending_windows` 无残留 open
- [ ] 新产生 `failed_220030` 登记台账（如有），窗口维持 open 不误关
- [ ] 写 `04-LOG/2026-09-16.md` 合并回补段 + 更新综合报告

## 用户前置动作（必须，红线外）
WorkBuddy **连接器管理页** → 找到 `ima-mcp` → 点 **「重新信任 / 重新授权」** → 走完 OAuth → **重启连接器**。重连后回复「点火」，即触发本合并预案。

---
## 🔥 执行记录（2026-09-16T21:35 用户「点火」）

**阶段一 · 三通道补核：✅ 部分完成（qcc 单通道）**
- 触发核验：用户回复「点火」。
- 通道实测：
  - `ima-mcp`：`userDisabled=true` 仍在配置态故障，**无工具注入** → 阶段二锁定（见下）。
  - `yuandian-mcp`：`pointBalance=0` → 按预案降级，不耗积分。
  - `pkulaw`：调用返回 **90001 鉴权失败**（"checking remaining points" 报错）→ 不可用。
  - `qcc-legal`：**可用** ✅ → 走 qcc 单通道核验。
- 核验结果：5 张卡核心法条经 **qcc-legal 逐字核验全部通过**（民法典781/9、慈善法14/110、广告法16、工伤保险39、道交法76/民法典1213），与卡文一致。
- 卡状态变更：`source_verified: false → partial`；`verified_by: WebSearch → qcc-legal(单通道·元典0/pkulaw鉴权失败降级)`；`source_degraded: true → false`；补 `verified_at` + `verification_note`；底部「来源降级」提示改为「三通道补核记录」。
- 遗留（仍 `partial`）：5 案案例（横州窗帘/贵阳美贝尔21万罚/宁国交强险/民政部处罚/乌鲁木齐工亡）及司法解释（人社部13号令第11条、道交赔偿解释）为 WebSearch 整理，**待元典/pkulaw 通道恢复后补权威案号级核验**。

**阶段二 · IMA 漏窗回补（45 篇）：⛔ 未执行**
- 原因：`ima-mcp` 仍 `userDisabled=true`，本会话无 `mcp__ima-mcp__*` 工具，用户尚未在 UI 完成重授权。物理前提不成立。
- `ingested_total` 维持 **411**，3 个漏窗（09-14/15/16 各 15 篇）维持 `open`。
- 解锁条件：用户 UI 重授权 ima-mcp + 重启 → 本会话出现 `mcp__ima-mcp__*` 工具 → 回复「点火/继续回补」即跑。

---
*预案 v2 合并生成：2026-09-16T21:32 · 来源 AI（小强）· 阶段一已于 T21:35 部分点火（qcc单通道），阶段二待 ima 重连*
