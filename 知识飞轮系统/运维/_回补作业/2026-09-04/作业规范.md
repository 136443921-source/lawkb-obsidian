# 09-02/03/04 三窗合并回补 · 蒸馏作业规范（v2）

> 主控（小强）下发，分库子代理共用。任何一条都是硬约束，不得自行放宽。
> ⚠️ v2 变更：**一切输入都在 LawKB 实体路径下，不要用 `/tmp`**（子代理沙箱看不到主控的 /tmp，2026-09-04 实证）。

## 一、任务背景

老强（实习律师，贵州贵阳）的「知识飞轮系统」有 3 个漏窗（09-02 / 09-03 / 09-04）因 IMA 通道掉线未摄入，合计 45 篇。
2026-09-04 12:52 IMA 通道恢复（对照组 fetch 实证成功），授权开跑回补。
你负责**一个库的若干篇**：取全文 → 写采集笔记 → 造规则卡 → 回报结果。

## 二、输入

选文清单：`/Users/chenyouqiang/Documents/LawKB/知识飞轮系统/运维/_回补作业/2026-09-04/选文清单.json`
（JSON：库名 → 数组，每项含 `rule_id` / `folder` / `media_id` / `title` / `lib` / `lib_id`）

**只读你负责的那个库 + 指定的 rule_id**，`rule_id` 与 `media_id` 一一对应，**不得自行换号、不得跳号、不得重排**。

## 三、取全文

1. 先 `ToolSearch({tool_names:["mcp__ima-mcp__fetch_media_content"]})`；
2. 命中则 `DeferExecuteTool({toolName:"mcp__ima-mcp__fetch_media_content", params:{media_id:"<清单里的 media_id>"}})`。

### 🔴 若 ToolSearch 查不到该工具（子会话未注入 IMA MCP）
**不要空转、不要瞎猜工具名、不要凭记忆编内容**。改为走**本地原文兜底通道**：
- 先查 raw 目录：`/Users/chenyouqiang/Documents/LawKB/知识飞轮系统/01-采集/IMA缓存/2026-09-04/_raw/<rule_id>.md`
  （主控会把取不到的原文落到这里）
- 有 → 直接读该文件做蒸馏，状态标 `OK (raw)`；
- 没有 → 状态标 `BLOCKED 无原文`，**立即停止该篇**，继续下一篇；
- 若 **9 篇里超过 3 篇 BLOCKED**，立刻停止全部作业，回报主控，不要继续烧轮次。

### ⚠️ 220030 铁律
返回 `220030`（或内容明显为空/截断到几百字）→ **立即放弃该篇**，**绝不写任何 ingested 标记**，
回报标 `FAIL 220030`，继续下一篇。不要重试超过 2 次，不要改 media_id 格式去猜。

## 四、产出物（每篇两件）

### 4.1 采集笔记
路径：`/Users/chenyouqiang/Documents/LawKB/知识飞轮系统/01-采集/IMA缓存/2026-09-04/<库简称>-<标题>.md`
- 库简称：`合同库` / `律师库` / `人伤库` / `合规库` / `慈善库`
- 文件名 `<标题>` 取原文标题前 30 字，**去掉 `/ \ : * ? " < > |`（正斜杠也必须去掉）**
- frontmatter：
```yaml
---
created: 2026-09-04
updated: 2026-09-04
tags:
  - <库简称>
  - 回补-2026-09-04
media_id: <完整 media_id>
source_lib: <库名>
---
```
- 正文：原文要点结构化摘录（**不是全文照搬**，是知识密度高的提纲 + 关键数据/条款/公式/判旨原文），
  保留可直接复用的：裁判规则、计算口径、条号、金额标准、清单、话术。末尾写 `## 原文信息`（作者/发布时间/来源）。

### 4.2 规则卡
路径：`/Users/chenyouqiang/Documents/LawKB/知识飞轮系统/06-沉淀/裁判规则库/<folder>/<rule_id>-<短标题>.md`
- `<folder>` 与 `<rule_id>` 直接用清单里的值，不要改
- `<短标题>`：6–18 字，概括该卡核心，不要照抄原标题

**frontmatter 模板**：
```yaml
---
title: <标题>
rule_id: <分配号>
card_type: 实务规则卡
source: <原文出处：公众号名 / 发布方 / 文件全称，尽量具体>
type: <域>·实务规则卡
created: 2026-09-04
date: 2026-09-04
created_month: 2026-09
review_date: 2027-03-04
geo_scope: <贵州省 / 全国 / 待核>
source_verified: false
library: 小强律师数字分身系统
aliases:
  - <分配号>
ingested_from: IMA-<库名>
ingested_at: 2026-09-04
media_id: <完整 media_id>
---
```

**正文八段结构**（段标题保持一致）：
```
# <标题>

> **卡型**：实务规则卡｜**编号**：`<rule_id>`｜**建卡日期**：2026-09-04

## 一、规则要旨（正向·怎么用）
## 二、审查要点（操作路径，编号步骤）
## 三、构成要件与举证（表格：要件编号 / 要件名称 / 要件内容 + 举证责任分配）
## 四、法条依据（权威源回填，非凭记忆）
## 五、抗辩与但书
## 六、翻车标本（负向拦截·HIR）
## 七、来源与地域效力
## 八、关联（知识飞轮连接层）
```
体例参照样本：`06-沉淀/裁判规则库/合同风险/R-HT-178-人防车位转让与被拆迁人拆迁安置房购买权转让.md`

## 五、🔒 法条铁律（防幻觉，最高优先级）

1. **法条正文绝不凭记忆默写**。只能从两处取：
   - 本地权威源：Grep `/Users/chenyouqiang/Documents/LawKB/法律法规库/`
   - 或华宇元典 MCP `mcp__yuandian-mcp__yuandian_rh_ft_detail`（fgmc+ftnum，**按次计费，能用本地就用本地**）
2. 本地查不到又不想花积分 → **不要写该条法条正文**，在「四、法条依据」写：
   `> ⚠️ 待回源：《XXX》第N条（本地库无源，未核填；**不得作文书引用源**）`
   并在 frontmatter 加 `statute_text_pending: true`
3. 每张卡引用的法条必须标**效力状态**（现行有效 / 已废止 / 已被取代）
4. **司法解释必须核对文号与年份**（如道交解释（一）是法释〔2020〕17号，不是〔2012〕19号）。拿不准标待回源
5. 公众号转载的法规条文**不得作条号级引用源**

## 六、其他硬约束

- **互链一律用文件名**：`[[R-HT-177-以房抵债合同的审查要点]]`（**不含 `.md`**），禁止用 title 互链
- **编号格式**：`R-(PI|HT|CF|HG|LN|PR|SH|GZ|CS|JG|HY|GS|LD|HJ)-\d{3}`，三位补零，**严禁 R-CASE- 或日期型编号**
- **隐私红线**：不写入任何个案卷宗信息、当事人真实姓名/身份证号
- **思政/政治内容中性沉淀**，保留原文表述，不附加政治解读
- 若原文是**法规/规章原文**（慈善库大概率是）→ `card_type: 规范要点卡`，
  重点提炼"合规要点 + 义务主体 + 违反后果 + 适用边界"，
  并在「七、来源与地域效力」注明：全文已归档至 `01-采集/IMA缓存/2026-09-04/<文件名>`
- 若本地 `法律法规库` 已有同名法规，**不要重复新建法规文件**，只在卡片注明"本地已有"并比对版本

## 七、回报（严格按此格式，控制在 40 行内）

```
## <库名> 回补结果
| rule_id | 状态 | 采集笔记 | 规则卡 | 字数 | 备注 |
|---|---|---|---|---|---|
| R-XX-000 | OK / OK (raw) / FAIL 220030 / BLOCKED 无原文 | 文件名 | 文件名 | 3200 |  |
...
成功 N 篇 / 失败 M 篇
⚠️ 待回源法条：<rule_id>: 《XXX》第N条
⚠️ 其他异常：<简述>
```
状态只许用：`OK` / `OK (raw)` / `FAIL 220030` / `BLOCKED 无原文`。

**不要**在回报里粘贴文章全文，主控只看表。文件写完即可，主控统一回写状态文件。
