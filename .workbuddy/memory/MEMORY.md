---
tags:
  - 慈善法
  - 合同法
  - IMA
  - 民事诉讼
  - 笔记
  - 公司法
  - 2026
  - 劳动争议
  - 元典
  - 知识飞轮
---

# LawKB 知识飞轮系统 - 长期运行原则

## ⭐ 核心指令（2026-07-23）
投喂存储知识→知识飞轮→赋能小强律师数字分身。分批沉淀、投喂前去重、大批量分批+可回滚。

## 🔒 跨会话硬约束
1. 互链用 `[[文件名(不含.md)]]` 2. 个案卷宗禁投喂（仅通用模板/方法论）3. 思政中性沉淀不附政治解读 4. 慈善决策须引《贵州省厚德公益基金会合规管理手册（试行）（2025版）》具体条款 5. 法律问题先 lawyer-yourself-skill

## 🔌 通道故障处置（可复用）
**IMA**：① 配置态（enabled=false/不在索引）→ UI 重新信任/授权 + 重启；运行态掉线（配置健康但工具不在索引）→ 等恢复不 --apply。② 双开关对照定位：生效目录 `~/.workbuddy/connectors/75c7265a-5f11-4420-9e71-be28329d68fd/`，以元典为对照组——正常 `connector-states.json.enabled=true` 且 `mcp.json` 无 `disabled`；故障 `enabled=false` + `disabled:true`。改完复检是否被系统覆写，再重启拉起 MCP。③ **IMA OpenAPI wiki v1 无取全文端点**（get/fetch_media/content/detail/download 全 404），只能取列表+search 片段 → 取全文必须走 ima-mcp `fetch_media_content`。④ `connectors/default/mcp.json` 非生效配置，别改。
**元典 unauthorized 根治**：工具在索引但实调 `{"error":"unauthorized"}` → 查 `.credentials.v3.json` 的 `mcpOAuth.<conn>.expiresAt` 未过期则非 Key 失效而是头未注入；向 `~/.workbuddy/connectors/<id>/mcp.json` 的 `mcpServers["connector:yuandian-mcp"]` 注入 `headers:{"Authorization":"Bearer <Key>"}`，远程 MCP 下个请求即生效无需重启。Key 明文不回显不落日志。

## 🆔 media_id 与 220030
- 格式：`wechatarticle_<32位用户ID>_<32位rawid><16位库ID>`（库ID直拼 rawid 后无分隔符）；rawid 必取全 32 位，8 位截断即 220030。
- **220030 终态**：对照样本 fetch 成功 + 目标全 220030（端点健康、IMA 列表 parse_progress:100/can_fetch_content:true 系假成功）→ 内容侧持久损坏，**绝不写 ingested**，登记 `failed_220030`（status=retry，幂等），窗口维持 open；须用户在 IMA 内「查看/重新处理」或等后端自愈后一键回补。
- **排查 220030 必须跑对照组**：同库另一文件可取回 + 目标失败 = 内容侧损坏；对照组也失败 = 通道/权限问题。

## 🤖 大批量回补「主控—子代理」架构
三段式：① 主控脚本 HTTP 挖候选池（翻页+文件夹递归+三重去重）→ ② 主控分配 rule_id 产出 `选文清单.json` → ③ 每库一子代理并行蒸馏（取全文→采集笔记→规则卡）。
铁律：① 子代理看不到主控 `/tmp` → 作业文件落 LawKB 实体路径；② MCP 在子会话随机注入 → 子代理首步探活，探不到走 `_raw/<rule_id>.md` 兜底，≥3 篇 BLOCKED 即停摆回报主控；③ **取号双维度**（文件名 `find R-<域>-*.md | tail` + 全库 frontmatter `rule_id` 扫描，取较大者+1），分配后立即写回治理表与排期表；④ 回写幂等（dry-run→apply→复跑验证）；⑤ **未取到全文一律不写 ingested**。
**子代理报「本地库无源」≠ 真无源**：民法典条文一律先查本地 `法律法规库/通用实体法/中华人民共和国民法典（全文）.md`（4577 行，行首「第X条+全角空格」），查不到再花元典。

## 📖 本地民法典抽取（幂等用行号切块，勿用正则）
按行首条号切块（起于「第X条」止于下一「第X条」/文件尾），阿拉伯条号→中文数字（10-19「十X」、整十不加「零」），回填标 `（本地·...md:起止行号）`。正则 lookahead 对"锚点标题已删"卡会失效→重复插入。

## 💰 元典积分治理
- `top_k` 是 `rh_ptal_search` 计费主杠杆：`top_k=1`≈5分 vs `top_k=5`≈46分；D 阶段一律 `top_k=1`。
- 已知法规名+条号 → `rh_ft_detail`（1–2分）；禁用 `rh_ft_search` 探路；`get_user_balance` 免费；元典查询一律不传 `type`（否则排除普通案例库假阴性）。
- 禁止用单次调用前后余额差推单价（异步5–15min结算）；免费额度每月3500。

## ⚖️ 法条时代校正 & 源可信度
- 引用司法解释须核对元典 `sxx` 字段+年份；民法典后条号重排常态（旧买卖解释18→622、15→623…）；慈善法2023把用途变更由55→56条；新旧条号双检索。
- ⛔ **公众号(mp.weixin.qq.com)转载法规禁止条号级引用**——只作线索，须 court.gov.cn/人民法院报/省级法院官方三方交叉核验。
- 回源必查"关联索引里的下位规章"（如 R-LD-008 经《因工死亡职工供养亲属范围规定》第2条由"类推"升级为"规章明文"）；计算类卡必查兜底/上下限条款（如《工伤保险条例》第64条第2款60%兜底）。

## 📌 当前稳态（volatile，详各日期日志）
- IMA 摄入 `ima_intake_state.json`：ingested_total=349（09-04 回补 15 后）。
- 09-02 窗已关；09-03/09-04 仍 open（共30篇，曾因限频/通道中断）。续跑资产：`知识飞轮系统/运维/_回补作业/2026-09-04/`（作业规范.md+选文清单.json）。
- 待办：① 16 个 persistent_220030 待人工核验 ② 剩余27篇（人伤9/合规9/慈善9）待续跑 ③ 11组待回源法条。

*拆分文件：[[自动化与工具链]] · [[版本库规范]]*
