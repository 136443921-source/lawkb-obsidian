---
tags:
  - 慈善法
  - 合同法
  - IMA
  - 民事诉讼
  - 笔记
  - 公司法
  - 07
  - 08
  - 2026
  - 劳动争议
---

# LawKB 知识飞轮系统 - 长期运行原则

> 跨会话长期运行原则与约定。详细执行日志见各日期 `YYYY-MM-DD.md`。
> **2026-08-30 归并精简**：原 126 行/17KB 压缩重构，章节内独立编号；删除连接层自动补链的「相关笔记」噪音区块；稳态数据更新至 08-30；并入 08-30 新经验。原文备份 `MEMORY.md.bak_20260830-144219`。

---

## ⭐ 核心指令（用户明示 2026-07-23，优先级高于一次性任务）

**「将所有存储知识投喂给知识飞轮系统，经提炼沉淀，赋能小强律师数字分身。」**

- **投喂对象**：每日知识摄入自动化产生的文件（走自动化分批沉淀，**非**全量盲写）。
- **执行约束**：① 投喂前**去重**（同名/同主题 check）；② 大规模投喂**分批+可回滚**，先映射后写入；③ 与每日摄入子任务 A3 协同。
- **当前状态**：全量知识源（知识库/案例库/法律法规库/合同类法律文书/[[人伤专业领域|人伤专业领域]]/学习笔记/案件管理通用方法论/贵州法院判例库/笔记助手）已投喂完毕；188 篇中 164 篇已转写为 141 经验卡 + 124 裁判规则并激活为分身铁律检索目标。

---

## 🔒 跨会话硬性约束

1. **互链用文件名**：`[[文件名(不含.md)]]`（basename 匹配），不用 title；`related_links` 仅展示不参与边计算。
2. **隐私红线**：`案件管理/` 仅投喂「通用模板/方法论」；个案卷宗（当事人/判决书/代理词/证据）一律排除。纯程序性法条知识经抽样确认无隐私后可脱敏为「通用程序知识」。
3. **思政类中性沉淀**：[[习近平法治思想|习近平法治思想]]/党史法治轨迹/争做满意好律师 等按「[[律师职业规范·思政培训|律师职业规范·思政培训]]」中性存储，保留原文、不附加政治解读。
4. **慈善合规内规引用**：慈善法问题决策须引用《[[贵州省厚德公益基金会合规管理手册（试行）（2025版）|贵州省厚德公益基金会合规管理手册（试行）（2025版）]]》具体条款（另见 ~/.workbuddy/MEMORY.md「慈善组织合规专家身份」）。
5. **法律问题优先级**：先 lawyer-yourself-skill（小强律师数字分身），不能回答才转其他智能体/搜索。

---

## 🛠 自动化与工具链

| 自动化 | ID | 版本 | 要点 |
|---|---|---|---|
| 每日知识摄入 | automation-1783920420205 | v1.17 | A3 扫描 `知识库/`；6 阶段 + checkpoint 续跑 |
| 周日知识维护批处理 | automation-1783920443461 | v1.11 | 断链自愈 21:30 → 补链 21:40 → 重刷图谱 |
| 月度回灌 | automation-1786370581518 | v2.0 | 控盘式回填 |
| 稳定性监测守卫 | automation-1784788482072 | v1.1 | 每日 23:30，仅追加缺口告警 |

**intake_runner 标准库**（`知识飞轮系统/03-连接/scripts/`，三自动化已接入）：
- `intake_runner.py` — IntakeState 单一真源 + 原子写 + totals 每次重算 / IntakeMachine checkpoint 续跑 / DegradationLayer 220030 判定与告警升级 / **StatuteHealthCheck 法条体检** / SELECT_3 去双重计数 / IntakeConfig 场景声明。
- 三 driver：`intake_daily_driver.DailyRun` · `intake_sunday_driver.SundayRun`（状态 `03-连接/check_links_state.json`）· `intake_monthly_driver.MonthlyRun`（状态 `03-连接/monthly_backfill_state.json`）。
- 周日/月度传 `libs={}` 空库不污染 `ima_intake_state`；中断后自动续跑，不需人工"请继续"。

**连接层脚本**（同目录）：`kg_scan.py`（图谱扫描/边计算）· `kg_html.py`（HTML）· `link_cards_rules.py`（补链 **v3.1：动态遍历规则库全部子目录**，2026-08-04 修复 17 子目录仅映射 7 的遗漏）· `resolve_broken_links.py`（断链→概念页 v1.3）· `fix_source_links.py`（畸形链接 v1.1）· `backfill_yuandian_ids.py`（华宇元典回填 v1.2）。

**概念页护栏（长期原则）**：`resolve_broken_links.py` / `kg_scan.py` 的链接解析索引须覆盖 **LawKB 工作区全量**（跨目录真实文件算已解析，勿误判断链建概念页）；两脚本护栏**同源一致**。误建/噪音概念页移 `概念页/.trash-<日期>/`（可回滚，manifest.txt 记录映射）。`META_SKIP` = `孤立笔记检测报告` + `知识库压缩去重报告`。

---

## 🔌 通道故障处置规范（2026-08-06 确立，08-30 验证有效）

1. **IMA 故障分型**（诊断入口 `03-连接/scripts/fix_ima_connector.py`；状态文件 `~/.workbuddy/connectors/75c7265a-5f11-4420-9e71-be28329d68fd/connector-states.v3.json`）：
   - **配置态**：`userDisabled['ima-mcp']=true` 或不在 `enabled` → 执行 `--apply` 翻转 + **重启 WorkBuddy**（08-30 实测：重启后通道完全恢复，成功补回漏窗）。
   - **运行态掉线**：配置健康但工具不在索引 → **会话级，不执行 `--apply`**，等待恢复。
2. **摄入中断铁律**：取全文失败**绝不回写 `ingested`**，只登记 `b_pending_backfill`。否则去重表被污染，候选被误判为已摄入而永久丢失。
3. **QQ 推送兜底**：`qq-mail` MCP 不在索引时改走 `~/.workbuddy/skills/qq-email/scripts/send.js`（SMTP 465）。参数是**位置参数** `<收件人> <主题> --stdin`（非 `--to`）；MCP 的 confirmation_token 有效期约 2 分钟，无人值守场景**直接切 SMTP**，勿等待确认。**不再降级为「响应正文替代推送」**。
4. **案例源降级标记**：法随 MCP 不可用用 WebSearch 时，文件必须标 `pending_source_upgrade`，待恢复回源补全。
5. **可用性判据**：连接器面板的 `connected/disconnected` 是**滞后快照，不可作判据**——一律以 `ToolSearch` 索引 + `DeferExecuteTool` 实调为准。
6. **media_id 构造（防 220030 假失败）**：`media_id = "wechatarticle_62fe55a7567bc291dfbbee29900b27c3_" + <32位十六进制 rawid> + <kb_id>`。rawid **必须取全 32 位**，8 位截断即返回 220030。遇 220030 **先核长度再判通道**。
7. **降级笔记不覆盖**：公开源降级笔记与 IMA 真源笔记若主题不重叠，降级笔记属独立资产，只消解 `ima_source_pending: true→false` + 加 `resolved_note`，不删除不覆盖。
8. **回补须翻页（08-30 强化）**：`get_knowledge_list` 首页 top-N 常已全部摄入，须用 `next_cursor` 翻页扩池。但**候选耗尽时应如实记 0**，不得灌水凑配额（价值保障条款第 4 条）。

---

## 🔎 裁判规则库回填与在线对账铁律（2026-08-29 确立）

1. **华宇元典查询一律不得传 `type` 参数**：`type`（`ptal`/`qwal`）不传则不筛选。历史对账传 `type:"qwal"` 把整个普通案例库排除，造成大量假阴性。除非明确只要权威案例，否则**禁止传 type**。
2. **共现笔记锚点污染（写入型任务通用）**：连接层会把其他笔记文件名写进卡片末尾「关联/相关笔记」区块与 `related_links`，其中常含案号但**非本卡源案例**（实测误挂 14 张，如 R-PI-145 工伤认定卡被挂房屋租赁案号）。**任何"从卡片提取元数据"的任务，锚点只从本体内容提取**：frontmatter 剔除 `related_*`，正文截断至首个「关联/相关笔记/共现笔记」标题前。宁可少挂不可错挂。
3. **vault 严格 YAML 不可解析（354/612 张）**：title 里的 wikilink 等导致 `yaml.safe_load` 失败但 Obsidian 容忍。**批量改 frontmatter 必须逐行编辑，禁止 yaml 往返序列化**。校验"零损坏"要**与写前基线备份比对失败集是否一致**，而非看绝对失败数。
4. **存在并发写入进程**：`updated` 时间戳被别的进程刷新、行内列表被重序列化为块式列表，导致幂等性不收敛（08-30 再次确证）。**派生数据不要写进 frontmatter**；大段 frontmatter 匹配易失败，改用**定点锚点插入**。幂等验收：连续 3 次 dry-run 需改写文件 = 0。
5. **回填脚本**：`backfill_yuandian_ids.py` v1.2（dry-run 默认 / --apply 落盘 / 写前整目录备份 + 文件数断言 / 幂等）。待实证队列 `03-连接/yuandian_verify_queue.json`（100 案号）；单次查询约 1.5 万字符，宜分批消费。当前覆盖率 96/612 = 15.7%，516 张无案号锚点诚实挂起 `yuandian_source_pending`（**不得伪造案号**）。

---

## 🧹 快照/备份与版本库 → 见独立文件

**已拆出独立文件：[[版本库规范]]（`memory/版本库规范.md`）** —— 6 条完整规范（.gitignore 对已跟踪文件无效 / `*.bak` 只匹配文件不匹配目录 / 写入型批处理快照策略 / 批量改忽略规则前全量误伤验证 / 快照排序禁用 mtime / `!` 白名单规则），信息一条不丢。

> 拆分原因：主文件字符超限，该章低频但完整，拆出后主文件降约三成。
> 涉及版本库瘦身、快照清理、gitignore 规则调整的任务，**先读该文件**。

---

## 📌 当前稳态（2026-08-30 更新）

- **连接层**：密度 0.0032→0.01684；孤儿 410→31(3.1%)；断链 919→**0**（2026-08-04 续跑补正后长期维持）。
- **IMA 摄入水位**：`ima_intake_state.json`；**ingested_total = 292**（08-30 回补 3 篇后）。5 库近期候选已高度消耗——合同库 top-30 仅 1 篇未摄入、律师库 top-24 全摄入、慈善库 top-20 全摄入。
- **漏窗**：`w_2026-08-30_A1B_channel_down` target=15 / done=3 / status=partially_backfilled（通道恢复后实测库内无更多未摄入候选，如实记录）。
- **法条体检库**：`StatuteHealthCheck.KB` 已登记 20+ 条，含 2 条 repealed（侵权责任法58、公司法71）、1 条不溯及（公司法88 ← 法释〔2024〕15号）、3 条 exact 待核。
- **图谱产物**：`03-连接/知识图谱-YYYY-MM.html`；补链日志 `03-连接/孤立笔记检测报告/补链日志-*.md`。
- **待办**：① 周日批接入 08-30 的 9 张新卡到枢纽 MOC + 重刷图谱；② 合规库 `0cf7ff35` 仍 PERSISTENT 220030，待人工核验。

---

## 🆕 2026-08-30 新增经验

1. **`run.statute(law, article)` 条号必须传整数**：传字符串（`'1222'`）会静默返回 unknown 假阴性，误判为"未登记需核验"。
2. **zsh 会对 `**` 做 glob 展开**：含 markdown 加粗的 `python -c` 内联脚本会被破坏（报 "no matches found"）。含 `**` 的文本处理一律写成 .py 脚本文件再执行。
3. **A2.5 实时库校验须三维**：年度 + 口径 + **索引指向哪个文件**。08-30 年度无滞后却查出**口径误用**型 P0 错误（贵州丧葬费误用社保全口径 87,894→43,947，正确应为非私营在岗职工 106,570÷12×6=**53,285**，单项差 9,338 元）。
4. **同省份多文件并存时索引只挂一个是重大风险**：办案按索引进入即拿到滞后/错误值。索引须显式列出文件分工。
5. **`score_article()` 返回 `(分值, 命中词)` 元组**，取 `[0]`；直接对返回值取负数会报 `TypeError: bad operand type for unary -: 'tuple'`。
6. **ingested 集合混有 dict 与 str 两种类型**，且含 `web_publicsource_*` 格式——用 `split('_')[-1][:32]` 提取会产出「2026-08-06」这类垃圾 rawid（潜在误判源）。去重脚本须做类型兼容与格式校验。

---

*最后更新：2026-08-30 ｜ 来源：用户明示核心指令（07-23）+ 链接/隐私/思政/慈善约束 + 工具链注册 + 通道故障处置规范（08-06，08-30 验证）+ 裁判规则库对账铁律（08-29）+ 08-30 归并精简与回补实战。*
*拆分记录：2026-08-30「🧹 快照/备份与版本库」整章 → [[版本库规范]]（`memory/版本库规范.md`，1792 字符，6 条完整保留），主文件留索引指针。备份：`MEMORY.md.bak_20260830-144219`（17077 字节，归并前）、`MEMORY.md.bak_20260830-144908`（13010 字节，拆分前）。*
