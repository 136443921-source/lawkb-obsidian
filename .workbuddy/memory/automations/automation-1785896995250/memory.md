---
tags:
  - md
  - 快照
  - ##
  - self
  - 校验
---

# 自动化执行记忆：self.md 每周快照冷存

## 任务定位
单点脆弱分身记忆的最后一道防线。源：`~/.workbuddy/skills/xiaoqianglvshi/self.md`（single source of truth）；备份：`LawKB/数字分身快照备份/self.md快照/`。全量系统 2026-06-11 已终止，此快照为唯一兜底。

## 最近执行记录（仅高层摘要）
- **2026-09-07 09:13（北京）**：首次执行。源校验通过（170801 字节 > 200）。备份目录不存在 → mkdir -p 创建。无既有快照 → 直接写入 `self.md-20260907.md`，字节一致 170801，非空校验通过。清理保留 12 份（当前仅 1 份，无删除）。日志追加「变更=有变化，当前快照数=1」。结果：✅ 成功。

## 关键规则（来自任务指令，防漂移）
1. 源缺失/≤200 字节 → 输出「⚠️ self.md 备份失败...」并停止，不写任何文件。
2. 与最新已有快照字节相同且 diff 为空 → 跳过写入，仅日志标「无变化」。
3. 写入后必须校验：目标存在、字节数=源、非空。
4. 仅保留最近 12 个快照（文件名日期降序），删更早。
5. `备份日志.md` 仅追加；格式 `- [北京时间 YYYY-MM-DD HH:MM] 快照 self.md-YYYYMMDD.md（<字节> 字节）变更=<有变化/无变化>，当前快照数=<N>`。
6. 只读源 + 复制，绝不修改源 self.md 或飞轮数据。输出经 LTI 文本监控器 wrap。

## 备注
- 快照文件较大（约 170KB），present_files 会展示新快照；属单次结果，非长期关注项。

## 相关笔记
- [[决策卡-交付校验每步校验闭环-20260905]] (共现关键词: 校验, ##)
- [[请求权基础卡族-对撞校验报告-2026-09-08]] (共现关键词: 2026, 校验)
- [[法律文书防幻觉提示词_LTI校验版]] (共现关键词: 校验, ##)
