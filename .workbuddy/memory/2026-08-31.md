# 2026-08-31 工作日志

## 每日知识摄入（v1.21，automation-1783920420205）

- **通道**：ima-mcp 运行态掉线（配置健康、工具不在索引）→ 判定会话级掉线，**未执行 --apply**，登记漏窗 `w_2026-08-31_A1B_channel_down`（15 待回补，写前备份至 /tmp/intake_backup_0831/）。A1/B 零摄入，ingested_total 维持 304。
- **元典**：余额 2566→2521（D 阶段 3 次语义检索 −45）🟢 正常；台账已更新。
- **A2 医疗三类（WebSearch 官方源）**：R-PI-188（手术操作指南遵循·原因力）、R-PI-189（医疗事故罪结果归责·韩杰再审）、R-HG-058（电子病历身份标识保管·娄底10号案）。
- **A2.5**：贵州实时库三维核验通过，丧葬费 53,285 无回退，零修正。
- **C**：学习资料推荐 10 项（P0：操作指南/结果归责/电子病历规范第9条）。
- **D**：yuandian 直采 3 案 3 卡——R-HT-115（检验期限怠于通知）、R-PI-190（急诊PCI过错·20%）、R-SH-036（股权款加速到期+一般保证推定）；排除 08-29 已采案号。
- **收尾**：checkpoint done=[A2,A2_5,C,D,FINAL]；报告+04-LOG+台账已写；QQ SMTP 推送成功。
- **待办**：① 通道恢复后回补漏窗 15 篇；② 周日批接入 08-30 的 9 张 + 今日 6 张新卡到 MOC + 重刷图谱；③ 合规库 0cf7ff35 人工核验；④ 待核法条 5 组。

**经验**：运行态掉线（工具不在索引）与配置态故障（userDisabled）判定路径不同——前者只登记漏窗等待恢复，绝不 --apply；本轮以 ToolSearch 精确查找 + DeferExecuteTool 实调双确认，避免误判。

## P0 回补预执行（10:20）

- ima-mcp 仍不在索引（双确认）→ 真源摄入待 MCP 恢复；未写 ingested（铁律）。
- **新发现**：ima-skill HTTP 通道可挖候选——`search_knowledge_base` 拿完整 kb_id（get_addable 返回截断 ID 会 220004）；`get_knowledge_list` 支持 folder 递归 + 翻页；**但无全文接口**（get_doc_content 210005 / search 高亮空 / fetch_media_content 是 MCP 专属）。
- 全 5 库 S1+S1.5 挖掘：**候选池 1825 篇未摄入**（各库均撞 400 上限），精选 15 篇（5库×3，医疗/在办优先）存入 `_backfill_pick_0831.json` + 漏窗 `candidate_pool.shortlist_15`。
- 下轮：MCP 恢复后按 shortlist 直接 fetch→蒸馏→ingest 闭环。

## P2 双项核验（10:40）

- **0cf7ff35（合规库 220030）**：HTTP 通道全库遍历确认**元数据仍在库中**（《公司治理合规管理百问百答（八）关联交易》，folder_7338800883716878）→ 判定为 IMA 内容解析侧故障非删除；failed_220030 更新为 needs_manual_check。
- **待核法条 8 组全部核验为现行有效**（华宇元典 rh_ft_search 在线核验）：民法典 578/686/687/1218/1221/621 + 医师法 55 + 条例 9/47。
- **重要纠偏**：医师法 55 第(五)项准确表述是"违反法律、法规、规章或者**执业规范**，造成医疗事故或者其他严重后果"（非"诊疗规范"）——已修正 R-PI-188 并补录 KB。
- **补录 StatuteHealthCheck.KB** 6 条（578/686/687/1221 + 医师法55 + 条例9/47，含 exact 原文）；intake_runner selftest 全通过。
- 元典消耗 40 分（8 次 rh_ft_search × 5 分）。

## P0 周日批接枢 + 图谱刷新（11:20）

- **核查**：08-30 的 21 张 + 今日 6 张 = **27 张卡全部已在枢纽 MOC**（接枢完成度确认）。
- 重跑 `link_cards_rules.py`：processed 877 / rules_linked 634（+6）/ edges 4737；uncovered 无。
- 图谱刷新：kg_scan（2801 笔记/24523 链接/孤儿 118）+ kg_html（知识图谱-2026-08.html 重新生成，写前备份 /tmp/kg_backup_0831/）。
- **P0 待办②完成**；剩余 P0 仅「MCP 恢复后回补漏窗 15 篇」。

## 回补预执行：定位并修复配置态故障（11:00）

- 老强指示按 shortlist_15 回补 → 探测发现**配置态故障**（userDisabled['ima-mcp']=True 且不在 enabled，早间 A0.0 时还是健康的）——这就是工具不在索引的根因，非早间判定的"运行态掉线"。
- 已执行 `fix_ima_connector.py --apply` 自愈（enabled+=ima-mcp、userDisabled→False，备份 .bak-20260831-105546）；**需重启 WorkBuddy 生效**。
- HTTP 通道全文路径穷尽确认无路（笔记≠知识库两套体系，知识库文章无 doc_id 镜像）→ 真源摄入必须等 MCP 工具加载。
- 铁律遵守：未写 ingested（304 维持），漏窗 pending（shortlist_15 已锁）。
- **下次动作**：重启后确认 `mcp__ima-mcp__*` 出现 → 按 shortlist 15 篇 fetch→蒸馏→ingest 闭环。

## 漏窗 15/15 闭环（12:20，P0 完成 ✅）

- 老强重启 WorkBuddy → `fix_ima_connector.py --apply` 生效，`fetch_media_content` 回归索引。
- **实补 15/15（304→319）**：合同 R-HT-116~118 / 律师 R-LN-038~040 / 人伤 R-PI-191~193 / 合规 R-HG-059~060+R-LN-041 / 慈善 R-CF-092~094。
- shortlist 替换 2 篇重复候选（法务案件分析报告、捐赠回扣、疫情捐赠均 08-30 已摄入）；2 篇初试 220030 系 media_id 构造错误（kb_id 少位/rawid 旧值）修正后成功，无新增真实 220030。
- link_cards_rules 892/649/4828 自动接枢；图谱刷新（2831 笔记）；漏窗 backfilled 15/15 + consumed。
- **今日 P0/P2 全部清零**。

## 自动化配置变更：D 案例采集 3→5 槽位（11:20）

- 老强指令：每日知识摄入 D 阶段由「合同/人身伤害(医疗优先)/商事 各1」改为**每日 5 案例**——合同类 1 / 慈善组织(社会组织类) 1 / 医疗类 1 / 工伤类 1 / 交通类 1。
- 默认定域：合同→R-HT / 慈善→R-CF / 医疗→R-PI / 工伤→R-PI 或 R-LD / 交通→R-PI；医疗/工伤/交通拆为独立槽位互不挤占。
- 自动化 prompt 升级 v1.21→**v1.22**（改 9 处：标题/变更记录/任务目标⑤/D 标题/D1/D1.1/D4 定域/QQ 摘要 D 行/价值保障第7条/版本说明）；元典日耗估算 +10 分。

## 元典免费政策确认 + 自动化 v1.24（11:35）

- 老强问「元典积分怎么回事」→ 查证官方政策（华宇软件官网 2026-07-27 + open.chineselaw.com）：普通用户**每月 3,500 免费积分**（登录领取、长期）、新注册送 50,000、专业版付费定制（RPM≤400）、免费版 RPM≤50。
- **账算**：历史日均 233 分 ≈ 6,990 分/月 = 免费额度的 2 倍 → 收紧势在必行。
- 自动化 v1.23→**v1.24**（省分收紧一档，13 处验证全过）：D 全禁语义⑥ / 案例详情⑨仅 1 情形 / 法条核验一律⑤（1分）/ 每日 D 预算 ≤30 分 / 预警阈值 <1500 / 免费额度纪律（月耗 ≤3,000）。
- 台账新增「三.5 积分获取渠道」章节 + 11:28 记账行（2439）；余额 2439。
- **经验**：① 元典积分非"充值制"而是"月度免费额度制"，**每月去 open.chineselaw.com 领 3,500 分**是可持续前提；② 写 Python 内联脚本时反引号会被 zsh 命令替换吃掉——含反引号的替换必须落 .py 文件（08-31 第三次踩坑，教训固化）。

## 新建自动化：每月元典免费积分领取提醒（11:44）

- 老强指示把"每月 1 号提醒领取元典免费积分"设为定时提醒。
- 创建 `automation-1788147876816`「每月元典免费积分领取提醒（1号）」：FREQ=MONTHLY;BYMONTHDAY=1;BYHOUR=9，ACTIVE，cwds=LawKB。
- 职责：查余额（不计费）→ 追加台账记账行 → 输出提醒摘要（含 open.chineselaw.com 领取入口）；只提醒不检索、不消耗积分；MCP 不可用时纯文字提醒兜底。

## 元典 API Key 替换凭证（11:47）——验证通过，连接器需 UI 操作

- 老强提供 sk_ 开头元典开放平台 API Key，选择「替换元典连接器凭证」。
- **key 验证**：curl 直连 https://open.chineselaw.com/mcp/law/stream、/mcp/case/stream、/mcp 三端点，带 Bearer 均 HTTP 200（yuandian-law / yuandian-case / yuandian-open 握手成功）→ **key 有效**。
- **连接器机制结论**：当前 yuandian-mcp 凭证为 OAuth token 型（`.credentials.v3.json` mcpOAuth，加密存储，expiresAt≈2026-09-02 快到期）；连接器认证头（headerOverrides Authorization）同样加密托管（iv/tag/ct）→ **手工改文件会破坏加密结构，不可行**。
- **正确路径**：WorkBuddy 连接器管理 UI → 华宇元典 → 更新凭证（填 sk_ key 或重新授权）→ 重启生效。
- 已备份 /tmp/yuandian_key_backup/（mcp.json / .credentials.v3.json / connector-states.v3.json）以备。
- **经验**：连接器凭证一律加密托管，API Key 替换只能走 UI；sk_ key 不入记忆文件（密钥纪律）。

## 元典凭证替换完成（11:56 验证）

- 老强在连接器 UI 完成凭证更新（11:55 文件时间戳刷新）。
- **双判据验证通过**：① ToolSearch 索引正常（yuandian 工具在）；② DeferExecuteTool 实调 get_user_balance 成功（code 200）。
- **凭证刷新确认**：`.credentials.v3.json` yuandian-mcp expiresAt 由 2026-09-02 → **2026-09-07 11:55**，tokenType=Bearer → 新凭证已生效（UI 重新授权模式，非手工 headerOverrides）。
- 余额 2439 与替换前一致 → 新旧凭证绑定同一元典账户，符合预期。
- 备份 /tmp/yuandian_key_backup/ 保留数日后可清理。
