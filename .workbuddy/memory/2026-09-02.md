# 2026-09-02 工作日志

## 每日知识摄入自动化核查（10:26）
- 用户问今日摄入自动化未跑完原因。
- 实证：本轮 fetch_media_content 3 探活（L1合同 / L4合规 / L5慈善）全 220030，叠加前序 22/22，确认 IMA fetch 端点全局故障、未自愈。
- 窗口 `w_2026-09-02_A1B_channel_down` 仍 open/done0；`ingested_total` 334 未动。
- 结论：非配置态（ima-mcp 已 enabled+索引正常，get_knowledge_list 通），系 IMA 后端内容取回失败（persistent_220030），须等恢复或人工在 IMA 内处理 15 文件。
- 15 真·未摄入目标已 `is_ingested` 实查复核 = 0/15，锁定有效，待端点恢复一键回补。

## IMA 重启后连接器故障处置（18:09）
- 用户报「IMA已重启」→ 探活 fetch + LIST 双双报 `proxy_gone`（非 220030，是代理会话失效）。
- 诊断根因：IMA 重启把 ima-mcp 连接器配置打回 `enabled:false`（活跃 ws `75c7265a-.../connector-states.json` 第38行），代理随之注销 → proxy_gone。
- `fix_ima_connector.py` 失效：它找 `connector-states.v3.json`，但当前实际文件是 `connector-states.json`（脚本文件名过时）。
- 处置：备份 `/tmp/lawkb_ima_connstate_bak_20260902-1809.json` → 手工翻转 `ima-mcp.enabled:false→true`（原位改写，凭据不动）。
- 待用户动作：**重启 WorkBuddy 或 UI 重载 ima-mcp 连接器**，使 MCP 代理重新注册；重启后说「继续」→ 首步探活，若 220030 消失即一键回补 15 篇（R-HT-129/130/131 等），关窗 + ingested_total 334→349。

## 「继续」回补受阻：手工翻转被覆写（18:28）
- 用户重启后回「继续」→ ToolSearch 重发现 ima-mcp 工具，但 `fetch_media_content`/`get_knowledge_list` 仍报「not found in deferred tools index」→ 工具未加载。
- 复查 `connector-states.json` 第38行：**`ima-mcp.enabled` 又变回 `false`**。我上步手工翻转被 WorkBuddy 在重启/重同步时覆写回去。
- 文件另含 `staleManagedAuthHeadersPurged:true` + `headerOverridesBearerStripped:true` → 重启时 ima-mcp 鉴权头被判定过期并 purge。
- **结论（推翻前假设）**：IMA 重启后 ima-mcp 在 WorkBuddy 侧真实状态=「需重新信任/重新授权」，手工改 `connector-states.json` 会被系统权威状态覆写、无效；`fix_ima_connector.py` 同理（且它找的文件名 `connector-states.v3.json` 也已过时）。
- **正确修复路径（待用户）**：WorkBuddy 连接器管理里找到 ima-mcp → **重新信任 / 重新授权（re-OAuth）**。这才会让 WorkBuddy 把 enabled 置 true 并真正拉起 MCP 服务、加载工具。完成后回「继续」。
- 回补主任务（15 篇）仍 pending，等通道恢复。

## IMA 退出重登 + 重连后仍 enabled:false（18:43）
- 用户按指引：断开 ima-mcp → IMA(ima.qq.com) 退出重登 → WorkBuddy 重连 ima-mcp（"已处理"）。
- 复核 `connector-states.json`：mtime 更新为 18:42（重连确实写了文件），但 **`ima-mcp.enabled` 仍为 `false`**；工具索引仍无 `mcp__ima-mcp__*`。
- 判读：reconnect/re-trust 只刷新授权，未把 enabled 置 true；或 WorkBuddy 内存态仍把 ima-mcp 判为禁用、每次写盘回写 false。
- 待用户确认 UI 实况：ima-mcp 是否有独立「启用/Enable」开关且处于 OFF；以及重连后状态文字（已连接/需授权）。若启用开关翻回 OFF 或不存在 → 需在「令牌已新鲜」前提下重启 WorkBuddy 清内存禁用态，再重连。
- 仍未破解；15 篇目标保持锁定（is_ingested 复核 0/15），窗口 open、ingested_total 334 未动。

## 2026-09-02 11:11 续跑（新会话）：15 篇规范登记为 persistent_220030（终态）

**触发**：系统注入无人值守续跑 + 用户「请继续」，承接 `w_2026-09-02_A1B_channel_down`（target15/done0）。

**动作**：
1. 确认 ima-mcp 本会话 `connector-states.json` `enabled:true` 且 `fetch_media_content` 在工具索引（ToolSearch 实证 schema 完整）→ 通道健康。
2. **全量复探 15 目标** → **15/15 仍 `code:220030`**（msg「该文件获取失败，请至ima内查看处理」）。前序对照实验（已摄入非目标「婚内财产协议」fetch 成功）已证 fetch 端点全局健康，坏的只是这 15 篇本身内容侧 → 判 `persistent_220030`。
3. **规范登记（非干等）**：先备份状态文件 `/tmp/lawkb_ima_intake_state_bak_20260902-1111.json` → 经 `DegradationLayer.on_fetch_fail(state, mid, "220030", channel_ok=True)` 逐一登记进 `failed_220030`（**15/15 成功，status="retry"**）。该路径与摄入中断铁律一致：**绝不写 ingested**。
4. 窗口维持 `open`/`done:0`，`ingested_total` **334 不变**。

**验证 PASS**：window `open`/done0、failed_220030 `1→16`（本 15 + 既有 1）、**0 泄漏入 ingested**、ingested_total=334。

**终态结论**：15 篇为 IMA 内容侧持久失败，IMA 重启 / UI 重连 / 退出重登均未修好（其 IMA 列表 `parse_progress:100`/`can_fetch_content:true` 系假成功）。须**用户在 IMA 内对这 15 文件查看/重新处理**使内容可拉取，或等 IMA 后端自愈。之后说「继续」→ 首步探活 220030 消失即一键回补 15 篇（R-HT-129/130/131、R-LN-045/046/047、R-PI-205/206/207、R-HG-062/063/064、R-CF-100/101/102），关窗 + ingested_total 334→349。

## 2026-09-02 23:08 续跑受阻：ima-mcp 本会话未连接（回补第一道关卡死）

**触发**：用户「请立即回补」→ 探活 6 篇（L1×3 + L2×3）全 220030（同 18:57 会话）；用户「请继续」→ 续探剩余 9 篇。

**阻断（比 220030 更前置）**：
1. 续探首篇即报 `Tool "mcp__ima-mcp__fetch_media_content" not found in the deferred tools index`；ToolSearch 重确认 ima-mcp 工具不在本会话索引。
2. 核查活跃 `connector-states.json`：`connectors` 字典仅含 `tencent-docs`(enabled:false) / `yuandian-mcp`(enabled:true)，**全文件 grep 零命中 `ima-mcp`** → ima-mcp 本会话被解除绑定 / 未连接。
3. 本会话 `connector-status` 亦仅列 `agent-mail` + `yuandian-mcp`，无 ima-mcp。

**结论**：**连取数通道都没有**，别说回补、连探活都发不出。且即便重连，18:57 探活 6/6 仍 220030 表明内容侧持久失败未消（双重阻断叠加）。

**回补须同时满足两条件**：
① 在 WorkBuddy 连接器管理重新连上 ima-mcp（重新信任/授权；必要时重启 WorkBuddy 重载工具索引）；
② 用户在 IMA 内对 15 文件「查看/重新处理」消掉 220030（内容侧）。
两步齐了说「继续」→ 首步探活 220030 消失即一键回补 15 篇，关窗 + ingested_total 334→349。

**状态文件未改动**：窗口仍 `open`、ingested_total 334、failed_220030 含本 15（幂等，重连后不会重复登记）。

---

## 23:10 案件材料自动归档（automation-1783919599675）续跑

`2026-09-02 23:10 | 运行 | CaseDrop空/无新文件 | 无异常` — CaseDrop 根目录 0 待处理文件，第2–6步不执行，零写入。

附只读一致性核查（距上次 08-06 近月）：经验卡片已按主题重组 15 子目录共 222 张，6 张历史真实卡全在，**无丢失**；映射索引 9 案件行齐全。**发现 2 项缺口待确认**：① 娄山华庭经验卡在 `03-连接/概念页/` 只是断链消解器 stub 存根，`02-提炼/经验卡片/` 实际缺卡（幻影链接）；② 百益服务中心上诉案复用道真百益卡、无独立卡。另有 2 个顶层 .bak 可清理。默认不擅自补录，等老强拍板后按六-B（先备份+核字节）处置。

留痕位置本次起迁至 `.workbuddy/memory/automations/automation-1783919599675/memory.md`（旧 CaseDrop 位置已加迁移说明，保留历史不再追加）。

---

## 23:39 补两缺口（案件归档自动化遗留项，用户"两个都办"）

`2026-09-02 23:39 | 成功 | 补真实卡1+独立卡1+索引2行+清stub1+清bak2 | 无异常` — 六-B 全程（备份 `/tmp/archive-fix-20260902-234142`）。
- 真卡：`02-提炼/经验卡片/非诉合规/经验卡片-娄山华庭房屋租赁合同审查.md`（消除幻影链接，删 stub 概念页）。
- 独立卡：`02-提炼/经验卡片/慈善组织合同纠纷/经验卡片-厚德基金会诉百益服务中心合同纠纷上诉案.md`（与一审道真百益卡区分）。
- 索引两行已补卡链接；清顶层 2 个 .bak 残留。
- **遗留**：上诉案"理事会决议撤销×合同解除"主线源自映射索引 5 维画像，专属源材料多在 `CaseDrop/processed/厚德基金会诉百益服务中心合同纠纷上诉案/`，待回源深化。
