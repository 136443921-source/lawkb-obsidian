# 2026-08-29 工作日志

## 裁判规则对账自动化 v3.0 月度索引合并（补执行）

- **背景**：automation-1783957225528 于 2026-08-28 完成了 v2.0 对账报告（`03-连接/裁判规则对账-2026-08.md`），但 v3.0 新增的「月度索引合并」步骤（P2-1）未执行。本日续跑补全。
- **合并脚本**：`知识飞轮系统/03-连接/scripts/merge_rules_monthly.py --month 2026-08 --apply`（幂等+写盘前自动备份）。
- **结果**：
  - `裁判规则库.md`：已含 08-13 建立的 202 卡 2026-08 索引段，幂等跳过，未改动（86240B）。
  - `合同风险规则库.md`：脚本追加 22 张 R-HT（当前 `date:2026-08` 扫描），再一致性补录主索引缺失的 R-HT-001~016，最终 **38 张 R-HT** 索引（5872B）。
- **关键发现**：`date:` frontmatter 字段漂移——主索引 202 卡（08-13）与当前扫描 77 卡不相交，纯靠 `date:` 扫描已不可靠。建议 P1 固化 `created_month` 或 `date:` 字段。
- **六-B 安全**：写盘前手动备份 + 脚本内置 `shutil.copy2` + 补录前再备份，三道均通过，仅追加未重建主文件。
- **留痕**：`.workbuddy/automations/automation-1783957225528/memory.md` 首建并写入执行记录行。

## 每日知识摄入 v1.17 续跑（16:24–17:0x，恢复上午中断 run）

- **恢复**：状态文件 08-29 中断 run 未持久化 checkpoint（回退显示 08-28 completed），以 fresh DailyRun 重新初始化；A0.0/A0.1 通过（ima-mcp 配置健康、工具索引可用）。
- **摄入 6 篇**（283→289）：合同1（买卖合同详细版 e9c40ca3→蓝红双视角 R-HT-110）+ 律师1（企业合规体系建设指引 c9fb85aa 回补→R-LN-033）+ 人伤3（鉴定机构六维度 f580b70d→R-PI-177 / 鉴定六节点 79e2aa78→R-PI-178 / 卫健委投诉 e40f503d→R-PI-179，全医疗纠纷）+ 合规1（公司自行解散清算 6fda0177→R-HG-055）。
- **A2**：医疗三类日课（3 人伤文合成）→ R-PI-180（原因力≠责任比例，指导案例24号）。
- **A2.5**：贵州2026实时库复核无滞后（丧葬费53285 已更，review 2027-02-28）。
- **C**：延伸学习 10 项。**D**：华宇元典直采 3 例（合同检验期 苏04民终3030 / 医疗参与度 沪0151民初3841 / 清算义务人 宁01清申18）→ 3 案例规则卡（R-CASE-20260829-01/02/03），规则—判例互证。
- **产物**：笔记6+规则卡10+案例源3+摘要1+报告+04-LOG；QQ SMTP 推送成功。
- **留痕**：① 合规库 0cf7ff35 仍 PERSISTENT 220030（failed_220030=1），待人工进 IMA 客户端核原文（不重复试取、不污染去重表）；② 医疗三类补强——医疗事故（刑事）类、医疗合规（行政监管）类今日未新增，下轮 A2 优先补「医疗事故罪构成要件」「医疗机构病历书写合规」。

## 知识库季度压缩去重（Q3 刷新轮）删除安全中止（16:30–16:34）

- **背景**：压缩去重自动化扫描出 7 篇「学习资料推荐」空壳日志（MD5 完全相同/0 正文），列为🔴删除候选。用户指示「先六-B 备份 + 复核入链=0 才动手 + git rm」。
- **六-B 备份**：`/Users/chenyouqiang/WorkBuddy/Claw/backups/压缩去重_20260829-163130/知识飞轮系统`（50M / 6391 篇 md），校验通过。
- **关键复核发现**：7 篇入链**全部≠0**（正文真边 4~25 条/篇，合计~124 处）。这些空壳被 `resolve_broken_links` 断链消解器生成的概念页枢纽（`概念页/2026-07-14.md` 等，frontmatter `generated_by: 断链消解器resolve_broken_links v1.0`）以 `[[...]]` 正文真链接挂为「相关笔记」。
- **方法论沉淀**：入链核查三坑——① 建 basename 索引须排除 `.backup/.git/.workbuddy`（否则同名备份造成假冲突）；② `related_links` frontmatter 是展示用、不参与边计算，须与正文真边分开统计；③ 空壳被概念页依赖时删除会重新制造断链。
- **决策**：触发用户铁律「入链=0 才动手」→ **不执行 git rm，7 篇全部保留**。用户 16:34 确认采纳方案A（导航层依赖，删之有害）。本轮最终 0 删除 / 7 保留 / 库结构不变。
- **留痕**：报告回填 5.9–5.11；`automation-1783919198533/memory.md` 追加 16:30 中止 + 16:34 结案两行。

## 裁判规则对账·date规范化补执行 + 权威源切换华宇元典（16:4x）

- **date 规范化续跑**：`03-连接/scripts/normalize_date_field.py` 初版正则 `R-[A-Z]+-\d+\.md$`/`R\d+\.md$` 仅覆盖 ID 式命名，**漏掉 180 张"描述式后缀"卡**（如 `R-SH-027-有限公司…md`、`R030-提供劳务…md`），导致"424/424 已规范化"实为子集口径（全量 604 张中仅 424 具 created_month）。
- **修复**：正则放宽至 `^R.*\.md$`（经核验 604 张 R 卡均含合法 frontmatter，安全）；`--apply` 写盘（六-B：自动整目录备份 `裁判规则库.bak-20260829-164234`/617 文件 + 文件数断言 + 幂等）。结果：跳过 424、实际变更 180（新增 date 116 / 新增 created_month 180）；**写盘后复核 604/604 具 date+created_month（100%）**。
- **权威源切换**：法随案例库（fasui）已断开不可用，对账权威源切换为 **华宇元典（yuandian-mcp，已连接）**。华宇元典实核 4 张可查卡（frontmatter 内嵌案号）全部 `code:200`：R-SH-002（指导案例67号）、R-PI-034（指导案例24号）、R-HT-018（指导案例33号）、R-HT-095（最高法民抗59号），id 入报告 3.1。
- **局限性（重要）**：法随 `source_case_id`（整型/短码）与华宇元典 id（32 位哈希）**不兼容**，华宇元典仅能覆盖 4/604 张；全量在线对账完整性依赖「案例回填」为每张卡写入华宇元典可查标识符。
- **产物**：报告 `03-连接/裁判规则对账-2026-08.md` 新增第八节（date 规范化）+ 第三节改为华宇元典校验（含 4 卡核验表 + 局限性 + 法随历史台账）；自动化 `memory.md` 追加执行记录、标题与权威源表头同步更新。

## 医疗三类定向补强（A2 单独补两篇，16:43–16:5x）

- **触发**：用户确认上午留痕项，指定补「医疗事故罪构成要件」「医疗机构病历书写合规」，并要求"现在就单独补这两篇"。
- **去重核查（关键）**：先排查发现两主题在库内已密集覆盖——规则卡层 `R-PI-152`（医疗事故罪七情形+行刑衔接）、`R-PI-172`（病历书写10大败诉漏洞）已存在；02-提炼 另有 医疗事故罪严重不负责任认定七情形、病历书写不规范三层法律责任、病历管理合规义务与行政处罚 等十余篇。盲建重复件将违反投喂去重铁律。
- **策略＝补强（additive，非重复）**：两篇均指向既有覆盖的"未覆盖层"——
  - 医疗事故罪 → 教义学认定争议 + 2025"新综合说"（重大过失向度·李某雪案二审无罪）：[[R-PI-181-医疗事故罪严重不负责任教义学认定争议与新综合说]] ＋ 笔记 `02-提炼/人伤法/医疗事故/医疗事故罪构成要件教义学争议与新综合说补强-2026-08-29.md`。
  - 病历合规 → 三层法律责任框架 + 2026 最新判例口径（民法典1222·医师法24·梁某羊水栓塞案90%·130万5次封存案）：[[R-HG-056-医疗机构病历书写合规三层法律责任与推定过错最新判例口径]] ＋ 笔记 `02-提炼/合规实务/医疗机构病历书写合规三层法律责任与2026判例口径补强-2026-08-29.md`。
- **产物**：笔记2（补强纪要，均显式 cross-link 既有覆盖）＋ 规则卡2（R-PI-181 / R-HG-056）；**未碰摄入配额**（ima_intake_state.json 未动），未重复 08-29 已摄入 6 篇。
- **留痕**：两卡已入对应枢纽 MOC（人伤法/合规）；下轮 A2 再触医疗刑事/行政时，优先核验是否已被本补强覆盖，避免二次重复。

## 17:10 裁判规则库·华宇元典标识符批量回填（自动化 automation-1783957225528 续跑）

- **任务**：为用户指令「为 604 张卡批量补写华宇元典可查标识符，打通全量在线对账」收口。卡片实为 612 张。
- **产出脚本**：`知识飞轮系统/03-连接/scripts/backfill_yuandian_ids.py` v1.2（dry-run 默认 / --apply 落盘 / 写前整目录备份 + 文件数断言 / 幂等 / 自动生成明细报告与待实证队列）。
- **落盘结果**：612 张全写 `yuandian_checked`；**96 张（15.7%）可在线对账** = 32 实证命中带 32 位 hash id + 4 命中未取 id + 1 实证未收录 + 59 有案号待实证；**516 张无案号锚点诚实挂起** `yuandian_source_pending`。
- **两个关键修复**（已写入项目 MEMORY.md 第 8、9 条）：
  1. 华宇元典查询**不得传 `type` 参数**——此前 `type:"qwal"` 排除整个普通案例库，造成大量假阴性。
  2. **共现笔记锚点污染**——`## 关联/相关笔记` 区块与 `related_links` 含他人笔记文件名所带案号，误挂 14 张（R-PI-145 工伤卡被挂房屋租赁案号、R-PI-142 被挂编号巧合案号），已隔离。
- **两个工程教训**（MEMORY.md 第 10、11 条）：vault 有 354/612 张 frontmatter 严格 YAML 不可解析 → **必须逐行编辑、禁 yaml 往返**；存在并发写入进程 → **派生数据不入库**，否则写-改-写不收敛。
- **校验全过**：字段自洽 / 互斥性 4 项全 0 / 无伪造 id（全 32 位 hex 实证值）/ 连续 3 次 dry-run 需改写 = 0 / 与写前基线比 YAML 失败集一致（354 个均历史遗留）新增 0。
- **产物**：报告 `03-连接/裁判规则回填-华宇元典-2026-08.md`（198 行）；队列 `03-连接/yuandian_verify_queue.json`（100 个案号，供下月分批消费）。
- **回滚点**：`06-沉淀/裁判规则库.bak-yuandian-20260829-170805`（写入前基线，619 文件）、`-171018`。


## 22:45 obsidian-lawkb 自动备份同步（automation-1782641923967）

- **结果**：✅ 成功。推送 `739f9e2..082425e` 到 `lawkb-obsidian.git`，远端 `ls-remote` 校验一致，工作区残留 0、无 ahead/behind。
- **规模**：提交前 status 3974 变更（新增 181 / 修改 3736 / 删除 57），分 8 批×500 提交；`git diff 739f9e2..082425e` 实际入库 **9338 个唯一文件**——差值因提交期间后台自动化又写入约 5364 文件（06-沉淀 3707 + .backup 2887）被后续批次一并收录。
- **安全预检全过**：密钥扫描 clean（无 .cache_*/secret/token/.env）；57 个删除项经 `find -name <basename>` 全库同名核验，**全部为目录重组移动、真丢失 0**；载荷 38.7 MB、最大单文件 1.3 MB（<GitHub 100MB）；ping github.com 0% 丢包。
- **工程改进**：分批提交改用脚本 `/tmp/lawkb_batch_commit.py`，用 `git add -A --pathspec-from-file=<NUL分隔文件> --pathspec-file-nul`，彻底规避 08-27「含空格/引号文件名解析失败」复发。已固化进自动化 memory.md。
- **待治理（P1，不阻塞）**：`知识飞轮系统/.backup/` 累计 3029 个文件被 git 跟踪（本次新增 2887），`.git` 已 86 MB，建议 `.gitignore` 增 `**/.backup/`；另 06-沉淀 单次涌入 3707 文件，疑为后台批处理产物，建议评估是否纳入版本库。
- **日志**：`logs/git-sync.log` 追加 JSON Lines 一条（含 precheck 四检结果）。
- **LTI**：报告全文经 LTI v4.5.6 `wrap_output()` 校验通过，无 R101/C301 阻断。


## 22:58 快照入库治理（P1）— 第一步已落地，第二步待拍板

**诊断（实地数据，非估算）**
- `知识飞轮系统/.backup/`：18.2 MB / 3029 文件，4 个目录全部是 2026-08-27 批处理快照。
- `06-沉淀` 本次涌入的 3707 文件 = 今天 17:10 华宇元典回填任务反复跑出的 **5 个回滚快照副本**（`裁判规则库.bak-*`，各 614~619 文件、3 MB），非新沉淀内容。
- 快照价值分级（按与正式库 `裁判规则库/` 的 `diff -rq` 差异数）：
  - `-yuandian-20260829-170805` 差异 **612** → 回填前基线，**价值最高**
  - `-yuandian-20260829-171018` 差异 **55** → 中间态
  - `-20260829-162931 / 164234 / 164736` → 脚本迭代期快照，价值低
- 根因之一：`.gitignore` 原有 `*.bak` **只匹配以 .bak 结尾的文件**，而快照是 `xxx.bak-时间戳` **目录**，故全部漏进版本库。
- `.git` 86 MB；知识飞轮系统整体已跟踪 59.7 MB / 8987 文件。

**已执行（第一步 · 止损，零风险）**
- `.gitignore` 新增两条：`**/.backup/`、`知识飞轮系统/06-沉淀/*.bak-*/`（备份 `.gitignore.bak-20260829-225724` 已移库外 /tmp/）。
- 全量 9725 个已跟踪文件校验：命中 6110、**零误伤**；模拟未来新增：新快照 🚫拦截、正式库与案件管理 ✅正常入库。
- 提交 `09b97b93` 已推送 `082425e..09b97b93`。

**待拍板（第二步 · 停止跟踪历史副本）**
- 关键认知：`.gitignore` 对**已跟踪文件无效**，6110 个历史副本仍占版本库，须 `git rm -r --cached` 才停止跟踪（本地文件保留，remote 最新快照移除，但历史 commit 内仍可回溯）。
- 另一关键认知：即使 `git rm --cached`，**`.git` 体积也不会降**（历史 blob 仍在），真瘦身须重写历史（改全部 commit hash + 强推，风险高）。
- 选项已提交老强：A 保持现状 / B 清理 .backup + 3 个低价值 bak / C 全清 6110 / D C+历史重写。

**踩坑记录**
- 备份文件命名 `.gitignore.bak-20260829-225724` 不受现有 `*.bak` 规则约束 → 被误 `git add`。教训：**库内备份文件应直接写库外**（/tmp 或 ~/WorkBuddy/Backups/），或命名为 `*.bak` 结尾。


## 23:10 快照治理收口（老强拍板：方案 B + 加快照保留上限）

**第二步 · 停止跟踪（已执行并推送）**
- `git rm -r --cached` 移除 **4874 个**：`.backup/` 全部 3029 + `-162931/-164234/-164736` 共 1845。提交 `9e5fdaa6` 并推送。
- 保留 2 份基线（`-170805` 差异 612、`-171018` 差异 55），在 `.gitignore` 加 `!` 白名单使其继续受版本控制。
- 校验全过：暂存删除 4874 / 本地 `.backup` 3030 文件仍在 / 两份基线各 618 仍跟踪 / 正式库 618 未受影响。

**源头治理 · 快照保留上限（已落地）**
- `backfill_yuandian_ids.py` 新增 `prune_snapshots()` + 三个参数 `--keep-snapshots N`（默认 2）、`--prune-only`、`--prune-dry`；每次 `--apply` 备份后自动清理。提交 `7dd1e931` 并推送。
- 实测清理：快照 5 份 → 2 份，删除 `-162931/-164234/-164736`，**释放磁盘 9 MB**（06-沉淀 21M→12M）。
- 删前已归档到库外：`/tmp/裁判规则库快照归档-20260829-2300.tar.gz`（1.7 MB / 1914 条目，含全部 3 份）。

**⚠️ 本轮最关键的坑：mtime 不可靠**
- 首版 `prune_snapshots()` 按 `os.path.getmtime()` 排序，预演结果**完全反了**——要删 `-170805/-171018`（本该保留的基线）、保留 `-164234/-164736`。
- 根因：5 个快照目录的 mtime 全被刷成同一时刻（23:53，Obsidian 索引/扫描进程所致），时间序失效。
- 修正：改从**目录名内嵌时间戳**提取 `YYYYMMDD-HHMMSS` 排序（无时间戳者降级 mtime 并当最旧）。修正后预演与预期一致。
- **dry-run 救了一次**——若无预演直接执行，两份回滚基线当场就没了。

**顺带发现与处置**
- 23:00:39 后台批处理给 **148 张裁判规则卡批量补 `aliases: [R-PI-*]` 字段**，已作为知识资产提交 `86cf90d6`（非本轮治理内容，属正常同步）。
- 我的脚本备份 `backfill_yuandian_ids.py.bak-20260829-230518` 也踩了 `.bak-时间戳` 漏洞，已移库外 `/tmp/`。
- 遗留：`.git` 仍 86 MB（`git rm --cached` 不减历史 blob），真瘦身须 `git filter-repo` 重写历史 + 强推，风险高，暂缓。
